You are Pi / `cms-router/minimax-m3`, the second declared fallback for conference role `general_grok45`. Grok Build exhausted both same-session recovery passes with cancelled progress-only output; Cursor CLI returned a static report but its Ask mode rejected every Shell command. Perform a fresh independent executable acceptance.

Hard boundaries:
- Work read-only inside `/Users/smkzw/Documents/AI Products/competitive-intelligence-workflow`.
- Do not edit project files or read any prior participant output.
- Use an OS temporary directory only for project-create and mutation checks.
- Runner-managed output path: `runs/conference/ci_phase0_task04_package_cli/general_grok45_minimax_fallback.md`. Never write that report path with tools; return the complete report and let the runner persist it.

Read these files only:
- `AGENTS.md`
- `context/ci_phase0_task04_package_cli_conference_context.md`
- `package-manifest.json`
- `schemas/package-manifest.schema.json`
- `src/ci_workflow/cli.py`
- `tests/contract/test_package_manifest.py`
- `tests/integration/test_cli_help.py`
- `tests/integration/test_cli_command_catalog.py`

You may then read only workspace files directly referenced by those contracts.

Task:
1. Run the focused tests, full pytest, Ruff, mypy and `uv run ci-workflow package verify --root .`.
2. Run root/nested help, a temporary project create/verify, deferred command negatives and forbidden aliases. Confirm deferred exits exactly 3 and Chinese messages are present.
3. Verify exactly 15 internal Skills, non-implicit policy, `$skill-id` prompt bindings, exact six-command manifest catalog and schema/contract/asset closure.
4. Challenge the new mutation tests by confirming a seventh CLI item and a missing `$monitoring` prompt both fail closed in a temporary copy.
5. Respect the recorded phase boundary: Python wheel is only the CLI distribution; Task 9.5 later produces and fresh-installs the complete `.tar.zst` Skill bundle.
6. Return exactly: `# Conference Participant Output: ci_phase0_task04_package_cli - general_grok45 second fallback`, `## Boundary Check`, `## Independent Work Product`, `## Evidence And Assumptions`, `## Risks, Gaps, And Verification Needs`, `## Recommended Next Step`. State P0/P1/P2 totals; PASS only if P0=0 and P1=0.
