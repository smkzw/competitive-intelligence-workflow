这是同一 Grok Build 会话的完成轮，不得重开会话。上一轮在首次状态句后以 `cancelled` 提前结束，尚未读取文件、运行测试或返回验收结论，因此不能计作验收。

现在从原任务继续并完成全部只读审计：读取上下文列明的设计、计划、七个差异文件及直接依赖；运行精确测试和至少一个仓库外临时反例；不要读取另一参与者或旧会商输出；不要修改仓库文件。完成后按原输出结构返回完整报告，且第一行严格为 `VERDICT: PASS` 或 `VERDICT: FAIL`。PASS 必须写 `P0=0, P1=0`；FAIL 必须给精确文件、行号、可复现反例和最小修复边界。明确列出实际读取文件、实际运行命令及结果；不得只陈述计划。
