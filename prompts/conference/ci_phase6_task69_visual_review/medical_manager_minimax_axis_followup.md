继续同一审阅会话，保持只读，不读取其他审阅报告，不修改文件。

实现方已将共享 `charts.js` 中所有可见横轴与纵轴标签统一显式设为 14px，并重新生成 B 类门户。请仅用真实浏览器复核当前 `output/acceptance/task-6.9/site/` 的首页、纵向结果和安全性：

1. 每个可见 ECharts 实例的 xAxis/yAxis `axisLabel.fontSize` 是否均为 14；
2. 纵向结果同终点各小图纵轴范围是否仍一致；
3. 控制台是否无错误。

只输出三项 PASS/FAIL、实测数值、剩余 P0/P1。不要扩展范围，不作最终验收声明。
