Continue Phase 4 Task 4.1 in the SAME Pi/OpenCode Go session `019ffb9d-3ca6-7000-a8bd-65943cb98a11`. Do not restart, broaden, or self-accept.

Codex independently reproduced four false-greens. Fix them RED-first within the existing Task 4.1 allowlist; additionally authorized package-data copies of the frozen page catalogs under `src/ci_workflow/` and exact contract tests. Do not edit the frozen originals, specs, plan, Trellis/context/prompts/runs/reviews/metrics, Phase 3 or renderer/visual files. No commit/stage.

1. **Mandatory page responsibility authority.** `validate_coverage_set_payload(payload)` currently accepts `page_responsibility_id=not-in-catalog` when `registry` is omitted. The production validator must always load/use the frozen `PageRegistry` by default; callers may inject an alternate registry only through an explicitly test-only/internal validator path, not the public production boundary. The same applies to `validate_report_view_model_payload`: report/page responsibility must belong to the matching frozen catalog. Add direct attacks without passing a registry and with A rows labeled by B/C-only page IDs.

2. **Filtered rows must be canonical objects, not ID-only aliases.** `FilteredRowSet` currently accepts a reconstructed `ReportRow` with the same row_id/scientific identity but altered `disclosure_state`, Chinese label, snapshot/page metadata if row_id remains present. Require every filtered row to equal the canonical view row for that ID byte-for-byte/model-for-model; reject any payload drift. Add attacks for reported value→not reported, label rewrite and snapshot/page drift, while preserving genuine reordering/filtering/empty behavior.

3. **Field-qualified row identity.** `derive_row_id(fact_id="x")` currently collides with `derive_row_id(claim_id="x")` because only values are hashed. Include the identity field names and canonical values in the stable ID material. Add attacks across each adjacent identity field and update fixtures coherently. Sorting, labels, format and filters must still not change row ID.

4. **Installed PageRegistry must work.** Current `PageRegistry.load()` only resolves repository `docs/architecture/page-catalogs`; the wheel lacks these catalogs. Add byte-identical package data copies under a module directory, resolve source-tree + packaged layout without caller-selected production path, and contract-test root/package equality. Build a real wheel, inspect all A/B/C catalog members, install into a fresh venv, and prove `PageRegistry.load()` returns all three reports and can expand a sitemap. Keep temporary build/install material outside repo and clean it after verification.

5. Reassess the Task 4.1 minimum ReportViewModel contract from first principles: it is acceptable for rows to reference immutable fact/claim identities rather than duplicate clinical values only if later consumers cannot fetch a different snapshot or substitute row content. Document and mechanically bind that lookup boundary now (snapshot + canonical row object equality). Do not pre-implement Task 4.4 chart compatibility or domain-specific efficacy/safety fields.

Run each new attack node individually, exact two planned files, Phase0 page/filter/format contracts, Phase3 snapshot/scientific-QC regressions, Ruff, strict mypy, schema/package, wheel isolation, diff, full pytest. Return exact counts and remaining limits for runner-owned `runs/pi_ci_phase4_task41_correctness_followup.md`; do not write it or self-accept.
