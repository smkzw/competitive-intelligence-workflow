Delegated mode. MODE=TEST. 续接既有 `pi/cms-router/minimax-m3:high` 会话完成有界复验。

Hard boundaries:
- 只读；只执行共享复验合同；不得修改文件、联网研究、安全测试或启动其他代理。
- Runner-managed output path: `runs/tests/r13f-named-visual/minimax-followup.md`。不得用工具写入，由 runner 持久化最终回答。

Read these files only:
- `prompts/tests/task102-r13f-targeted-followup.md`

Create/write only this output file:
- `runs/tests/r13f-named-visual/minimax-followup.md`（runner 管理；只返回正文，不用工具写入）

按共享复验合同完成全部操作，不读取其他审阅者输出。
