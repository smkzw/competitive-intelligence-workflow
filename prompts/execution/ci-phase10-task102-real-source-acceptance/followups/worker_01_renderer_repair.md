Continue the same worker_01 session with this repair only. Worker_04 proved that expanded real registry excerpts fail the production C renderer because population/eligibility display normalization is fixture-specific. Inspect `research/c-real/render-check.json` and the staged expanded C input. Make the smallest generic production-renderer repair in the now-authorized `report_c.py` (and `run_service.py` only if genuinely required) so registry-native adult and pediatric wording is accepted without an in-memory adapter, without hard-coding the 20 trial IDs, and without weakening the C evidence gate.

Run the expanded staged C payload through the unmodified production entrypoint after repair and require all 32 physical routes, chart-before-table, Chinese/local-asset checks, and existing C regression tests to pass. Keep visible language Chinese-native and do not perform final browser or clinical acceptance. Return the full execution report schema.
