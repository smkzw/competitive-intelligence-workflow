Continue the same worker_03 session. Codex scientific review rejects a B-class competitive report containing only iptacopan. Build a real PNH innovative-drug comparator universe appropriate to pivotal clinical-result comparison, including at minimum the major approved or late-stage complement therapies supported by current official evidence (for example C5, C3 and factor-B/D pathways); determine the actual in-scope list from current sources rather than blindly using the examples.

For each included medicine, collect source-bound pivotal trial efficacy, safety, baseline and disposition/completion data, retaining placebo/active-control effects. Explicitly mark genuinely undisclosed fields, but diagnose technical/access/extraction failures separately. Preserve trial/arm mapping without relying on `drug_name`, and add per-product/per-trial numerical-coverage and missingness audits so one well-populated medicine cannot mask absent competitors. If a comparator cannot meet the B key-evidence threshold, block or exclude it with a specific reason; do not fabricate or silently accept a single-product report.

Rebuild the staged B input and all digests within the existing worker_03 write roots, run contract/gate checks, and return the full execution report schema. Do not run final browser or clinical acceptance.
