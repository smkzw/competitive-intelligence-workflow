# Codex 复核：康哲 design_specs S1 候选

日期：2026-08-11
结论：`FAIL — 不可冻结，不可进入 Task 0.4`

## 直接观察

| 轨道 | 真实结果 | 结论 |
|---|---|---|
| 流式 HTML | 写入工具连续四次缺少 `content` 参数，目录为空，但进程 `EXIT:0` | 假完成 |
| 站点 HTML | 生成 4 页；本次未把生成者自述当作独立接受 | 待独立验收 |
| HTML-PPT | 可打开，但无 `S` 讲者窗口，`N` 快捷键冲突，逐字稿不足，存在 10–13 px 字号；运营主责出现 5/9 与可重算 6/9 | 不合格 |
| PPTX | 12 页可编辑文件已生成；第 11 页 KPI 标签重叠，第 4/6/8/11 页含 9–11.25 pt 可见文字，且业务数字不能从指定来源重建 | 不合格 |

## PPTX 内容真源问题

- 指定来源只写“封面、目录、总览、风险、协作、AI 边界、结束”及品牌硬约束，没有任何业务数字。
- 生成脚本硬编码 `11/5/4/1`、`9/3`、`15/5/10/67%`，分别对应或衍生于同目录其他 S1 样例，属于来源外内容。
- 第 4 页同时出现“待复核 9”和“待澄清 4”，没有定义两个集合及分母关系；第 11 页 `10/15=67%` 又使用“待复核”标签，进一步混淆口径。
- 缺少事实时应保留明确的无数据版式样例或使用受控结构化 fixture，不能从兄弟文件拼数、猜数或自由补写。

## PPTX 视觉问题

- 第 11 页四个 KPI 卡片的标签两两重叠，原始分辨率即可直接观察。
- 对 12 页全部文本 run 扫描：第 4、6、11 页最小 9 pt，第 8 页最小 10.5 pt；这些对象是 KPI 提示、流程标签和图例，不是 `data-density=ultra` 表体/坐标轴，不能使用合同的 10.5 pt 例外，故违反默认 ≥12 pt。
- `FRICTION.md` 已识别最小 9 pt，却以“辅助 hint”和旧 R9 母版为理由改判通过；生成节点无权放松合同。
- 生成者明确说明无法图像解析 PNG，因此 XML 色值、几何扫描、页数和 LibreOffice 成功渲染只能证明文件可打开，不能证明视觉可交付。

## 规范内部状态

- `design_specs/local_map.md` L.5 仍要求同步维护 `design_v2.md` 与 `design_share_v2.md` 两份全文。
- `design_specs/ARCHITECTURE.md` 明确禁止双全文，根文件只允许作为 stub。
- 上游 `CLEAR_MATRIX.md` 的 S1 四轨仍全部为 `pending`。

## 继续条件

1. 上游修正 `local_map.md` 与架构矛盾。
2. 四轨以相互隔离、可追溯的结构化 fixture 重跑，禁止读取兄弟样例补数。
3. 对流式零产物、HTML-PPT 运行时/逐字稿/字号/定量一致性、PPTX 最小字号/重叠/内容真源增加机器断言。
4. 由生成者以外的检查者查看最新实产物并接受；上游矩阵不再是 `pending`。
5. 整个 `design_specs/` 稳定双读后，才向用户呈现一次精确摘要确认。
