# Codex Execution Review: ci-r2-terminal-recovery-review-boundary-20260905

## Verdict

REVISE / SELECTIVE ACCEPT.

Worker 03 is accepted. Worker 02 is accepted only as a review-receipt contract
foundation after Codex removed duplicate host evidence models, registered both schema
copies, and added real artifact-byte verification. Worker 01 is rejected wholesale:
its 1,285-line parallel exhaustion model duplicates the already-authoritative
`gates.exhaustion` and `gates.blocker_audit` contracts. Codex instead added a
small adapter over those existing contracts.

## Worker Outputs

- `worker_01`: one governed ZCode execution completed. Report retained as evidence;
  source patch rejected for duplication and divergent terminal truth.
- `worker_02`: one governed ZCode execution completed. Contract/schema/tests
  selectively integrated and tightened; runtime scientific-state authorization remains
  a separate open item.
- `worker_03`: one governed ZCode execution completed. Preview-origin release
  isolation integrated; `rendered_unreviewed`, recovery and evidence-blocked states
  cannot produce visual or real-source acceptance successors.

## Manager Assessment

No manager was declared by the live route. Codex performed direct disposition as
required by the packet. No worker statement was treated as acceptance.

## Codex Independent Verification

- Focused release-boundary checks: Ruff and strict mypy passed; 17 tests passed.
- Scientific review receipt: 35 contract/package tests passed; authorization reopens
  the bound artifact and compares actual SHA-256 bytes.
- Terminal adapter and B/C scientific regressions: 200 tests passed.
- Formal `tools/gate.sh`: Ruff passed; strict mypy passed for 203 source files;
  919 unit/contract tests passed; legacy dependency scan returned `LEGACY_REF_OK`.
- Full repository run was intentionally not hidden: 2,765 passed, 1 skipped,
  20 failed and 25 setup errors. The failures expose known non-release PDF/HTML-PPT
  collection, stale preview fixture/snapshot contracts, stale real-acceptance roots,
  and two B drawer assertions that expect prohibited numerator/denominator backfill.
  Therefore this execution does not claim whole-repository or R2 completion.
- Open scientific boundary: the immutable review receipt is not yet the runtime
  authority for the B/C transition; current package review identity remains
  caller-supplied. This is a required next work item.

## Cleanup Decision

Archive this execution packet after guard audit. Delete only the three exact isolated
APFS clones and disposable tool caches after evidence is archived; preserve source,
reports, route manifest, review, metrics and protected Codex session state.
