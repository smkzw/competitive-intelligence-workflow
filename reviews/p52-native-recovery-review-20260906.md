# 原生恢复边界审阅与主线程处置

范围：A已提交渲染恢复、运行产物复用记账、host-smoke完成态及回执；不覆盖科学完整性、真实宿主、安装整链或RC。原生Erdos句柄`01a0730a-b74f-7a92-a773-ce96ca257a95`，首轮干净上下文，只读生产源码、允许隔离反例。不调用旧执行/会商机制。

## 首轮结论

发现两项P1，无已证实P0；不能关闭恢复验收。

1. 完成态恢复只验证运行清单中列出的`html.manifest.json`，并不递归验证站点和锁定快照。修改efficacy.html或快照后，锁定来源加载拒绝，scenario却仍返回成功；回执只核入口字节也有相同覆盖不足。
2. `_collect_outputs`扫描全部历史HTML清单，把任何未变化清单视为复用。提交后中断，加入其他项目failed B/v99假清单后，恢复最终摘要将其纳入。

审阅原29项通过，4个缺陷反例已复现，保留`tmp/recovery-review-kBXyxV/test_review.py`。五个重点生产文件首尾摘要相同；没有全src首尾清单，不推断全源码冻结。

## 主线程处置（持续更新，不是接受回执）

- 正式反例`tests/integration/test_recovery_artifact_validation.py`三项先全部失败（站点、快照、假历史）；与场景合跑后23通过13.36秒。
- 运行清单验证加入完整锁定站点/快照核验和项目/合同绑定；完成态对当前项目合同；非no-draft宿主回执复用同一链。
- 初版收集过滤按本次节点artifact_id，未再将无关历史默认为复用。但完整integration714通过、1失败162.43秒：FreshA节点ID还绑定package_digest，简单两参路径ID重算漏记合法A。此回归尚未关闭，必须修复而不是删改多报告输出断言。
- 宿主入口/回执显式替身14通过10.98秒，不是真实三宿主验收。A320像素双引擎来源抽屉2通过，主线程实际查看Chromium截图，报告级来源限制可读；不等于全门户视觉完成。
- 同一原生审阅者正在复核修复；已告知上述多报告回归，暂冻结恢复生产文件，保留原报告和全部反例。不发出RC或阶段完成信号。

## 显式引用修订

原生复审确认两个原始缺陷反例已修，合法A漏记仍不接受，建议停止反推ID并使用显式引用。主线程据此将format节点升为1.3，六个handler新增站点及清单路径，保留原artifact_id算法；收集器移除历史报告扫描，只检查当前节点引用，完整校验项目/合同/站点/快照。每节点不得缺引用、错报告或重复引用。历史完成节点缺新字段明确拒绝自动恢复，不静默补写旧事件。

图合同及恢复/多报告组合79通过116.75秒；新增引用反例与多报告13通过9.99秒；全Ruff和strict-mypy216源通过。完整integration与六步开发门、原生最后有界复审仍在运行，不能提前接受。两次阶段代码变更均保留失败证据及原因，没有缩减原先多报告输出要求。

## 最终有界结论

Erdos最后独立验证42项通过（40定向与2补充），确认两项原始P1及合法A漏记回归可关闭，未发现新的相关P0/P1。补充检查保留于`tmp/recovery-final-review-Be7EI3/test_independent.py`。两次src聚合指纹一致，重点源首尾一致；主线程随后解除本次审阅冻结以继续主线。

主线程完整integration722 passed149.31秒，六步gate全部quality-only通过；最后新增历史缺引用专项另行通过，总专项11通过。此结论仅覆盖恢复与引用缺口，不构成真实宿主、科学结论、安装最终包、24门户或RC接受。所有历史及临时反例保留，无提交或删除。
