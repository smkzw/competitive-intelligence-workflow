2026-08-12 Task 3.2 最终回归范围

- 三份 GateSpec 的全部适用关键单元均从 YAML 枚举：A=12、B=11、C=8；每例为非空候选且只缺目标单元。
- 三个空/无适格对象场景、三个 A/B/C 关键冲突和三个科学质控否决场景均走公共入口并复用零下游断言。
- 数据库无 report_snapshots、coverage_sets、coverage_projections、format_jobs、render_queue、artifact_records；报告正式/draft/占位目录均不存在。
- 搜索/恢复执行者与独立遗漏复核者分离；复核结论精确绑定审阅输入摘要。
- Phase 2 EvidenceGap 精确绑定缺口身份、对象、状态、候选路线、完成策略和逐轮信息增益。
- 每个适用路线只有一条摘要；一条摘要可包含多条互不重复的实际回执；科学证明与技术重试/替代路线逐路线失败关闭。
- 路线回执 ID、尝试次数、访问方式和最终结果类别均由实际回执派生；ACCESS_BLOCKED 终末类别必须属于封闭技术失败类别。
- 科学路线可以成功取得文档但仍缺目标字段；真实 content_acquired 不被误判为技术失败。
- 空宇宙候选集合全部处置且适格/排除互斥；排除回执精确覆盖唯一资格缺口的全部路线回执。
- 空宇宙唯一资格缺口绑定同报告 GateSpec、资格字段、检索范围与科学缺失状态；A/B/C 不能跨报告调包。
- B/C 空宇宙必须绑定闭合产品/试验快照并精确匹配候选集合；A 真正零产品不伪造非空快照。
- 未决关键冲突不写成未公开或数值零；技术失败不写成科学未找到。
- 公共入口重验证 model_copy/计算字段/旧摘要/跨项目/跨快照调包；同参重复写入字节级幂等，内容漂移拒绝覆盖。
- audit.json 通过 Schema；audit.md 使用原生中文临床语境，不显示内部枚举、路线 ID、状态名、节点名、日志或提示词。
- 科学质控否决绑定真实 PASSED GateResult、项目、候选快照、宇宙摘要和结果键；可修复回 recovering，已穷尽进 evidence_blocked，均零下游。

非阻断后续加固：重试链与恢复历史的展示口径、跨缺口回执 ID 全局唯一、同版本阻断包受控重发、Graph 层从事实重新推导 GateResult、科学质控穷尽记录的关系图实体复核。它们不改变本任务的无草稿与失败关闭结论，分别进入 Task 3.3/3.4/3.7 的真实运行边界。
