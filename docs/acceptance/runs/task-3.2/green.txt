2026-08-12 Task 3.2 最终机械验证

Task 3.2 精确套件（四份集成测试）：98 passed
Task 3.1 不可变输入回归：143 passed
全库回归：429 passed
Ruff：All checks passed
strict mypy：Success, no issues found
包校验：PACKAGE_OK version=0.1.0a0 stage=phase-2-accepted
blocker-audit.schema.json：JSON 与 Draft 2020-12 合同验证通过
git diff --check：通过

主会场真实攻击复测：
- 同一科学路线重复摘要并注入虚假回执/99 次尝试：拒绝。
- 科学路线回执为 not_found、摘要伪报 content_acquired：拒绝。
- 技术访问阻断路线终末回执为 content_acquired、摘要伪报 rate_limited：拒绝。
- A 空宇宙携带 B GateSpec/错误资格字段：公共写入口拒绝且不产生 blockers 目录。
- B/C 空宇宙缺少闭合快照：拒绝；A 真正零产品且 snapshot=None：通过。
- 空宇宙携带第二资格缺口、错误检索范围对象或技术状态：拒绝。

独立验收：
- Pi/OpenCode Go 同一隔离验收会话复测 98/143/429、Ruff、strict mypy 与攻击正反例，最终 PASS；P0=0，P1=0。
- Cursor Grok 同一备用验收会话完成源码和精确负例合同复核，最终 PASS；P0=0，P1=0。其 Shell 被宿主拒绝，未把静态复核冒充机械执行，机械执行由主会场和 Pi 验收者独立覆盖。
