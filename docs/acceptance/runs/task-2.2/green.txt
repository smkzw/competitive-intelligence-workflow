Task 2.2 GREEN — 2026-08-11

当前 Task 2.2 精确覆盖：
- 产品名称或 NCT 号不作为全局主键：1 passed。
- 别名冲突保留全部候选和证据，不先到先得：1 passed。
- 企业、试验、队列、组别身份及关系分离：1 passed。
- 产品、药物项目、方案身份分离，NCT/CTR 外部标识保留：1 passed。
- 同一外部标识符冲突不自动合并实体：1 passed。
- 同一内部身份出现不一致内容时拒绝静默覆盖：1 passed。
- 150 个适格实体完整进入竞品宇宙，不做 Top-N 截断：1 passed。
- 身份依据不得直接等于名称、别名或外部登记号；更换 NCT/CTR 不改变内部身份：1 passed。
- 每个候选实体必须且只能有一条适格记录，重复或漏评均拒绝闭合：1 passed。
- 待审查适格记录阻止身份绑定的竞品宇宙闭合：1 passed。

组合套件：10 passed。
全库回归：147 passed。
Ruff：通过。
strict mypy：27 个源文件通过。
包校验：PACKAGE_OK version=0.1.0a0 stage=phase-0-task-0.4。
git diff check：通过。

首次独立验收发现的 2 项 P1 已修复；以上为修复后的构建者检查，最终接受仍须原验收会话复核。
