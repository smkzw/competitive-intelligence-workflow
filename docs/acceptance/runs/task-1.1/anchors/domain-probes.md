# Task 1.1 非模型锚点

- `stable_id` 拒绝 `None`、`0`、`False`，并固定 NFKC 与身份分隔符行为。
- 九个状态族成员与 v1.2 精确一致；24 组共享原始值的外部枚举成员转换全部抛 `ValueError`。
- 生产 IANA 格式检查器与双重验证入口同时拒绝无效时区、HTML 非首位和无 offset 日期时间。
- `America/New_York` 2026-03-08 日末正确物化为 `-04:00`。
- 跨日恢复返回同一冻结合同；刷新生成新版本并保留原版本。
