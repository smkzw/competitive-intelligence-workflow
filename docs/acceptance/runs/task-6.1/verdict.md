# Task 6.1 验收结论

## Verdict

通过。

## Boundary

本结论只接受研究角色、论文角色分列和 B 类终点—时间窗兼容合同，不创建或放行 B 页面。

## 决定性结论

- 五类默认排除、支持层、特殊核心和 C 早期决策域均由版本化规则确定，不接受调用方注入最终角色。
- 母试验标准证据可进入核心；延伸、亚组、事后和真实世界结果只能进入支持层，不与母试验等权比较。
- 非 NCT 论文在当前分类器不能重算时失败关闭；论文角色不覆盖母试验角色。
- 终点必须先命中明确终点标识，构念只能收窄；邻近时间点兼容时仍保留实际时间点差异标签。
- 相同结构适用范围却分配不同角色的规则、终点重叠规则和时间窗重叠规则均拒绝加载。

## 独立复核与技术异常

Grok 首轮及定向复核发现并证实错误纳入路径，Codex 均按探针修复。最终修复后，原 Grok 会话恢复和同模型新会话分别失败；健康检查显示模型目录与登录正常，实际调用明确返回 `402 Payment Required: Grok Build usage balance exhausted`，因此没有伪造最终 Grok 通过结论。原 Pi 会话按用户规则切换至 `cms-router/minimax-m3` 后重放关键 III 期四类证据及冲突规则探针，最终通过；Codex 另以 34 项定向测试和 986 项跨 A/B、PubMed、ClinicalTrials.gov 回归独立验收。

## 下一步

进入 Task 6.2，构建指南/竞品终点依据、单时间点与纵向疗效视图和用户可逆排序；继续以 `StudyCandidate`、`TrialRoleOutput` 与 `EndpointObservation` 作为强类型输入边界。
