# Task 3.5 验收与恢复锚点

日期：2026-08-13

## 状态

- Task 3.5：`accepted`
- 实现提交：`854aaa14277c8f14c83807b639b9e53274429f8a`
- Phase 3：`in_progress`
- 下一步：Task 3.6，构建唯一 fixture catalog、真实项目/fixture 默认执行器、CLI 和规范产物路径；不提前进入科学质控或报告门户。

## 已接受范围

- A/B/C 报告与 HTML、PDF、HTML-PPT、PPTX 按完整选择矩阵独立计算；报告或格式受阻不撤销其他已交付对象，也不能通过删掉失败对象、忽略可选格式或同版本改选择获得完成。
- 项目可机械区分仍有可继续对象的部分交付、已有交付但其余对象已穷尽的部分交付受阻、无任何可交付内容的整体受阻，以及全部选定格式就绪的完成。
- v1.2 §10.2 状态表作最小勘误：`running` 与 `awaiting_user` 可在“已有交付、其余选定对象全部终态阻断、无运行对象”时直接进入 `partial_delivery_blocked`；不虚构 `partially_delivered` 中间状态。
- 合同选择从首次公开动作起绑定。更高版本绑定后，陈旧版本、未绑定版本和同版本选择漂移均不能读取权威条件或推进协调。
- 项目与格式重新打开绑定具体阻断进入代次；同一次重放不追加事件，原因漂移失败关闭，连续两次阻断仍可分别恢复。报告版本重绑必须绑定阻断之后且原因一致、未被消费的项目重开回执。
- 格式阻断证据未携带合同版本时，可按事件顺序追溯阻断前最近的已绑定版本；无法证明版本则失败关闭，更高合同版本才可按“新版本恢复”重开。

## 决定性证据

- Task 3.5 三个精确节点：3 passed。
- 全部图测试：13 passed；全库：445 passed。
- Ruff、strict mypy、compileall、wheel 构建与内容清单、`git diff --check`：通过。
- Pi/Qwen 原审查会话独立临时探针最终 `PASS；P0=0；P1=0；P2=2`；Grok Build 原审查会话最终 `PASS；P0=0；P1=0；P2=1`。两者均无 fallback。
- Grok 明确说明没有取得修复后 pytest 退出码；最终接受只使用 Codex 实际执行的测试结果，不把源码阅读冒充测试通过。

## 后续责任

- Task 3.6：把当前协调器接入真实项目与 fixture 的同一默认执行器，固定唯一 catalog、输入摘要、当前 run/snapshot/manifest 绑定和规范产物路径。
- Task 3.7：独立科学质控接受/否决必须绑定当前候选快照和真实验证结果，不能由调用方自报。
- 用户明确排除系统安全测试；恶意直接写原始事件、密码学碰撞和合同标识空白规范化作为非阻断强化项，不扩展本阶段工作。
- 未创建或验收 HTML、PDF、HTML-PPT、PPTX 用户报告；这些属于后续阶段。
