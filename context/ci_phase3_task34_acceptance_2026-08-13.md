# Task 3.4 验收与恢复锚点

日期：2026-08-13

## 状态

- Task 3.4：`accepted`
- 实现提交：`a978cde4ba3193d273c7d6b04de5860e80243277`
- Phase 3：`in_progress`
- 下一步：Task 3.5，构建报告/格式分支相互隔离的部分交付与恢复编排；不提前进入 fixture CLI、科学质控或报告门户。

## 已接受范围

- v1.2 的项目、报告证据、格式、下载请求和修订批准迁移表已固定为类型化声明；未声明迁移、错误触发条件、错误当前状态和守卫证据不足均失败关闭并留存审计事件。
- 节点合同覆盖接收、预检、竞品宇宙、检索、摄取、抽取、冲突解决、证据评估、恢复、快照、科学质控、分析、格式生成和验收；每个节点声明输入输出、完成条件、读写范围、重试、错误、幂等材料和副作用类别。
- A/B/C 共用证据引用，但证据评估、候选快照、分析和格式状态彼此隔离；一个报告阻断不会改写其他报告。
- 共享事件库中的迁移接受事件与节点完成事件均在归约前重新验证。直接追加的伪造边、守卫、当前状态、输出类型、完成摘要、事件身份或幂等身份不能进入规范状态或检查点。
- 发布、移动、批准和删除使用稳定目标身份；在“副作用已完成、检查点尚未保存”的中断窗口重放不会重复执行。仅有路径的删除事件也可正确恢复，目标不明的副作用在执行前失败。

## 决定性证据

- GT01–GT11：11 passed。
- 全库：443 passed。
- Ruff、strict mypy、wheel 构建及 wheel 内图模块清单、`git diff --check`：通过。
- Pi/Qwen 原会话复核最终为 `PASS；P0=0；P1=0；P2=1`；Grok Build 原会话以 23 个独立临时探针复核，最终为 `PASS；P0=0；P1=0；P2=4`。两者均未 fallback。
- 主会场独立复现并关闭三类假绿：删除目标身份不一致、原始迁移接受事件绕过校验、原始节点完成事件绕过校验。

## 后续责任

- Task 3.5：副作用事件生产入口统一验证来源路径、目标路径、批准决定等必要字段并返回领域化中文错误；把节点完成、状态迁移与部分交付编排真实连通。
- Task 3.5：若生产入口允许外部构造迁移接受事件，补充事件 ID 与幂等键的重算校验；默认应用入口继续只允许由 `submit()` 生成。
- Task 3.5：增加移动、批准各自独立的中断窗口攻击，不仅依赖四操作组合场景。
- Task 3.7：把科学质控和格式验收守卫绑定真实验证结果，不接受调用方自报布尔值。
- 未进行安全测试；未创建 HTML、PDF、PPT 或 PPTX 报告。
