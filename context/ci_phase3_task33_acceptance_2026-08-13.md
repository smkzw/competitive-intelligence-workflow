# Task 3.3 验收与恢复锚点

日期：2026-08-13

## 状态

- Task 3.3：`accepted`
- Phase 3：`in_progress`
- 下一步：Task 3.4，构建 GT01–GT11 类型化控制图；不提前进入 fixture 执行器、科学质控或报告门户。

## 已接受范围

- 只有真实 GateSpec 关键单元仍阻断、且目标资料预计关闭至少一个缺口时，才生成用户辅助下载请求。
- 用户保留下载文件原名并放入唯一收件目录；系统识别 DOI、PMID、NCT 和中英文标题，完成规范命名、内容寻址归档、来源版本与重抽取任务登记。
- 大小写扩展名等价；真实 PDF、网页、纯文本与角色一致性受控；错附件、登录/错误页、不可读文件和同运行歧义附件进入摘要隔离区。
- `file_detected`、`matched`、`accepted` 任一写入后中断均可幂等续跑；不重复事件、来源版本、归档或重抽取任务，并清理已持久化接受后的遗留收件副本。
- 下载列表和处理说明使用用户可执行的中文医学工作语言，不展示内部状态、规则名、摘要或媒体类型术语。

## 决定性证据

- 精确三文件：3 passed。
- Task 3.1/3.2 与阻断语义回归：241 passed。
- 全库：432 passed。
- Ruff、strict mypy、JSON Schema、包校验、`git diff --check`：通过。
- Pi/Qwen 以 66 个临时探针复验；Grok Build 以 70 个临时探针复验。两者最终均为 `PASS；P0=0；P1=0`，无 fallback。

## 边界与后续责任

- Task 3.4：扩展 PDF 登录/错误页的可恢复识别，并把收件恢复事件接入类型化控制图。
- Task 3.5/3.6：在 Agent 交互层把机器目录映射为更易操作的投递指引；底层仍保存可移动的项目相对路径。
- 未进行安全测试；未创建 HTML、PDF、PPT 或 PPTX 报告。
