# Task 3.7 与 Phase 3 验收锚点

日期：2026-08-13
实现提交：`8d50e13`
结论：`PASS；P0=0；P1=0；P2=1`

## 已实现

- 科学质控只读取当前候选快照、规则、覆盖、来源和精确定位，不接收构建者推理、提示词或草稿；生产者与审查者身份分离。
- GateSpec 结果绑定候选快照完整内容摘要和规则版本；同标识下内容漂移、跨报告对象、跨标准版本或过期结论均失败关闭。
- 生产边界依次执行打包 Draft 2020-12 Schema、Pydantic 和语义校验；来源、片段、声明、事实、定位及问题引用保持唯一且可追踪。
- 科学质控授权只能由已完成上述验证的边界经专用事件入口签发；公共事件追加和普通图迁移不能自造授权。授权一次性消费并绑定本次进入科学质控的代次，精确重放可复现，恢复后不能复用旧授权。
- 接受结论才可进入锁定快照；可修复否决回到恢复，已穷尽否决进入证据阻断。三条路径互斥，否决前后均保持零报告下游产物。
- 运行时 Schema 同时进入目录包与 wheel，两个副本逐字一致；隔离安装 wheel 后生产解析器可真实加载。

## 决定性验证

- Task 3.7 精确与安装合同：21 passed；关联 Gate/图/事件/重放：102 passed；全库：472 passed。
- Phase 3 正式退出命令：277 passed。
- Ruff、strict mypy（69 个源文件）、package verify、`git diff --check`：通过。
- wheel 构建、成员检查、全新虚拟环境安装和生产 Schema 解析：通过。
- 真实 `no-draft-a-empty`：运行 `run_8f13149186023d423577c8d5` 以定义的证据阻断码退出；仅生成中文《证据不足说明》和内部审计记录，无报告/HTML/PDF/PPTX；数据库 `report_snapshots`、`coverage_sets`、`artifact_records` 均为 0。
- 同一 Luna 独立验证会话最终结论：`PASS; P0=0; P1=0; P2=1`。

## 非阻断说明

- P2 仅指出 Trellis 索引引用工作区外批准实施计划；仓内批准设计副本和批准记录存在，功能与验收不受影响。
- Python 私有能力不是敌对进程安全边界；本任务验证的是产品公共 API 的状态溯源正确性，不开展安全测试。

## 下一步

Phase 3 已接受。下一项按批准计划进入 Phase 4 / Task 4.1：共用 ReportViewModel、coverage_set 和页面注册表；不得回写或放松 Phase 3 的科学真源、无草稿和图状态不变量。
