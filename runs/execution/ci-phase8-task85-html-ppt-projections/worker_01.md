I am the `pi` fallback `cursor` / `cursor-grok-4.6` for this bounded Task 8.5 audit. I will stay inside the assigned workspace, follow the execution-manager contract, and start from the initial read set.I have the contract: worker_01 may only write the projection contract and its tests. Next I will audit the Task 8.5 specs, locked A/B/C inputs, and page coverage.I have enough to freeze the A/B/C deck map, data paths, and fail-close rules. Next I will write the two authorized artifacts and run the new contract tests.The C deck currently ends on the identity slide instead of the closing page. I will put that identity check before the ending page so `End` lands on the real close.# Execution Output: ci-phase8-task85-html-ppt-projections - worker_01

Fallback: declared `pi` fallback `cursor` / `cursor-grok-4.6` after the primary `grok-build` role was unavailable. Tools for read/search/eval/write/pytest were available. This pass did not run Chromium/WebKit, did not generate decks, and does not claim Task 8.5 or visual acceptance.

## Boundary And Context Check

- Role: `worker_01` only. Authorized writes: `docs/acceptance/runs/8.5/projection-contract.md`, `tests/html_ppt/test_projection_contract.py`.
- Did not modify locked fixtures, portal, PDF, Task 8.4 runtime, or Kangzhe assets.
- Context files read: execution context, Codex plan, Task 8.5 prd/design/checkpoint, Task 8.4 verdict, page catalogs A/B/C, `page_registry.py`, coverage-set-expected, A/B/C locked JSON structure, PDF coverage needles (coverage only), runtime fixture HTML, `track_htmlppt.md` geometry/type, FX/logo actual paths.
- Evidence vs inference: hashes, counts, and missing B modules are measured; 15/24/18 page maps are the proposed implementable freeze.

## Work Performed

Audited locked A/B/C inputs and froze three deck maps plus fail-close rules.

**A** (`research-content.json` `report_data` only, 38 products / 49 trials): 15 slides covering all 12 catalog responsibilities. Efficacy family must be regex `EASI[- ]?75` (548 rows / 25 products, includes 度普利尤单抗). Literal `endpoint=="EASI-75"` drops core marketed products and is fail-close. Do not project top-level `sources`/`facts`/`claims`/`gate_status` or package `scientific_review`.

**B** (PNH, 伊普可泮, APPLY + APPOINT): 24 slides, no dropped baseline/disposition modules. Measured gaps: longitudinal only 第24周; no `disease_context` domain; 40/54 disposition facts unpublished; APPOINT matrix `not_applicable` with `reason_zh`; no subgroup rows. Unpublished pages must use disclosure matrices, not zeros or hemoglobin-as-context.

**C** (3 drugs / 4 trials / 48 observations): 18 slides, element compare → identity (drug/NCT/scale/timepoint) → ≥2 design paths, ending last. Audience copy must use Chinese labels/thresholds, not English `source_text`.

Runtime/FX/logo hashes locked to files on disk. FX lives at `contracts/kangzhe/design_specs/assets/htmlppt/gx_fx.{css,js}`, not the context’s `assets/htmlppt/` shorthand.

## Artifacts And Evidence

| Artifact | Role |
|---|---|
| `docs/acceptance/runs/8.5/projection-contract.md` | Structure, mapping, fail-close, machine JSON |
| `tests/html_ppt/test_projection_contract.py` | Hash, catalog coverage, A/B/C shape, notes helpers |

Input SHA-256:

- A content `988c1607e08c7f9747a6feb493dcdaf66b6ccd7c26cafefe96e956622196fa1a`
- B `eeae14ce581aeacc6c098cde5f45571e4d0f88d2cc5877cba5429654448381d1`
- C `a59d7f88b3d8a2e163422c01f0d0981aa64bf2610cc6bb41852c58e9561ed2e6`

Runtime JS/CSS match Task 8.4. Logo `8d16d3ae…`; `gx_fx.css` `7e2ba095…`; `gx_fx.js` `92cdb591…`.

## Commands And Observations

| Tool | Target | Observation |
|---|---|---|
| Read | execution context, plan, 8.5/8.4 docs, catalogs | Task 8.5 still unimplemented; runtime locked |
| Eval | A/B/C JSON | A 38MB; B views present with unpublished disposition; C 12×4 observations |
| Eval | asset hashes | FX/logo under `contracts/kangzhe/design_specs/assets/` |
| pytest | `tests/html_ppt/test_projection_contract.py` | **7 passed** in 0.18s |

## Blockers Or Missing Environment

None for this work item. Renderer package `src/ci_workflow/renderers/html_ppt/` and `output/html-ppt/` do not exist yet (worker_02/03). No browser run assigned here.

Codex decision (does not block the frozen contract): A may have >12 EASI-75 series; contract requires target small-multiples rather than a 16th slide. Confirm that remains the rule if worker_02 cannot fit 25 products without overflow (overflow belongs to 8.6, but split would break the 15-page freeze).

## Rerun Requests Or Next Step

1. Codex: accept this contract as worker_02/03 input.
2. worker_02: shared assembler + Kangzhe/FX/runtime inline + A projection against this map.
3. worker_03: B/C projection, three candidates, structural/offline/notes/font/coverage/browser tests; do not treat 8.5 as visual final.

Resume point: contract tests green; no deck HTML yet.
