# Phase 3 Task 3.1 独立方案最终复核

## 结论

**PASS（合同层复核，不等于实现验收）。**

当前：

- **P0=0**
- **P1=0**
- **P2=0**

上轮剩余 5 个 P1 已写入可执行合同，建议激活 Task 3.1 实现阶段。实际 YAML、Schema、Python 和测试仍需按合同执行并通过验收。

## 五项结果

### 1. A 成熟度与 `result_bearing`

**PASS。**

已固定：

- 开发成熟度封闭集合：临床前、临床、申报、已批准、暂停/终止/撤回。
- 临床结果状态封闭集合：无适格临床试验、适格试验尚无观察性数值结果、已有观察性临床数值结果。
- `result_bearing=true` 必须同时满足：目标适应症、适格试验、已接受事实、疗效或安全事实域、`observation_kind=observed_result`、数值结果及允许来源角色。
- 计划样本量、计划终点、计划时间点、目标值和方案假设不能触发。
- 调用方不能直接传入或降级布尔值。

依据：`0011...md:27`、`0011...md:28`、`design.md:34`。

### 2. 来源、披露、事实状态、冲突及 C 登记来源

**PASS。**

已固定：

- 来源角色封闭集合。
- 披露成熟度的明确序列。
- Phase 2 封闭事实状态。
- 关键单元只能接受已解决冲突；非阻断单元可保留开放冲突。
- C 的官方登记设计事实按来源角色判断，不使用结果披露成熟度序列替代。

依据：`0011...md:24` 至 `0011...md:26`。

### 3. 只收紧偏序

**PASS。**

已固定确定性比较式：

- `new_applicable_set ⊇ old_applicable_set`
- `new_source_roles ⊆ old_source_roles`
- `new_maturity_floor ≥ old_maturity_floor`
- `new_conflict_acceptance ⊆ old_conflict_acceptance`
- `blocking=false → true` 允许，反向拒绝。

同时明确禁止仅按单元数量或总分判断，操作类型只允许增加单元、提高阈值、扩大适用对象或收窄可接受集合。

依据：`0011...md:36`、`design.md:35`。

### 4. 结果不可变键与受影响报告

**PASS。**

已固定：

- 结果键绑定报告类型、证据快照 ID、基础规则版本、项目合同版本和适用对象集合摘要。
- 父结果不得就地覆盖。
- 受影响报告由规则依赖反向索引计算。
- 调用方声明集合与计算集合不完全一致时拒绝。
- 父合同缺失、错误替换或证据快照不一致时不得生成子结果。

依据：`0011...md:37`、`design.md:30`、`design.md:36`。

### 5. Exact nodes 与非空反例

**PASS。**

`implement.md:16` 至 `implement.md:18` 已覆盖：

- A 观察性临床结果触发、计划值排除、成熟度矩阵、疗效/安全分别缺失；
- B 逐组基线、治疗/对照、单臂、多臂、组级安全和总体值替代；
- C 登记充分、核心字段缺失、统计扩展非阻断；
- 覆盖增加单元、提高阈值、删除/降低/放宽拒绝；
- 结果键、父子版本、错误受影响集合、反向依赖重算；
- 内部状态与中文用户说明分层。

合并命名的来源/披露和冲突用例必须实际参数化覆盖双方分支；这是实现验收条件，不再构成合同缺陷。

## 仍需最小修补

合同层无剩余 P0/P1/P2 修补。

激活后仅需验证：

1. YAML 与 JSON Schema 严格实现封闭枚举和未知值拒绝。
2. 所有合并命名 exact node 实际包含完整参数化分支。
3. `GateResultKey` 和反向依赖计算不是由调用方伪造。
4. 执行精确套件及独立只读验收。

因此，**可以建议激活 Task 3.1**；本结论不代表实现代码已经通过。