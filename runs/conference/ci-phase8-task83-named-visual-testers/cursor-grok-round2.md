C 的 `summary.json` 哈希与提示不一致。正在核验证据绑定并目视点名页。# Task 8.3 点名视觉复验（第二轮）

## 路线与边界

- 实际路线：`pi/cursor/cursor-grok-4.6`，effort medium。
- 只读复验；未改 PDF；未读其他测试者结论；未把 `ok: true` 当通过。
- 点名页已对照当前原分辨率页图：A2、A9、B2、B7、B12、C2、C12、C20；相邻页抽查 A8、B13、C11、C19。

## 当前哈希核验

| 报告 | 提示给定 SHA-256 | `summary.json` / `locked_sha256` | 页数 | 页图 |
| --- | --- | --- | --- | --- |
| A | `b5e224cf2a558154a024c8b43e5211353abd5dead77455c541a4e84d20f53bf7` | **一致** | 10 | 10 |
| B | `83bec909d39a8bfe22a1c35ee42072ac9d9cdfac7b4908d16c14d5876276d716` | **一致** | 24 | 24 |
| C | `9cfa1a6323fcddac0a3d83df32ab351ebab03c12e84f0984f59d746a87628a68` | **不一致**：实际绑定 `a77dac17da0464d1ce09673afbd320da48c8eb71abd85af8bb39989742582aa4` | 20 | 20 |

合计仍为 A10 / B24 / C20 = 54 页。本轮目视与文本结论绑定 **磁盘上的 `summary.json` 实际哈希**，不是提示里的 C 旧值。A/B 与提示锁定一致；C 文件已相对提示再变一次，终验必须用 `a77dac17…`。三份 `hash_ok`、页脚/页码/A4 机器项仍为 true。续表页：A8；B 9/11/13–15/17/18/20；C 5–7/10/14。未因改表头导致页数漂移。

## 逐页复验（点名项）

### 1. 目录文案 — **通过**

- **A2 / B2 / C2** 导语均为「按阅读顺序列出本报告各章节。」
- 页图与 `page-text` 均不再出现「责任章节」「下表标题」。
- 页脚分别为「…第 2 页」。无裁切、重叠、乱码。

### 2. A9 / B7 气泡矩阵 — **B7 通过；A9 未完全通过**

**B7**（`B/renders/page-07.png`）

- X 刻度 `56.1 / 66.2 / 76.4`，Y 倒序 `58.2 / 68.3 / 78.5`；十字网格；上箭头 +「疗效观察 →」。
- 气泡 A、B 在框内，不压边。页脚第 7 页。

**A9**（`A/renders/page-09.png`）

- X `37.2 / 59 / 80.8`，Y 倒序 `58.2 / 70.5 / 82.8`；十字网格；方向说明与箭头均在。
- **气泡 C（瑞格替尼）下缘压住并越过底轴（Y=82.8 基线）**，A/B/D 在框内。页脚第 9 页。无乱码、无出页裁切。

### 3. B12 表头 — **通过**

- 表头：`产品 | 试验 | 组别 | 观察类别 | 指标 | 数值 | 分母 | 分母口径 | 披露`。
- 「字段族 / 字段 / 分母角色」已从点名表头消失；B13 续表同样新表头。
- 「导致排除的方案偏离」在指标列折成「导致排除的方案 / 偏离」，可读、未出格。
- 导语仍有「失访及实施字段」（非表头，见非阻断）。

### 4. C12 终点列 — **通过**

- 终点列单行完整：`主要终点（IGA）` ×3、`主要终点（EASI）` ×1，无孤行 `）`。
- 表头 `终点披露`、`时间披露` 各占一格、未拆字。页脚第 12 页。
- 完整定义列仍正常折行（定义长句），不属点名缺陷。

### 5. C20 — **部分通过，NCT 折行未闭合**

- 「技术过程不进入受众页」已删除；现为「呈现登记版本、设计事实来源范围和影响解释的局限。」
- 支撑试验格：`NCT05149313（ADvantage）`、`NCT02260986（CHRONOS）`、`NCT04178967（ADvocate2）` 单词与括号未拆；`ADvocate2` 完整。
- **未通过点名第 5 条后半：** 正文「差异：主要终点定义上…」把第二个登记号拆成行末 `NCT0` + 下行 `2260986为第16周…`（`C/renders/page-20.png` + `page-20.txt` 第 15–16 行）。应按整号换行，而不是从号中间切断。
- 另：异常点句末句号单独落到下一行。页脚第 20 页。无黑块乱码。

### 6. 相邻页回归抽查 — **未见新裁切/乱码/缺续表头**

- A8：`续表 · 安全性完整数据表` + 表头在，瑞格替尼/诺维单抗接续，第 8 页。
- B13：续表 + 新表头，第 13 页。
- C11 四卡片、C19 试验档案完整，第 11 / 19 页，无新分页截断。
- 物理页数仍 A10/B24/C20。

## 仍存阻断

**未发现阻断。** 无空白页、黑块、中文乱码、整表不可读、页数丢失。

## 非阻断（仍建议修后再放行点名项）

| # | 页 | 证据 | 严重度 | 点名项 |
| --- | --- | --- | --- | --- |
| 1 | **C20** | 长句将 `NCT02260986` 拆成 `NCT0` / `2260986` | **重要但不阻断** | 第 5 条未闭合 |
| 2 | **A9** | 气泡 C 压过底轴 | **重要但不阻断** | 第 2 条「不得压框」未闭合 |
| 3 | **C20** | 「不表示优劣判断」后句号孤行 | 后续优化 | — |
| 4 | **B12** 导语 | 仍写「实施字段」 | 后续优化 | 表头已改，导语未清 |
| 5 | **B12** 末行 | 「方案偏离」被折在「方案 / 偏离」 | 后续优化 | 可读 |

C 哈希与提示给定值不一致：属绑定说明，不是页面渲染阻断。终验清单须改绑 `a77dac17…`。

## 是否建议 Codex 最终接受

**不建议本轮最终接受。**

已修回：三份目录、B7 刻度网格箭头、B12/B13 临床表头、C12 终点/披露列、C20 内部指令与试验名硬折。页数与相邻续表未回归。

点名清单仍有两处未闭合：**C20 第二个 NCT 被从号中间切断**，**A9 气泡 C 压框**。建议修这两处并重绑 C 哈希后再终验。本意见不是最终放行。
