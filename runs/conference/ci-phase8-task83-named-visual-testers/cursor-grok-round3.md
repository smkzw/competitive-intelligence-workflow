只复验 C20 与 A9，先核当前哈希绑定，再打开原分辨率页图。# Task 8.3 点名收口复验（C20 / A9）

## 当前哈希与证据绑定

`docs/acceptance/runs/8.3/verification/summary.json`（`ok=true`，`defects=[]`，合计 **54** 页）：

| 报告 | SHA-256 | 页数 | hash_ok |
| --- | --- | --- | --- |
| A | `b5e224cf2a558154a024c8b43e5211353abd5dead77455c541a4e84d20f53bf7` | 10 | true |
| B | `83bec909d39a8bfe22a1c35ee42072ac9d9cdfac7b4908d16c14d5876276d716` | 24 | true |
| C | `9cfa1a6323fcddac0a3d83df32ab351ebab03c12e84f0984f59d746a87628a68` | 20 | true |

C 的 `evidence.json` 中 `pdf_sha256` / `expected_sha256` 同为 `9cfa1a63…`，与提示锁定值一致。已打开当前 `C/renders/page-20.png`（993×1404）和 `A/renders/page-09.png`（1404×993），并对照 `C/page-text/page-20.txt`。A9 另对原图像素逐行取样，不以 contact sheet 为准。

## C20

**点名项已闭合。**

- 长比较句第二个登记号已整号换行，未再拆成 `NCT0` / `2260986`。抽取文本为：
  - `差异：主要终点定义上，NCT03985943为ITT人群中IGA达到0或1分且较基线下降≥2分的受试者比例；`
  - `NCT02260986为第16周IGA评分达到0或1分且较基线下降≥2分的受试者比例。`
- 支撑试验：`NCT05149313（ADvantage）`、`NCT02260986（CHRONOS）`、`NCT04178967（ADvocate2）` 单词与括号完整；页图未见试验名硬折。
- 「技术过程不进入受众页」已不出现。现导语：`呈现登记版本、设计事实来源范围和影响解释的局限。`
- 页脚：`特应性皮炎临床试验设计比较 · 第 20 页`。无裁切、重叠、黑块、乱码。

残留（非本步点名、不阻断）：异常点句末句号仍单独落到下一行（`不表示优劣判断` / `。`）；路径2 支撑格里「奈莫利珠单抗疗效与安全性研究」在「疗效与 / 安全性研究」处折行，登记号本身未断。

## A9

**点名项已闭合：气泡 C 与底轴之间有间隙，未压线、未出框。**

原图（1404×993）在绘图区内：

- 底轴（近黑横线）在 **y = 334**。
- 气泡 C（品牌橙 `#FF9900` / `(255,153,0)`，x≈936–960）最底纯橙像素在 **y = 329**。
- **y=332–333 为纸白** `(255,254,251)`，**y=334 才是轴线** `(26,26,26)`。轴所在行及以下 5 行无橙色。
- 纯橙底缘到轴线 **5 px** 间隙（120 dpi 下约 1 pt 量级，符合半径加 2 点钳制后“贴底但不压线”）。
- 刻度仍在：Y `58.2 / 70.5 / 82.8`，X `37.2 / 59 / 80.8`。A/B/D 互不重叠。页脚 `特应性皮炎竞品全景 · 第 9 页`。

上一轮目视把贴底当成压框；本次按像素，**轴线未穿过气泡填色**。

## 仍存阻断

**未发现阻断。**

## 是否建议 Codex 接受

**建议 Codex 接受本轮收口。**

C 已绑到提示中的 `9cfa1a63…`；C20 第二个 `NCT02260986` 整号换行；A9 气泡 C 相对底轴有可测白隙。页数仍为 A10+B24+C20。本意见只覆盖这两处点名修复，不是整份 54 页的重新终验，也不自称最终放行。
