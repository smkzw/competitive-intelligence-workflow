# Task 8.3 独立视觉与阅读器审评

## 路线与边界

- 实际路线：`pi/cursor/cursor-grok-4.6`，effort medium。
- 角色：只读、独立视觉与阅读器审评；不是最终验收人。未改 PDF、未改医学内容。
- 未采信 `ok: true` / `defect_count: 0` 作为通过依据；未读其他测试者或会商结论。
- 已逐张查看 `A/renders/page-01.png`–`page-10.png`、`B/renders/page-01.png`–`page-24.png`、`C/renders/page-01.png`–`page-20.png` 共 54 张原分辨率页图，并对照三份 `coverage_projection.json`、`defects.json`、`evidence.json` 与逐页文本。本环境未启动 Adobe/Preview；打开/检索判断来自绑定当前哈希的 `pdftotext` / `pypdf` 结构报告。

## 对象核验

| 报告 | PDF | 当前 SHA-256（与锁定哈希一致） | 页数 | 页图数 |
| --- | --- | --- | --- | --- |
| A | `.artifacts/pdf-complete/reports/A/v-fixture-001/report.pdf` | `4aa62ade74d2055a40ef740b58fb6006591f49d4209433aa15527dd8821eb1a6` | 10 | 10 |
| B | `.artifacts/pdf-complete/reports/B/v-fixture-001/report.pdf` | `783485c697ab967a10e9487b6476c748da4381ba3997c66bc66b1218f9a7eb93` | 24 | 24 |
| C | `.artifacts/pdf-complete/report-c.pdf` | `6954dfa92f05767ad92660b6e660c4baf0246cf35b6a45486b27f60487ce7b95` | 20 | 20 |

合计 54 页 / 54 张页图，与 `summary.json` 的 `total_pages` / `expected_total_pages` 一致。三份 `hash_ok`、`page_count_ok` 均为 true。机器 `defects.json` 均为 `[]`，本审评不以该空结果代替目视。

封面「数据截止：2026年7月31日」相对审评日 2026-08-31 可成立，不判占位错误。「示例登记号301/201」等属锁定 fixture 文案，本步不按医学内容错误处理。「未公开」按既定披露口径，不因数量多本身判渲染缺陷。

## 逐页覆盖

**已真实查看：A1–A10、B1–B24、C1–C20。** 无问题连续页合并；问题页单独定位。

### A 类（特应性皮炎竞品全景）

- **A1–A3（封面、目录、首页摘要）**：无裁切、重叠、黑块、乱码。页脚连续为「特应性皮炎竞品全景 · 第 n 页」。封面/目录/摘要下半页留白大，属封面与轻量首页形态，未造成内容丢失。目录无印刷页码。导语含「正文责任章节」「书签与下表标题」，偏生成说明。
- **A4（竞争格局 / 产品总览 / 临床开发组合，横向）**：三表可读、表头完整。前两表明显窄于顶栏橙线，右侧留白；第三表全宽。无裁切乱码。
- **A5（监管 / 企业与交易 / 专利）**：三表完整。「未公开」出现在交易条款等字段，导语已说明缺失口径。
- **A6（疗效，横向）**：柱状图组间并列，柱顶有 68.4 / 31.2 等。Y 轴缺单位「%」，刻度贴线，但柱标仍可读。所谓「森林图」实为差值横条、无 CI 须；同页完整表可定量对照。
- **A7–A8（安全性）**：热图格内有数值，图例写「数值为准」。深色格对比偏紧但仍可辨。A7 完整表只到安澜双抗；A8 标题「续表 · 安全性完整数据表」并重复表头，瑞格替尼/诺维单抗接续。续表规范成立。
- **A9（疗效—安全性矩阵）**：有 A–D 图例与「向上=发生率更低；向右=疗效观察信号更强」及 X/Y/半径映射。图框无数值刻度；气泡 C 底缘贴/略压框线。同页完整表可定量对照。
- **A10（历史与边缘 / 依据与局限）**：两表可读，页脚第 10 页。下部留白，不影响阅读。

### B 类（特应性皮炎临床试验结果比较）

- **B1–B3**：封面/目录/摘要结构同 A。页脚连续。目录无页码；说明句含「正文责任章节」。
- **B4（疗效，横向）**：**柱状图 Y 轴 100/80/60/40/20/0 垂直挤叠并贴刻度线**，刻度本身难读；柱顶仍有 68.4/31.2/64.1/28.9，比较值未完全丢失。横条图 Y 轴为 `安澜双抗/HORIZON-AD`、`泰瑞奇单抗/澄明-3` 拼接标签。完整表可读。页脚「第4页」空格与前后页不完全一致。
- **B5（纵向）**：小节写「折线图」，图注已写「单一时间点…不连线」。散点无点上数值，须看下表。图例橙/黄、浅灰/深灰接近。非空白、非乱码。
- **B6（安全性）**：2×4 热图格内有数；深格对比偏紧。完整表与热图对齐，本页未截断。
- **B7（矩阵）**：同 A9，无轴刻度，靠表读精确值。
- **B8–B11（基线总览续表、人口学、疾病语境、严重程度）**：B8/B9/B11 续表标题与表头齐全。全表「未公开」是披露状态，页顶有「不作零填充」说明，**不判渲染丢数**。B10 疾病语境表仅安澜双抗，B11 续表补澄明-3 行。下部留白多。
- **B12–B15（试验完成情况及续表）**：表头含 **「字段族」「字段」「分母角色」**，像后台字段。续表标题与表头均在。B13 首行是澄明-3 治疗组末字段、末行切入 HORIZON-AD「已筛选」；B15 首行是治疗组「其他来源」。分页在治疗组末行/新产品首行切开，定位成本升高，但未丢表头。
- **B16–B20（流转、失访续表）**：续表规范。表头继续用「字段」「分母角色」。内容几乎全「未公开」，单元格写明文字而非空格或 0。
- **B21–B22（依从性/筛败/补救/禁用药、方案偏离）**：四表+一表均可读；同一套工程表头。B22 下半页空。
- **B23–B24（暴露语境、档案、依据）**：可读。B24 登记号仍为「示例登记号301/201」（fixture）。

### C 类（特应性皮炎临床试验设计比较）

- **C1–C3**：封面/目录/摘要可读。目录无页码，且未列出书签中的四份 NCT 试验档案分项。摘要「未公开」在分析人群列，说明清楚。
- **C4–C7（设计图谱及续表）**：覆盖图「有/未」可读。完整表从 C4 跨 C5–C7，每页有「续表 · 设计图谱完整表」和重复表头。C6 个别单元格第二行以逗号开头（避头尾未处理）。C4 完整表只到 CHRONOS，后续页补齐，属续表而非丢表。
- **C8（人群与入排）**：入排表可并列读原文与条件；运算符混用 `≥` 与 `>=`，不乱码。
- **C9–C10（分组干预续表）**：续表规范。
- **C11–C12（终点卡片与完整表）**：2×2 卡片定义可读。C12「终点」列 `主要终点（IGA` 与下一行单独 `）`；抽取文本中「终点披露/时间披露」表头被拆行。
- **C13–C14（访视时间线及续表）**：时间线点标注偏小；NCT04178967 的 14 与 16 周点靠近。C13 完整表只到两项试验，C14 续表补齐，表头在。C14 仍有行首逗号。
- **C15（样本量气泡）**：图注写明位置不编码优劣、面积∝样本量；无坐标刻度，同页表有例数。可作示意，非定量坐标。
- **C16–C19（四份试验档案）**：一试验一页，字段可定位。页脚 16–19 连续。下半页留白。未见裁切乱码。
- **C20（路径与资料版本）**：**支撑试验列硬折行**：`(ADvantage` / `)`、`(CHRONOS` / `)`、`ADvoc` / `ate2`。列表有句号单独落到下一行。导语出现 **「技术过程不进入受众页」**，像内部约束进了读者页。表体其余中文可读。

## 阅读器与结构证据

结构报告与 `evidence.json` 足以支持本步要核的阅读器事实，且均绑定当前哈希：

| 项 | A | B | C |
| --- | --- | --- | --- |
| 打开/页数 | 10 页，锁定页数一致 | 24 | 20 |
| 中文可抽取（pdftotext） | 全文约 17 221 字节；封面/表体为中文，可检索「特应性皮炎」「EASI-75」 | 约 44 189 字节 | 约 54 540 字节 |
| 书签 | 14 条中文章节名（封面…研究依据与局限） | 23 条 | 18 条（含四份 NCT 试验档案） |
| 页码/页眉页脚 | `all_page_numbers` / `all_header_footer` true；目视每页有报告名+「第 n 页」 | 同左；个别为「第4页」「第 5页」空格不统一 | 同左 |
| A4 / 方向 | 全部 `is_a4`；A1–3、5、10 纵向，A4、6–9 横向 | B1–3、19–21 纵向，多数结果页横向 | C1–3、20 纵向，C4–19 横向 |
| 续表机器标记 | 第 8 页 | 9、11、13、14、15、17、18、20 | 5、6、7、10、14 |
| 哈希绑定 | `locked_sha256` = `pdf_sha256`；页图/文本路径写入 `hash_binding` | 同 | 同 |
| coverage | 12 个门户责任 ID 均 `covered`，`missing_page_ids` 空 | 21 个责任 ID 均覆盖 | 12 个责任 ID 均覆盖 |

说明：coverage 的 `expected_page_count` 是门户责任页 ID 数，不是 PDF 物理页数，二者不一致不视为缺页。书签为中文临床标题，支持标准阅读器导航；目录页无印刷页码，纸质翻页要靠书签或逐页找。未在本环境实际启动 Adobe/Preview。全文检索未见 `&&`、`@@`、`gate`、`pending`、`not_run`、`AI evidence` 等内部状态原样出现。

## 缺陷清单

**未发现阻断**：标准阅读器结构证据支持打开、中文抽取/检索与书签导航；54 页均非空白页、无黑块、无中文乱码；关键疗效/安全性数值在柱顶标签、热图格内数字或同页完整表中仍可取得；跨页表均见「续表」+重复表头。

| # | 页 | 证据 | 严重度 | 用户影响 |
| --- | --- | --- | --- | --- |
| 1 | **B4** | 柱状图 Y 轴 100–0 刻度垂直挤叠、与刻度线碰撞；目视确认字形相交 | **重要但不阻断** | 轴本身难读；柱顶数值与下表仍在，懒于调视图的读者会先觉得「图坏了」 |
| 2 | **C20** | `ADvocate2` 被拆成 `ADvoc`/`ate2`；`ADvantage)`、`CHRONOS)` 括号分行；列表句号孤行 | **重要但不阻断** | 支撑试验身份扫读成本高，易看成两个试验名 |
| 3 | **C20** | 导语「技术过程不进入受众页」 | **重要但不阻断** | 暴露内部约束，不像给医学经理看的正文 |
| 4 | **B12–B22** | 表头「字段族」「字段」「分母角色」贯穿处置/流转/失访/依从/筛败/补救/禁用药/方案偏离 | **重要但不阻断** | 不熟悉计算机的读者会当成后台导出表，定位临床含义变慢 |
| 5 | **A2/B2/C2** | 「正文责任章节；书签与下表标题一一对应」；目录无印刷页码；C 目录未列四份 NCT 档案（书签有） | **重要但不阻断** | 纸质定位弱；电子靠书签尚可 |
| 6 | **A9/B7** | 气泡图无 X/Y 数值刻度；A9 气泡 C 底缘贴/略压框线 | **重要但不阻断** | 不能单靠图读精确值；映射说明+完整表仍在 |
| 7 | **C12** | 「主要终点（IGA」与下一行单独「）」；表头「终点披露/时间披露」视觉拆行 | **重要但不阻断** | 终点列像截断，需对完整定义列才能读通 |
| 8 | **C6、C14** | 单元格第二行以「，」开头 | **后续优化** | 避头尾未处理，不挡语义 |
| 9 | **A6/B4** | 「森林图」无 CI 须；B4 Y 轴标签 `产品/试验` 斜杠拼接 | **后续优化** | 名实不符增加扫读摩擦；差值仍印在条上 |
| 10 | **B5** | 标题「折线图」对单一时间点散点；点上无数值 | **后续优化** | 图注已说明不连线，须看表 |
| 11 | **A4、B1–B3、B8–B22、C1、C16–C19 等** | 封面/续表尾页大面积留白；A4 前两表未撑满橙线宽 | **后续优化** | 仅当读者误判「后面没了」才伤效率；本批未把「未公开」排成空格 |
| 12 | **A4/A7 等** | 页脚「第 4页」「第 5页」空格不统一 | **后续优化** | 不影响找页 |

未把下列项升为缺陷：封面截止日；「未公开」高密度；fixture「示例登记号」；Lebrikizumab 英文通用名；机器 `ok: true`。

## 独立结论

**建议 Codex 不接受本轮视觉放行。**

阅读器结构（哈希绑定、54 页齐全、中文可抽取、书签、页码、A4/方向、续表标记）足以支持打开与检索，且未发现裁切整页、黑块、乱码或关键表完全不可读。但 B4 轴刻度重叠、C20 试验名硬折行与内部指令泄漏、B 类处置表程序员表头，已明显增加不熟悉计算机、懒于调视图的医学经理的误读与定位成本，不符合 Task 8.3「不得出现重叠 / 不可读图表」及中文原生读者页边界。本意见仅供 Codex 终验参考，不自称最终放行。
