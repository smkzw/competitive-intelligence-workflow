# Conference Participant Output: ci-phase8-task83-named-visual-testers - visual_single_object

## Boundary Check

- 只读执行：未修改三份锁定 PDF，未写任何生产路径，未写 runner 报告路径。所有证据读取限定在 `docs/acceptance/runs/8.3/verification/`（A10/B24/C20 共 54 张页图逐张打开）、`summary.json`、`evidence.json`、`pdftotext_full.txt`。
- 未进行最终视觉/验收判定；以下为独立审阅意见，Codex 保留最终权威。
- 未做联网搜索与安全测试；未重新评价 Task 8.2 锁定数值披露状态（"未公开" 大面积表格仅按锁定状态接受，不作内容异议）。
- 无 Trellis 任务创建（delegated worker 模式，按 workflow-state 要求不主动建任务）。

## Independent Work Product

以医学经理视角逐页审阅 54 张 120 DPI 页图 + 逐页文本层 + 书签证据后的发现，按严重度排序：

**阻断级缺陷：未发现。** 54 页均无黑块、乱码（`pdftotext` 全文 0 个 U+FFFD 替换字符）、缺页、渲染空白；页脚统一为 "报告名 · 第 N 页"，页码连续；三份报告 14/23/20 个书签标题与目录一致。

**中等问题（建议列入缺陷清单，由 Codex 裁定是否回炉）：**

1. **气泡图无坐标轴（A p9、B p7）**：疗效—安全性气泡矩阵只有边框内的气泡，**无任何轴线和刻度**，仅靠文字 "向上=发生率更低；向右=疗效信号更强；Y 轴倒序"。医学经理无法从图上读取任何定量位置关系，只能依赖下方表格。C p15 样本量气泡图因明示 "位置仅用于分开展示" 属于设计意图，可接受；A/B 两图声称编码了两个维度却不给轴，属于可读性缺陷。补救：至少加带方向的箭头轴 + 图例刻度，或降级为明示的纯定位图。
2. **混合纵向/横向分页**：A 报告在 p3（纵）→p4（横）→p5（纵）→p6-8（横）→p9-10（纵）间交替，C 报告 p5 单页横版。标准阅读器浏览无碍，但打印装订场景方向频繁翻转，读者需反复转纸。建议同章节内方向一致或全报告统一横版宽表策略。这可能是设计合同既定决策——需 Codex 确认 `track_pdf.md` 是否允许逐页自适应方向。

**轻微问题（排版/中文表达，医学经理会批注但不阻断）：**

3. **B p24 "示例登记号301"**：锁定交付物中出现 "示例" 占位字样，真实医学经理会立即质疑数据真实性。若是 fixture 编号的有意标记，应在页脚或图例说明；否则是最高优先级的表达缺陷。
4. **运算符排版不一致**：C p8 排除条件用 ASCII ">="（"; >= 16 分"），同页入选原文与 C p13/p19 用 "≥"。同报告内混用。
5. **续表表头存在但续表页无章节定位**：B p9/p11/p13-15/p17-18、C p10 等续表页顶部仅有 "续表 · 表名" 题注，无章节标题。续表规则本身执行正确（题注均带 "续表"），符合预期；仅提示跨页断点处读者回看成本。
6. **B p7 与 A p9 图注空格不一致**："向上 = 发生率更低"（有空格）vs "向上=发生率更低"。
7. **C p20 断行问题**："NCT02260986" 在行内被断开为 "NCT0 2260986"；一个句号 "。" 孤立占一行（"不表示优劣判断\n。"）。登记号断裂影响检索复制。
8. **C p8 "筛选期；不适用；不适用"**：排除条件字段以分号堆叠两个 "不适用"，中文原生表达生硬，医学经理会要求改为单一 "不适用"。
9. **B p4 柱状图 Y 轴刻度拥挤**：100/80/60/40/20/0 标签在窄纵列内堆叠，120 DPI 下勉强可读，打印偏小。
10. **A p2 目录无页码**：目录仅列条目（13 项带圆点），不含页码或可视跳转指示；依赖 PDF 书签（14 个，验证存在）。对打印版读者不友好。B p2、C p2 目录同样无页码（对照检查确认三份一致——若属设计合同决策则可接受）。

**证据层异常（需 Codex 复核，低置信）：**

11. 我在同一轮并行读取三张 contact sheet 时，返回图像与请求顺序出现错位（`B/contact-sheet.png` 响应疑似显示 20 页 "设计比较" 内容），但 `evidence.json` 中 contact sheet 的 sha256/page_count 绑定（B=24 页、C=20 页）与单独逐页渲染核对均正确。判断为读取工具的图像返回顺序问题而非证据绑定错误；建议 Codex 以 sha256 重算 contact sheet 一次即可闭环，不应据此改判 PDF。

## Evidence And Assumptions

- 证据：54/54 页图逐张目检（A p1-10、B p1-24、C p1-20）；`pdftotext_full.txt` ×3（288/640/680 行，0 替换字符）；`summary.json`（hash_ok/page_count_ok/coverage_ok 全真，defects=[]）；`evidence.json` 书签与 contact sheet 绑定。
- 工具观察：本会话图像读取工具对同批并行请求多次乱序返回（页脚页码可自我校正，不影响结论）。
- 假设（标注为推断）：混合方向与无页码目录可能是 `track_pdf.md` 设计合同的既定决策 [INFERENCE]，我未读取该合同全文逐条比对；若合同明确允许，则第 2、10 条降级为风格观察。
- "未公开" 密集的 B 类基线/处置表（p8-22）按 Task 8.2 锁定状态接受，仅验证其渲染完整、无裁切重叠。

## Risks, Gaps, And Verification Needs

- **风险 R1（最高影响）**：B p24 "示例登记号" 若为占位泄漏，直接破坏 "锁定 PDF" 的可信度叙事；若是 fixture 有意标记需显式披露。需 Codex 决定：披露说明 or 视为缺陷退回（但本会议只读，不修改）。
- **风险 R2**：气泡图无轴（A p9/B p7）是三名真实医学经理最可能一致提出的图表可读性异议；现有 `defects.json` 为空说明自动检查未覆盖 "图表无语义轴" 这一语义级问题。验证需求：Codex 裁定该图为缺陷还是可接受的设计降级。
- **缺口 G1**：我的审阅基于 120 DPI 渲染；300 DPI 打印级细节（细线、小字号脚注）未验证。如需打印交付，建议 Codex 抽查 2-3 页高 DPI 渲染。
- **缺口 G2**：检索/书签证据我只核对了书签数量与标题前缀、文本层可提取；未在真实阅读器（Acrobat/Preview）实测点击跳转与全文检索定位——按边界这是 Codex 自有职责，建议主会场补一条实测记录。
- **有界问题给 Codex**：(a) 混合方向与无页码目录是否在 `track_pdf.md` 中已有决策？(b) "示例登记号301" 的处置口径？两者不阻断本角色结论；安全临时路径：将第 1、3、4、7 条录入缺陷清单供最终验收引用，PDF 保持锁定不动。

## Recommended Next Step

1. Codex 将第 1 条（气泡图无轴）、第 3 条（示例登记号）、第 4/7 条（运算符一致性与登记号断行）列入 Task 8.3 缺陷清单并逐条裁定 "接受/披露/记录不修"；PDF 本体保持哈希锁定。
2. 以 sha256 重算三张 contact sheet 闭环第 11 条工具异常。
3. 主会场补一条标准阅读器（书签跳转 + 全文检索一个登记号如 NCT02260986）实测记录后，即可进行 Codex 最终验收签署。
