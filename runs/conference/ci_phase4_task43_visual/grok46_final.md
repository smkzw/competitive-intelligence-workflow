强制刷新后只复核上一轮高优先级项，按新口径真实点选。MCP 浏览器被占用。改用独立 Chromium 强制刷新并走完整点选路径。## 最终结论：**PASS**

强制刷新后在 `http://127.0.0.1:8765/filter-test.html` 亲自走完 1280/1024。上一轮 P0（叠字、假红字、超长项丢网址）在当前页复现不了。P0/P1：**无**。余下是非阻断观感，不挡本轮筛选验收。

---

## 本轮真实证据

| 状态 | 视口 | 操作 | 计数 / 行 | Chip | URL |
|---|---|---|---|---|---|
| 初开+开面板 | 1280 | 强制刷新后点「筛选条件」 | **匹配 5 项结果**；甲/乙/丙五行 | 无筛选条件 | 无业务 hash |
| 组合 | 1280 | 狼疮 → 产品甲 → 主要终点 → 治疗期不良事件 | **匹配 1 项**；疗效·产品甲·主要终点 NCT00000001 | `整份报告·适应症/产品`、`疗效数据·终点`、`安全性数据·不良事件术语` | hash 含 ind-sle、product-alpha、ep-pri、ae-teae |
| 清疗效 | 1280 | 「清除疗效条件」 | 仍 1 项 | 终点 chip 没了，**安全性 chip 还在** | 疗效段从 hash 消失 |
| 清安全 | 1280 | 再勾主终点后「清除安全性条件」 | 仍 1 项疗效行 | **疗效 chip 还在**，安全 chip 没了 | 只剩整份报告+疗效 |
| 空结果 | 1280 | 类风关 + 产品丙 + 主终点 | **匹配 0 项结果**；「当前选择下暂无可比较数据」 | 三个 chip 可读，无红字重叠 | hash 含 ind-ra + product-gamma + ep-pri |
| 后退 | 1280 | 浏览器后退 | **匹配 1 项**；产品丙安全行 | 类风关 chip 去掉 | 回到产品丙+主终点 |
| 前进+刷新 | 1280 | 前进再刷新 | 仍 **0 项**，空态与 chip 恢复 | 与空结果一致 | 同一 hash 可复现 |
| 1024 关键路径 | 1024 | 菜单在；同上四选再分别清除 | 组合 1 项；清疗效后安全 chip 独立在；再清安全只剩整份报告 | 四 chip 折行不叠 | 与 1280 同一套 hash 语义 |

截图（当前页，不是旧图）：`/tmp/rev-01-panel.png`、`/tmp/rev-03-combo.png`、`/tmp/rev-07-empty.png`、`/tmp/rev-10-reload.png`、`/tmp/rev-11-1024-combo.png`。控制台：**Errors 0 / Warnings 0**（无 favicon 404）。页面正文不含「验证 / 合成数据 / 测试用途 / 超长标识测试项 / 产品αβγ / 本模块 / 全页范围」。产品选项与表列统一为甲乙丙。

---

## 上一轮高优先级是否关闭

| 原问题 | 本轮 |
|---|---|
| 1 项就报「选择过多」+ 与 chip 重叠 | **关**。空态无该红字；chip 一行排开可读 |
| 超长测试项画面有、网址没有 | **关**。用户侧看不到该项；空态 hash 能前进/刷新恢复 |
| 「全页/本模块」要学软件、一个重置清错块 | **关**。现为「整份报告条件 / 疗效数据 / 安全性数据」，各有「清除…」；实测互不污染 |
| 测试页「验证/合成/测试用途」 | **关**。标题「竞品数据筛选」，脚「竞品临床试验数据」 |
| 产品α vs 产品甲 | **关**。筛选项与表都是甲乙丙 |
| 计数「共 N 条记录」 | **关**。现为「匹配 N 项结果」 |
| favicon/控制台错误 | **关** |

勾了治疗期不良事件后，表仍可能留下疗效行（模块各管各的）。本轮要求是清除互不污染，这一点已满足，不升为阻断。

---

## P0 / P1

**无。**

---

## P2（不挡过）

1. **「重点模块」两张空卡**（疗效数据 / 安全性数据只有编号和标题）— 像没做完，1024 更占地方。最小修复：有内容再留，或先拿掉。  
2. **地址栏仍是 `product-alpha`、`b-efficacy` 等** — 页面中文，拷贝链接仍像后台字段。不影响操作。  
3. **1280 搜索框「搜索页面或关键词..」被裁** — 小。

不建议加重置确认弹窗：后退已能恢复，应保持低打扰。

---

## 值得留

独立「清除疗效条件 / 清除安全性条件」；chip 写清「整份报告 / 疗效数据 / 安全性数据」；空态复述当前条件且无假警报；「匹配 N 项结果」与可见行一致。

---

## 未验证（范围外，非阻断）

Task 4.4 图与表联动；Task 4.5 证据抽屉；正式 A/B/C 真数据；键盘全路径；Safari/WebKit；打印。
