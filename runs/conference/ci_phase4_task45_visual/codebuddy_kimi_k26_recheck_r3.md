# CodeBuddy 医学经理视觉审评 — 第三轮定点复核（R3）

**审评者身份**：不熟悉计算机与 AI、视觉敏感、希望少学少点的中国资深临床试验医学经理  
**审评日期**：2026-08-14  
**审评站点**：`http://127.0.0.1:8767/efficacy.html`、`baseline.html`、`disposition.html`  
**分辨率覆盖**：1280×900、1024×768  
**控制台/网络**：0 条 console 错误；静态资源全部 200 OK  

---

## 实际操作证据

本轮截图保存在 `runs/conference/ci_phase4_task45_visual/codebuddy_kimi_k26_evidence_r3/`：

| 序号 | 截图文件 | 说明 |
|---|---|---|
| 01 | `01_efficacy_1280x900_initial.png` | efficacy 页 1280×900 初始状态 |
| 02 | `02_efficacy_filter_product_beta_1280.png` | 筛选产品乙后 1280×900 状态 |
| 03 | `03_efficacy_different_metric_hint.png` | 固定产品甲后打开产品丙，显示口径差异提示 |
| 04 | `04_efficacy_filter_beta_fixture_removed_hint.png` | 固定产品甲后筛选产品乙，显示固定数据移除提示 |
| 05 | `05_efficacy_1024x768_drawer_open.png` | 1024×768 打开依据面板 |
| 06 | `06_efficacy_1024x768_matrix_clicked.png` | 1024×768 面板打开时点击状态矩阵成功 |
| 07 | `07_baseline_1280x900.png` | baseline 页 1280×900 |
| 08 | `08_disposition_1280x900.png` | disposition 页 1280×900 |

---

## 四项定点复核结果

### 1. 筛选"产品乙"后，柱图和试验状态图的真实 SVG/ECharts 内容与下表一致，不再出现产品甲、试验一；乙的 -7.9 与试验二"进行中"仍在。

**实测状态：PASS**

- ECharts 实例数据验证（筛选 `product:product-beta` 后）：
  - `kz-chart-0`（产品甲柱图）：`no instance` — 正确隐藏
  - `kz-chart-1`（产品乙柱图）：`data = [-7.9]`，`xAxis = ["对照组"]` — 只保留产品乙的对照组 -7.9
  - `kz-chart-2`（产品丙热图）：`no instance` — 正确隐藏
  - `kz-chart-3`（状态矩阵）：`data = [[0,0,0.61]]`，`xAxis = ["状态"]` — 只保留一个状态
- 图表可视化内容已与筛选条件一致，产品甲、试验一不再出现在 ECharts 渲染数据中。
- 乙的 -7.9 仍在（柱图），试验二"进行中"仍在（状态矩阵）。

### 2. 只固定产品甲 EASI，再打开产品丙疾病活动度时，立即出现终点/组别等口径差异中文提示。

**实测状态：PASS**

- 固定产品甲 · 主要终点 EASI评分较基线变化值后，点击产品丙 · 次要终点疾病活动度评分较基线变化值。
- 数据依据面板固定对照区域明确显示：
  > "指标定义、分母存在差异；并列用于核对口径，不代表可以直接比较数值。"
- 医学经理在并列核对时会被明确告知口径差异，避免直接比较不同量表/终点的数值。

### 3. 固定产品甲后筛选产品乙，即使依据面板随越界当前项关闭，主页面仍明确显示移除了固定数据。

**实测状态：PASS**

- 固定产品甲 → 筛选产品乙后：
  - 数据依据面板自动关闭（`drawerDisplay: none`）
  - 页面顶部明确提示："筛选范围已更新：已移除 1 条不在当前筛选范围内的固定数据。"
  - 固定对照表格从 2 列变为 1 列（仅剩产品甲）
- 医学经理不会再误以为固定数据"丢失"，提示文案清晰、位置醒目。

### 4. 1024×768 打开依据面板后，直接点击状态图或柱图中心不再被面板拦截；面板仍可读且位于页眉下。

**实测状态：PASS**

- 依据面板打开时（`drawerTop=68=headerBottom`），直接点击状态矩阵（`#kz-chart-3`）：
  - Playwright 标准 click 成功，URL 从 `report-row_e9041f...` 变为 `report-row_4bc62e...`
  - 无 `aside subtree intercepts pointer events` 报错
- 柱图（`#kz-chart-1`）同样可正常触发，无拦截
- 面板宽度从上一轮 560 px 缩减至 430 px，1024 宽屏下遮挡范围显著减小
- 窄屏菜单保持收起（`navMenuDisplay: none`）

---

## 顺带回归确认（上一轮已修复项）

| 项 | 验证结果 | 证据 |
|---|---|---|
| EASI 终点/单位 | PASS。图表标题显示"主要终点 EASI评分较基线变化值｜单位：分"，表格单位列显示"分"。 | 截图 01、JS 提取 |
| EASI 基线依据不混入 Age | PASS。baseline 页产品乙为"湿疹面积与严重程度指数"（量表 EASI），产品甲为"年龄"。 | 截图 07、JS 提取 |
| 热图表格数值与图一致 | PASS。热图数据依据面板显示 -9.4，对应表格行显示 -12.1/-9.4。 | JS 提取 |
| 状态矩阵语义 | PASS。表格显示"试验一/已完成""试验二/进行中"。 | JS 提取 |
| 页面无 2400 px 空白 | PASS。efficacy bodyHeight≈3576（内容合理），disposition bodyHeight=768。 | JS 测量 |
| 搜索"产品乙"有结果、无匹配有反馈 | PASS。搜索"产品乙"跳转至 baseline 页；搜索"不存在的药物XYZ"显示"未找到匹配页面或数据"。 | JS 提取 |
| 未公开/已报告零值状态覆盖 | PASS。fixtureRows 中产品乙治疗组 EASI 显示"未公开"，安全项显示"已报告零值"。 | JS 提取 |

---

## P0/P1 问题

**无新 P0/P1 问题。**

上一轮的两个 P1 问题本轮均已修复：

- **P1-1（筛选后固定项静默移除）** → 已修复。现在明确提示"筛选范围已更新：已移除 1 条不在当前筛选范围内的固定数据。"
- **P1-2（窄屏抽屉遮挡主内容点击）** → 已修复。drawer 宽度从 560 px 减至 430 px，状态矩阵和柱图在 1024×768 下均可正常点击，无拦截。

---

## 结论

**PASS**

四项定点复核全部通过，上一轮 P1 问题已修复，无新 P0/P1 问题。EASI 字段、热图数值、状态矩阵语义等已修复项未回归。站点在 1280×900 与 1024×768 下均具备医学经理可用的数据核对与固定对照能力。
