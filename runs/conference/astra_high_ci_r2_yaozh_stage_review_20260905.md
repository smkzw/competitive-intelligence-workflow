**当前阶段判断：有条件通过。**

拟议边界符合 v1.3，建议按此方向实施；**当前实现尚不能作为“药智来源计划与宿主预检已接通”的验收依据**。本阶段应同时补齐三态消费、非阻断操作回执、预检结果执行和恢复绑定，不能只新增一条 `authenticated-browser` 路线。

本轮仅检查指定的 23 个文件，未修改文件、运行项目、执行 pytest 或调用真实宿主。历史 checkpoint 中的测试数字仅作为既往记录。CLI 实现、底层 receipt/schema、宿主适配器和完整科学门不在允许阅读清单中，对这些部分不作已验证声明。模型与 effort 须由 runner 调用回执确认，本轮没有可独立核验该身份的记录。

**P0/P1/P2/P3 发现**

**P0：指定材料中未发现可确认的 P0。** 未观察到真实凭据泄露，也未完成运行期安全验收。

**P1-1｜预检结果没有成为 run service 的执行门。**

- 证据：[run_service.py:458](/Users/smkzw/Documents/AI%20Products/competitive-intelligence-workflow/src/ci_workflow/application/run_service.py:458) 固定使用 `host="local"`、`source_routes=("public-http",)`，计算矩阵摘要后只返回 ID，没有检查研究或交付是否阻断。第 1435–1438 行仍按普通共享节点完成。
- 合同风险：缺少独立上下文能力时，预检矩阵即使为 `blocked`，此 handler 仍正常返回；与设计 §5.4“缺少独立审查能力即预检失败”冲突。这不等于后续科学门已经被绕过，但入口预检本身没有履行合同。
- 本阶段修改：保存可核验的矩阵及实际宿主身份，按受影响操作消费其结果。必需能力缺失阻止相应操作；药智可选能力缺失不得阻断其他来源。不能以统一的“全部通过/全部阻断”代替依赖判断。

**P1-2｜当前认证浏览器模型会同时产生假可用和过度阻断。**

- 证据：[capability_preflight.py:240](/Users/smkzw/Documents/AI%20Products/competitive-intelligence-workflow/src/ci_workflow/application/capability_preflight.py:240) 将检索、登录和验收浏览器都实现为空白 Chromium 渲染；第 385–406 行把选中的 `login_browser` 纳入研究依赖，第 483–510 行将其失败传播到全部所选报告及交付。
- 合同风险：空白浏览器成功不能证明可使用用户已登录会话；直接添加认证路线，又会让药智成为核心研究的硬依赖。
- 本阶段修改：区分“宿主能操作用户浏览器”“此次药智访问结果”“核心研究是否可继续”。会话是否有效在实际访问时判断；工具能力缺失、会话失效、网络失败分别记录。不得通过读取或导出 Cookie、浏览器存储来证明可用性。
- 本轮纯内存源码求值已确认：选择认证浏览器后，`_research_dependencies()` 确实包含 `login_browser`。这属于确定性函数检查，不是实机浏览器测试。

**P1-3｜三态约束只封住写入入口，未封住模型与读取入口。**

- 证据：[intake.py:187](/Users/smkzw/Documents/AI%20Products/competitive-intelligence-workflow/src/ci_workflow/application/intake.py:187) 的 `YaozhAnswer` 仍包含 `session_expired`，`YaozhAccessRecord` 和 `record_yaozh_answer()` 接受该类型。[yaozh_access.py:104](/Users/smkzw/Documents/AI%20Products/competitive-intelligence-workflow/src/ci_workflow/application/yaozh_access.py:104) 只在写入 API 检查初始三态；读取记录时未重复这个约束。
- 合同风险：结构合法的 `session_expired` 持久记录可通过当前读取校验，违反设计 §5.3 和 Task 10.6 implement 第 50 行。
- 本阶段修改：初始回答类型收窄为三态；运行期访问诊断另用类型表达。禁止把已有四态记录静默迁移成 `skipped`。
- 另一个完整性限制：当前校验能拒绝非法结构和身份替换，不能证明所有“结构合法的内容改写”均被识别。正式文档不应笼统宣称所有篡改已封闭；消费后应以既有事件或运行输入摘要绑定原回答，不新增签名体系。

**P1-4｜悬空软链接被误判为“没有回答”。**

- 证据：[yaozh_access.py:109](/Users/smkzw/Documents/AI%20Products/competitive-intelligence-workflow/src/ci_workflow/application/yaozh_access.py:109) 和第 139 行先调用 `exists()`，只有目标存在时才进入普通文件校验。悬空软链接会绕过第 67 行检查；读取返回 `None`，写入则可能通过 `os.replace()` 替换该链接。
- 合同风险：违反“软链接失败关闭、不重复询问、不覆盖已有对象”。[test_yaozh_access_cli.py:263](/Users/smkzw/Documents/AI%20Products/competitive-intelligence-workflow/tests/integration/test_yaozh_access_cli.py:263) 只覆盖目标存在的软链接。
- 本阶段修改：先检查路径对象类型，再判断记录缺失；补悬空链接及父目录路径边界测试。保持现有单写者范围，不借此建设跨进程事务系统。

**P1-5｜回答尚未进入任务，直接接线还会撞上旧任务及预检复用。**

- 证据：[autonomous_research.py:133](/Users/smkzw/Documents/AI%20Products/competitive-intelligence-workflow/src/ci_workflow/application/autonomous_research.py:133) 不接收项目回答，第 215 行总是生成 `answer_required_once`；[run_service.py:563](/Users/smkzw/Documents/AI%20Products/competitive-intelligence-workflow/src/ci_workflow/application/run_service.py:563) 在任务字节变化时拒绝继续，同字节也重新写文件。预检输入摘要目前只含节点、报告和合同版本。
- 合同风险：已回答仍出现待询问任务；未来加入回答字段后，首次任务→回答→resume 的正常路径可能被判为合同冲突；旧预检还可能跨宿主或环境变化复用。
- 本阶段修改：明确“未回答任务→回答已绑定任务”的合法、可恢复迁移。任务绑定回答摘要、来源政策内容摘要和适用报告；同值重放不改文件。可变能力在下一次相关操作前复查，不能永久复用旧环境判断，也不能为了重新检查药智而重建已接受门户。

**P1-6｜药智来源权威与操作回执仍缺少可执行合同。**

- 证据：`source-policy-v1.yaml` 尚无药智条目；[autonomous_research.py:125](/Users/smkzw/Documents/AI%20Products/competitive-intelligence-workflow/src/ci_workflow/application/autonomous_research.py:125) 的证据来源选择仅取 `DIRECT`。现有 `YaozhRouteDecision` 只有启用布尔和说明，不是绑定实际操作的回执。
- 合同风险：仅加提示文字不能证明药智不会单独支撑关键数值或监管结论。反过来，为让药智进入 `_direct_sources()` 而赋予 `DIRECT` 会直接违背拟议边界。
- 本阶段修改：独立生成可选辅助路线，药智所有声明域均不得为 `DIRECT`，不得设为全球/中国必查来源。疗效、安全性及基线数值仅作定位线索；身份、设计、监管及企业信息最多交叉核验。最终事实仍须具有相应声明域的适格依据。
- `SourceEligibility` 第 55 行要求科学片段引用，`RouteCompletion` 第 24 行要求完成策略单元。不能为满足这些结构而给“用户跳过”编造检索尝试、证据片段或科学完成。底层 receipt 未在本轮读取，具体兼容方式仍需下一实现切片核查。

**P1-7｜浏览器接入前必须封闭错误输出，不能依赖事后扫描。**

- 证据：[run_service.py:1365](/Users/smkzw/Documents/AI%20Products/competitive-intelligence-workflow/src/ci_workflow/application/run_service.py:1365) 将一般异常 `str(exc)` 传入事件，第 3158 行保存原始原因；能力诊断字段也允许任意文本。现有凭据 CLI 测试检查拒绝参数，但未断言错误输出不回显所传值。
- 合同风险：新浏览器路线若把含认证 URL、请求头或页面存储的异常直接传入通用错误路径，材料可能进入事件、日志和后续提示词。目前这是接入风险，未观察到实际泄露。
- 本阶段修改：浏览器边界只输出封闭诊断码和受控中文说明；不得传入原始异常、HAR、存储导出、认证页面全量快照或未经筛选的截图。原文证据采用最小必要内容，不以认证材料换取可追溯性。

**P2-1｜Publication 任务不能仅由声明域的直接来源集合推导。**

- 证据：C 的声明域仅为试验身份/设计/状态；政策中 PubMed 在该域为 `cross_check`，因此不会进入 C 的 `_direct_sources()` 集合。任务虽写“Publication 识别”，但没有显式的登记关联论文与附件子任务。
- 风险：不能据任务标题证明设计 §5.2 的主要、延长期、关键长期/安全发表物检查已实例化。
- 建议：在 R2.3 增加明确任务与终态依据；论文发现、论文分类、全文访问、人工补件分别处理。完整 manual gate 可另切片，但仍属于 v1 必须完成项。

**P2-2｜运行完成文案可能掩盖待科学复核状态。**

- 证据：`run_service.py:1915–1986` 在无复核回执时保留 `rendered_unreviewed`，函数仍返回 `"completed"`；第 3288–3291 行据运行结局写“项目运行完成”。
- 建议：保留内部运行结束语义，但用户文案应表达“网页已生成，仍待复核”等实际状态。药智操作完成更不得推进科学接受。

**P2-3｜正式质量门范围需要消除文档歧义。**

- 证据：路线图第 65 行要求 `mypy --strict src tools`，执行计划第 48 行仅列 `src/ci_workflow`；当前 metrics 第 14 行记录的是 `src tools`。
- 建议：统一规范命令、范围和脚本引用，保留实际较完整检查范围；不得借本次接线缩小门禁。

**P3｜孤儿快照回收不应进入此次药智切片。**

- 证据：渲染事务 checkpoint 第 32–33、50–51 行已明确其非覆盖、单写者边界。
- 建议：继续登记，延期小型治理；不扩大渲染事务，不自动清理快照。

**建议的三态及运行期映射**

| 项目回答 / 运行期情况 | 路线行为 | 操作回执 | 核心研究影响 |
|---|---|---|---|
| 尚未回答 | 保留一次原生询问待办，不启动药智 | 不伪造 `skipped` 或不适用 | 其他来源继续 |
| `available`，浏览器可用且会话有效 | 执行可选辅助检索 | 记录实际访问结果；不授予事实接受 | 适格来源继续 |
| `available`，会话失效 | 暂停该路线，提示用户自行登录 | 技术访问受限，原因 `session_expired` | 不覆盖回答，不阻断其他来源 |
| `available`，宿主缺浏览器工具 | 不执行该路线 | `tool_capability_gap` | 不冒充会话失效 |
| `unavailable` / `skipped` | 不探测、不访问药智 | `not_applicable`，绑定对应回答及政策依据 | 非阻断，不计作科学接受 |
| 回答记录损坏或身份不符 | 拒绝消费损坏合同，给恢复指引 | 完整性错误 | 不自动重问或改成跳过 |

操作回执至少绑定项目、回答摘要、政策摘要、路线、宿主、当次操作身份及结果。`not_applicable` 表示这条可选操作不执行，**不表示临床字段不适用、宇宙闭合、两轮恢复完成或独立复核通过**。

**建议的最小本阶段 source-set**

| 实现面 | 本阶段最小变更 |
|---|---|
| `intake.py`、`yaozh_access.py` | 初始三态与运行期诊断分离；修复悬空链接；保留一次回答和同值字节幂等 |
| `source-policy-v1.yaml`、必要时 `sources/policy.py` | 增加药智非直接权威矩阵、版本及约束；不提升为必查来源 |
| `autonomous_research.py` | 消费回答、生成可选路线或不适用操作；保留全球/中国及四类反向扩展 |
| `capability_preflight.py` | 可选路线依赖与核心依赖分离；区分浏览器能力和实际访问状态 |
| `run_service.py` | 消费预检结果；绑定当前回答/政策/宿主；支持正常任务迁移和恢复 |
| typed receipt 及对应 schema | 优先复用既有来源审计体系，增加封闭操作类型；不伪造科学证据引用 |
| CLI、三宿主薄适配层 | 仅传递确定性状态、执行原生 Ask、提供中文结果；不新增凭据入口或自然语言解析器 |
| 指定四个测试文件及必要相邻测试 | 补以下负向矩阵；模型/schema、提交与恢复合同同步 |
| 设计、路线图、执行计划、Task 10.6 当前文档 | 同步映射、阶段证据与未完成边界；历史 checkpoint 保持原样 |

CLI、receipt/schema 和适配层的实际文件需由下一实施合同明确纳入检查范围；本轮未读取。`sources/planner.py` **不必默认修改**：仅在操作回执确实需要适配时改动，保留其科学路线完成校验。

**建议测试矩阵**

| 测试组 | 必须证明的行为 |
|---|---|
| 初始回答 | 三态正向；API、模型、读取入口均拒绝第四态；改答、错项目、非法字段失败关闭 |
| 文件边界 | 普通及悬空软链接均拒绝；损坏记录不重问；同值重放字节及 mtime 不变 |
| 非阻断 | 三态 × A/B/C及联合运行；跳过不调用浏览器；登录失败不阻断其他适格来源 |
| 真实能力语义 | 空白 Chromium 成功不能证明登录成功；无工具、会话失效、超时、验证码分别诊断 |
| 预检执行 | 独立上下文缺失阻止相应研究操作；药智缺失不阻止；用户收到准确中文指引 |
| 恢复 | 首次待办→回答→resume；重复恢复；宿主切换；会话失效→用户登录；回答始终不变 |
| 摘要绑定 | 回答、政策、任务或回执替换不能复用旧结果；正常迁移不误报篡改 |
| 科学权威 | 药智单独提供疗效、安全性、监管状态时不能满足关键门；冲突进入既有冲突门 |
| 宇宙与独立复核 | 药智不适用不能替代必查路线、四类扩展、两种恢复或独立遗漏检查 |
| Publication | 药智登录问题不触发论文补件门；必需论文不可用仍走唯一补件与证据充分性判断 |
| 凭据边界 | 合成秘密置于未知参数、URL、异常、嵌套元数据及浏览器结果；输出、日志、回执、提示词和包均不得含该值 |
| 多报告与快照 | A 已有门户、B 等待、C 阻断等组合；药智状态变化不得改写既有 HTML、快照、复核请求或阻断决定 |
| 三宿主 | 同一输入产生同义任务、回执和恢复结果；测试替身与真实宿主证据分开记录 |

本阶段先完成定向合同和相邻集成检查，再执行正式质量门及隔离安装验证。真实三宿主、真实登录/失效路线须在相应阶段权限内获得证据；模拟结果不能记为真实能力已验证，也不能因没有药智账号而新增全产品阻断。

**路线图、设计和执行计划修订建议**

1. **设计 §5.3、§11.3**：补上上述映射，明确 `available` 是项目允许启用辅助路线的回答，不是实时认证证明；浏览器存储及认证材料不得进入提示词和任何记录。
2. **R2.1/R2.3**：将下一切片界定为“项目回答→来源计划→可选能力→操作回执→恢复”的闭合链；预检结果消费列为前置修复。
3. **R2.2–R2.4**：继续分别跟踪宇宙闭包、Publication/manual gate、逐对象门槛；不能因药智接通而一起勾选完成。
4. **Task 10.6 implement 第 50 行**：在取得证据后，分别记录持久化、合同接线、真实浏览器路线、三宿主验证状态。
5. **R5/R6**：保留最终安装入口、三宿主、不可变快照和实际门户验收要求；本次操作回执不构成科学或发布回执。

**新增功能建议与下一阶段优先级**

- **优先于扩大药智功能：计划—实际回执对账，进入 v1。** 每条必需路线及登记关联发表物任务都应能对应实际执行或合法终态，拒绝用自由文本“已完成”代替闭合。这是在实现既有 v1 合同，不是增加产品范围。
- **本阶段进入 v1：可选来源失败的中文提示及有限恢复。** 提示应说明其他资料研究继续；不反复询问项目初始回答，不后台轮询登录状态。
- **随后完成、仍属 v1：Publication/manual gate 和全球/中国宇宙闭包。** 其必要性高于药智批量浏览、商业数据库横向扩展。
- **deferred：** 多商业数据库通用插件框架、统一账号中心、自动续登、后台会话监控、跨进程并发研究及孤儿快照自动回收。

**不应实施的过度设计**

- 新建第二套事实库、进度数据库、回执账本或认证状态中心。
- 导出/复制用户浏览器 profile、Cookie、token 或存储来实现“可恢复登录”。
- 将药智不适用计成 `accepted`，或为它伪造来源片段、检索尝试和恢复轮次。
- 放宽来源权威、竞品宇宙、Publication、独立复核或不可变快照合同。
- 为药智状态变化重建全部 A/B/C 门户，或修改已绑定的渲染事务产物。
- 增加自然语言 CLI、复杂通用爬虫框架、PKI、自动登录或定时监测。

读取结束时，23 个指定文件的“相对路径→SHA-256”映射经排序、紧凑 JSON 编码后的摘要为 `766a3c3e0e7d7ac823230e49b49c828410b74c64c70e865e7d4ef7abe3d2b48d`。本结论绑定该文件集合。

当前环境为只读；本审阅正文由 runner 保存至 `runs/conference/astra_high_ci_r2_yaozh_stage_review_20260905.md`，本轮未自行写入该文件。