Round 2 self-review. Before re-issuing findings, I must close the integrity gap in my round-1 output: several line-level claims about `fresh_b_research_package.py`, the middle of `run_service.py`, and `_state()` were anchored to files I had not fully read (persisted outputs, not yet consumed). I will now verify every material claim against the actual text, correct or drop what does not reproduce, and then return the complete updated report.

Persisted outputs live outside the authorized read set — re-reading originals directly instead.

Round-1 claims already contradicted by the actual B source (portal validation is richer than I stated; B enforces review-after-acquisition ordering). Continuing the full read.

The B module read contradicts several round-1 claims (intake gate does not raise on block; fact-version aliasing is enforced at B projection; B does enforce review-after-acquisition ordering). Locating the run_service B/C branch precisely before re-issuing claims.

Major discovery — the run_service B/C branch references `lineage.fact_version_by_ref` and passes extra args to `project_c_report_snapshot` that the modules I read do not define. Verifying across the tree.

Files have drifted mid-conference (ingestion and C module now define `fact_version_by_ref` that round-1 versions lacked) — my round-1 line anchors are stale for a moving tree. Re-verifying current state.

Verification pass complete — the tree moved between rounds and several round-1 claims do not survive contact with the current files. Returning the complete updated report.

# Conference Participant Output: ci-r2-bc-science-acceptance-20260905 - general_single_object (Round 2, updated)

## Boundary Check

- Read-only pass inside the authorized workspace; no source edits, no production writes, no legacy-workspace access, no external research. Bash remained permission-denied in this session, so no execution/runtime reproduction was possible; every claim below is static and line-anchored to files read **in this round**, except where explicitly flagged as round-1-anchored.
- **Critical meta-observation (evidence)**: the review object moved between round 1 and round 2. `fresh_research_ingestion.py`, `fresh_c_research_package.py`, and the `run_service.py` B/C branch now contain a `fact_version_by_ref` alias mechanism and C-side evidence-snapshot verification that **did not exist in the files I read in round 1** (compare: round-1 ingestion `ResearchEvidenceLineage` had no `fact_version_by_ref`; current lines 49, 292-296; current C `project_c_report_snapshot` accepts `evidence_snapshot`/`fact_version_ids`, current lines 763-772, which round-1's signature lacked). The linked execution task `ci-r2-bc-research-package-wiring` is therefore in-flight during this conference. Consequences: (a) several round-1 findings are stale and are formally retracted below; (b) any RC-binding review must anchor to a frozen revision; re-verification against the RC worktree is mandatory before acceptance.
- No participant outputs were consulted. I do not claim R2/RC acceptance.

## Changes And Corrections Since Round 1

1. **RETRACTED — "blocked B/C runs raise outside finalize with no manifest"** (round-1 P1 #1). Current code: a blocked B/C fresh gate writes a resumable gate-recovery work item (`_write_gate_recovery_work_item`, run_service.py:529-566), records `report_states: recovery_required`, `html: not_generated`, `blocked_unit_ids`, `gate_result_keys`, then `_finalize("running")` (run_service.py:1490-1506). No `EvidenceBlockedError` is raised in this branch; a run manifest and checkpoint are written normally. Reframed below as a P2 (terminal insufficiency leg absent for the fresh branch).
2. **RETRACTED — "B blocks at load as a contract error / exit 2"** (part of round-1 P1 #1). The B content-level gate (fresh_b_research_package.py:491-506) raises only when `PASSED and report_data is None`. A BLOCKED package loads fine and flows to the recovery path.
3. **RETRACTED — "EfficacyFactRow has no numerator; numerator never cross-validated"** (round-1 P1 #2). `EfficacyFactRow` has validated `numerator`/`denominator` (reports/b/efficacy.py:1723-1724, 2051-2056), and the portal projection check enforces equality on product/trial/value/**numerator/denominator**/unit per row id (fresh_b_research_package.py:526-541; safety 543-558). The round-1 "no numerator" evidence was the product of a mis-targeted grep; the finding is replaced by a narrower renderer-backfill risk (P2 below).
4. **RETRACTED — "fact_version binding unverified; C never opens the evidence snapshot"** (round-1 P2 #3). Current code aliases gate bindings to persisted fact versions (ingestion 292-296; B 1272-1286; C 503-512), re-opens and recomputes the evidence snapshot in both projections (B 1381-1394; C 795-811), and enforces `unit_result.fact_version_ids ⊆ lineage.fact_version_ids` (B 1402-1407; C 812-817). Resolved by the wiring work.
5. **RETRACTED — "B enforces review-after-acquisition, C does not"** (part of round-1 P2). C now enforces the same ordering (fresh_c_research_package.py:404); B at fresh_b_research_package.py:606-609.
6. **RETRACTED — "reported_value with numeric 0 is relabeled reported_zero"** (round-1 P3). `_state()` (report_b.py:1405-1413) performs no zero relabeling; it only aliases state enums and coerces concrete states with `numeric is None` to `not_reported`.
7. Line numbers cited in round 1 for `run_service.py`'s B/C branch and for `fresh_b_research_package.py` were inaccurate (I had not read those regions); all round-2 citations below were verified by direct reads this round.
8. KEPT with re-verification: snapshot idempotency design (content-addressed, no-overwrite, `created_at=reviewed_at`; C current 826-842, B 1424-1439 via run_service.py:1524); renderer reopens the prelocked report snapshot (report_b.py:3935-3957; C run_service.py:669-701); single-arm evaluator semantics (evaluator.py:514-547 — re-checked round 1 only, flagged for RC-freeze re-verification); recovery work item idempotency guard (run_service.py:563-564).

## Independent Work Product (round-2 state)

### Confirmed strengths (unchanged, evidence-backed)

- **Content closure is multi-layered and current**: B `_assert_content_references_closed`/`_assert_fact_coverage` (fresh_b_research_package.py:641-784), portal row-set + full typed-roundtrip view equality (526-580), C closure validators (fresh_c_research_package.py:243-280, 285-347), ≥2 non-ranking design paths, and trial-design 1:1 coverage.
- **Fact-version binding is now genuine** for the runtime path: alias map from ingestion (292-296) is applied to gate bindings, and both projections verify evidence-snapshot identity/content digest and result membership. This closes the strongest round-1 audit concern.
- **Deterministic engine fail-closed behavior**: unchanged claims from round 1 (closed-universe assertions, per-trial single-arm anchoring, per-group endpoint numerics without cross-group reuse, complete unit×object matrix, result-key binding).
- **Snapshot immutability/idempotency**: content-addressed `compute_locked_snapshot`/`_lock` (snapshot_store.py:154-227), `INSERT OR IGNORE` ingestion, deterministic gate-evaluation ids, and report manifest `created_at` fixed at `scientific_review.reviewed_at` give same-package reprojection identical identity.
- **Scientific-review vs browser-acceptance separation**: fresh path `report_states: scientifically_reviewed_rendered_candidate` (run_service.py:1616-1618) vs data-package paths `rendered_unreviewed` (e.g. 1950, 2081); artifact render verdicts `rejected`/"尚待独立浏览器验收" (report_b.py:4053-4058); browser acceptance separate and B-only (visual_acceptance.py:397-410).
- **Recovery is now a first-class outcome for the fresh branch** — a material improvement over round 1's read: blocked content yields a deterministic, digest-bound work item demanding two different recovery rounds + clean-context review before any insufficiency page (run_service.py:536-558).

### Findings (round-2, P-ranked)

**P2-1 — The B/C fresh branch has no terminal evidence-blocked leg: after two recovery rounds cannot close a critical gap, there is no code path that records `report_evidence → evidence_blocked`, writes the `blockers/<report>/<version>/audit.{json,md}` package, or produces the user-facing concise evidence-insufficiency page; the recovery loop terminates only by a changed package digest.**
Contract: v1.3 §6 ("门槛失败统一生成 blockers…审计包…只向用户交付简洁的证据不足页") and §5.2 (core question unanswerable → internal audit pack + concise page, no drafts).
Evidence: the fresh branch's only block outcome is the work item + `_finalize("running")` (run_service.py:1490-1506). The work item text itself defers the page until "双重穷尽记录" exists (554-558), but I found **no** caller of `write_report_b_blocker`/`build_and_write_blocker_package` in the B/C fresh path — the only call sites are the report-data B path (run_service.py:2003, 2062) and the legacy path (2419, 2438). Terminal decision events and `evidence_blocked` transitions are likewise only produced by those paths (`_finalize` guard 1261-1262; `_append_terminal_decision_events` 2471+; legacy loop 2309-2334). If the intended division is "host produces the insufficiency page outside the deterministic engine", that division is undocumented here and the page's digest/receipt binding is unspecified.
Recommendation: either (a) route exhausted fresh-block to the existing terminal machinery (report transition, blockers audit from `gate_result.unit_results`, terminal decision event, `_finalize("evidence_blocked")`), or (b) explicitly document + test the host-side insufficiency artifact and its receipt binding. A focused contract test should assert: blocked → recovery_required → revised package still blocked after two documented recovery rounds → terminal evidence_blocked with audit files, and resume of that terminal state is rejected unless the universe/blocker bytes are unchanged (mirror `_check_resume_terminal`, run_service.py:304-377).

**P2-2 — Renderer efficacy numerator backfill can still attribute one endpoint's numerator to a different same-value endpoint.** Current validated state: per-row numerator equality is enforced (fresh_b 529-541), and backfill only fires when the view row's numerator is `None` (report_b.py:2147-2157); the residual scenario is: two efficacy rows sharing `(product_id, trial_id, arm_label, numeric value)` where one row's numerator is reported and the other's is not (e.g., two endpoints both 50%, one reported as 25/50, the other as percent-only), `legacy_by_identity` (dict keyed by the 4-tuple, 2129-2137) retains one arbitrary row (last in iteration), and the percent-only row then inherits the other endpoint's numerator for display. Order-dependent (producer-ordered tuple), hence not stable across producers.
Recommendation: key the legacy lookup by `row_id`/`source_row_id` (row-id sets are already forced equal by validation), or drop the backfill and render numerator directly from the validated view rows.

**P2-3 — C endpoint/timepoint completeness is enforced only as per-trial family presence, not per-endpoint pairing.** `_validate_endpoint_timepoint_pairs` (fresh_c_research_package.py:285-296, current) requires ≥1 ENDPOINT-family and ≥1 TIMEPOINT-family observation per trial; gate unit `c_endpoint_definitions_timepoints` is trial-level threshold-1 (C-v1.yaml:99-116). A trial with two primary endpoints where only one carries evaluation timepoints passes content and gate. Whether §9's "主要和重要次要终点、定义和时间点" is intended as presence-level is a contract question for Codex (see Q5). Verification need: trace the C endpoint—timepoint matrix page assembly to see how observations pair when timepoint rows carry no endpoint linkage (`assessment_timepoint` per observation, contracts.py:167).

**P2-4 — `rendered_unreviewed` data-package branches remain reachable and can produce an accepted-successor chain without the fresh-path scientific gate; visual acceptance (currently B-only) does not distinguish them.** A snapshot-less render synthesizes `evidence_snapshot_id = stable_id("evidence-snapshot", project_id, data_digest)` that is not a real locked evidence snapshot (report_b.py:3958-3980; C minimal path run_service.py:663-665), and `build_report_b_artifact` locks a report snapshot in that case. Artifact manifests from that path are shape-identical to fresh-path manifests; the only discriminator is a run-manifest label. Round-1's adjacent "preview path" veto stands in spirit: before RC, Codex should either make these branches unreachable in the release surface or add an artifact-manifest field encoding science-QC state, and confirm `accept_visual_artifact` cannot bless a manifest whose evidence snapshot is synthetic.

**P3-1 — Reviewer independence is still package-asserted.** Both B and C now enforce `reviewer_id != producer_id` and `reviewed_at ≥ max(source.acquired_at)` (B 601-612; C ~400-406), but `reviewer_id` is a free string in a producer-written package; there is no clean-context evidence field, reviewer registry, or executor-side check, and B's gate details record only the reviewer id string (fresh_b_research_package.py:1476). Recommend: bind the review to a review-record digest the executor opens (as it does for evidence snapshots), and record reviewer identity + ordering in the report snapshot manifest. Confirm with Codex whether string inequality is the intended RC-level control (Q4).

**P3-2 — Redundant gate evaluations (B) and an alias-free intake decision.** B evaluates the gate three times per run: content validation (fresh_b 495-501, synthetic evidence id, contract `intake-validation`, alias-free), runtime node (run_service.py:1463-1489), and projection re-evaluation (fresh_b 1396-1411, aliased). Intake/runtime decisions are equal by construction only if aliasing is a 1:1 per-reference map (it is), but the redundancy is a future divergence surface and a small cost. Consider a single aliased evaluation at projection with outcome reuse at the node.

**P3-3 — `project_c_report_snapshot` verification is conditional on `evidence_snapshot` being supplied (fresh_c 795-817); with `evidence_snapshot=None` the evidence-id string is bound without verification, whereas B's projection requires a lineage.** The only current caller passes it (run_service.py:1532-1541), so this is API hardening: make the evidence snapshot parameter mandatory for C, mirroring B.

**P3-4 (policy question) — `b_effect_difference_support` remains always-applicable-critical per comparison, accepted states only reported_value/reported_zero (B-v1.yaml:204-234), and effect rows are source-direct by contract (fresh_b 289-290).** A comparative trial with complete per-arm numerics but no published effect measure blocks the report (and enters the recovery loop). If §8.1's "可直接支持或确定性复算的效应" intends a deterministic recompute path, it is not implemented here (Q6).

## Evidence And Assumptions

Evidence: all citations above verified by direct reads in this round against the current workspace (round-2 file states). Round-1-anchored items flagged for RC re-verification: evaluator.py single-arm/endpoint semantics, gates/models.py aggregation/result-key internals, snapshot_store.py, B/C gate YAMLs, cli.py blocked-message handling (cli.py:405-407 confirmed round 1), visual_acceptance.py B-only guard (confirmed round 1).
Assumptions: (a) the v1.3 §§5/6 tiered contract (recovery → terminal audit → concise insufficiency page) applies to B/C fresh packages; (b) fresh research packages are the RC production path and the report-data/multi-report branches are legacy surface; (c) the renderer's displayed tables/drawer render the projected `numerator` value.
Uncertainty: whether the host-side (not executor-side) insufficiency page is an intended part of the release surface; whether the drift observed between rounds is fully captured by the wiring task's own scope; no execution was possible (Bash denied) to confirm the recovery-loop behavior end-to-end or template-level visibility of numerator.

## Risks, Gaps, And Verification Needs

1. Highest risk: RC freeze while the B/C wiring tree is still moving — round 1 vs round 2 diff shows material semantic changes (`fact_version_by_ref`, C evidence-snapshot checks) mid-conference. Recommendation: bind acceptance to a frozen revision (RC worktree commit) and re-run this review's surviving findings (P2-1…P2-4) against that revision; document the file hashes used.
2. P2-1 terminal-insufficiency leg: needs an end-to-end blocked→recovery→still-blocked fixture to establish current behavior and the absence of blockers files (execution required).
3. P2-2 numerator scenario: needs a fixture with two same-value endpoints, one numerator-reported and one percent-only, to confirm display contamination (execution + browser check).
4. Re-verify round-1-anchored evaluator/models/snapshot claims at RC revision.
5. Confirm which of the `rendered_unreviewed` data-package branches are reachable from the release entry point and whether acceptance can bless them (P2-4).

## Recommended Next Step

Codex should (1) freeze the review target revision before further acceptance work; (2) dispatch one focused execution pass (tests only, no source changes) reproducing: blocked-B/C → `recovery_required` + work item, blocked-after-recovery → current terminal behavior, and the P2-2 numerator fixture, binding findings to the frozen revision; (3) answer Q1/Q4/Q5/Q6 below, which determine whether the remaining items are defects to repair or documented dispositions.

**Bounded questions for Codex:**
- Q1 (P2-1): Is the terminal evidence-blocked leg (blockers audit + concise insufficiency page + terminal decision) for B/C fresh packages owned by the deterministic executor (not yet implemented) or by the host outside it (undocumented)? What artifact/receipt binds it for RC?
- Q2 (P2-4): Which run paths (fresh package vs report-data vs multi-report) are in the RC acceptance surface, and can visual acceptance (B-only today) bless a manifest whose evidence_snapshot_id is synthetic?
- Q3 (P2-2): Acceptable to key the renderer's legacy lookup by row_id/source_row_id (rows are already set-equal), or should the numerator backfill be removed?
- Q4 (P3-1): What evidence of reviewer independence is acceptable at RC beyond `reviewer_id != producer_id` on a package-asserted string?
- Q5 (P2-3): Is C endpoint/timepoint completeness intentionally trial-level family presence, or is a per-endpoint timepoint-pairing contract required?
- Q6 (P3-4): Is `b_effect_difference_support` intended to block on per-arm-only comparative evidence, or should §8.1 "确定性复算" have a documented recompute path?
Safe provisional path if answers are pending: treat Q1 and Q2 as explicit RC decision items (no behavior change until decided), and schedule Q3's fix (row-id keying) with the P2-2 fixture as a contained repair.
