# Task 6.10 视觉定向复核（B 类 PNH · v7）

审阅身份：独立视觉审阅者（非最终验收方，不替代 Codex）。  
候选：`output/acceptance/task-6.10/b-pnh-current-v7/reports/B/v-fixture-b-pnh-001/html/`  
视口：真实 Chromium **1024×900**。只核对上一轮三项 P1，并扫横滚、英文内部状态、明显回退。  
未改仓库、未读其他审阅、未写 runner 文件。  
口径：82.3% 为来源披露的模型估计；51/60 为同时披露的计数，不复算、不改为 62。

---

## 1）浏览器证据

| 项 | 记录 |
| --- | --- |
| 服务 | 本地 `http://127.0.0.1:57911/`（hub `pnh-html-v7`，审后已停） |
| 路径 | 首页 → 点图中 82.3 → 抽屉 → 展开筛选/更多筛选 → 基线总览 → 试验完成情况总览 |
| 视口 | `innerWidth×innerHeight = 1024×900` |
| 横滚 | 首页 / 基线 / 完成情况 `scrollWidth − clientWidth = 0` |
| 英文内部状态 | 上述页未再扫到 `comparable` / `sample_size` / `mean` / `proportion`；矩阵口径在首页可见「可比较」 |
| 图点击 | 柱顶 82.3 上有透明按钮 `.kz-chart-evidence-hit`，**32×32**（约 339, 643），`aria-label="查看数值 82.3 的数据依据"`；点 SVG「82.3」中心，`elementFromPoint` 命中该按钮 |
| 抽屉 | 值 **82.3**、分子 **51**、分母 **60**；原文定位「章节：Efficacy results；表 Table 2；列：血红蛋白较基线持续升高≥2 g/dL且无需输血」；无 `nct04558918/efficacy/apply-treatment` |
| 默认筛选 | 组：**靶点/机制、试验、组别、时间点**。组别芯片：治疗组 / 对照组 / 试验内治疗组与对照组 / **全研究人群**。无「组别未列示」「不适用」，无「终点/事件/字段」 |
| 更多筛选 | **观察指标**（血红蛋白应答率、TEAE、基线样本量、已筛选等）；另有字段族 / 分母角色 / 计量对象 |
| 基线表组别 | **治疗组 / 对照组**；正文无「组别未列示」；轴文案含「基线样本量 / 治疗组」「年龄 / 对照组」等 |
| 完成情况表组别 | **全研究人群 / 治疗组 / 对照组**；正文无「组别未列示」 |
| 百分比轴 | 疗效柱图 Y 刻度仍为 **0–100** |
| 热图 | 首页格内有「不适用」；对应格为底色 `#F4F1EC` + `url(#…-p0)` 斜线，未退回空白格 |

---

## 2）三项 P1 复现结果

### P1-1 点图中 82.3 → 抽屉 51/60（**已修复**）
- **复现**：首页 APPLY 柱图，点 SVG「82.3」中心。  
- **观察**：命中层为 `.kz-chart-evidence-hit`（32×32，大于合同 24×24）。抽屉打开：分子 51、分母 60、值 82.3。SVG 字本身仍约 29×20，医学经理点数字区域能进依据。  
- **分类**：不再作 P1。

### P1-2 基线/完成情况组别身份（**已修复，有 P2 遗留**）
- **复现**：`baseline-overview.html`、`disposition-overview.html`。  
- **观察**：表组别不再写「组别未列示」。基线轴可见「基线样本量 / 治疗组」「年龄 / 对照组」等。完成情况轴有「已筛选 / 全研究人群」「已随机 / 治疗组」等。  
- **遗留（不升 P1）**：完成情况柱挤、一簇 62/62/62/60/60/59/59 只摊到少量轴标签；部分标签下无柱。APPLY「已随机 / 对照组」为 **62**，同页基线对照样本量仍为 **35**（不按本轮口径改数或复算）。

### P1-3 默认筛选精简（**已修复**）
- **复现**：首页展开「筛选条件」。  
- **观察**：默认只有四组；混合观察指标在「更多筛选 → 观察指标」。组别无「组别未列示 / 不适用」。  
- **未升 P1**：默认时间点仍有「时间点未列示」；「更多筛选」仍有字段族、分母角色等后台词。

---

## 3）缺陷清单（仅本轮）

### 已修复（相对 v6 三项 P1）
| 原项 | 结论 |
| --- | --- |
| 图 82.3 点不开抽屉 | 32×32 命中层，抽屉 51/60 |
| 组别未列示 | 已知臂改为治疗组 / 对照 / 全研究人群 |
| 默认筛选过满 | 默认四维；观察指标进更多筛选 |
| 英文 enum 上屏 | 本轮定向页未再现 |
| 整页横滚 | 1024 未测到 |

### 仍存在（非本轮三项 P1，不升 P0）
- **P2**：完成情况图过密、柱与轴不对齐。  
- **P2**：默认层仍有「时间点未列示」；更多筛选仍有「字段族 / 分母角色」。  
- **P2**：抽屉仍占「量表 / 阈值：来源未列示」「原文未提供」。  
- **P2**：1024 主导航仍进「菜单」。  
- **无法在本轮当 P1 关闭**：APPLY 对照已随机 62 vs 基线对照 35，需科学/数据轨另核。

### 无法验证
- 1280 / 1440 全站回归（本轮按指令只做 1024 定向）。  
- `prefers-reduced-motion`。  
- 产品 / 试验档案返回路径（不在三项 P1 内）。

---

## 4）视觉与中文

1024 点柱顶数字能进依据，数字与表一致。筛选默认层像临床筛选，不再像整页维度浏览器。基线 / 完成情况不再用「组别未列示」顶替已知臂。破坏感剩下更多筛选里的后台词、完成情况图过挤、抽屉空字段。未见新的英文内部状态，也未见疗效轴缩回 80 或热图「不适用」变回空白格。

---

## 5）P0/P1 结论

**本轮定向三项 P1 均可复现为已修复。未发现新的 P0。**  
仍有 P2 与一项数据疑点，**不替代 Codex 最终验收**。就「这三项是否还挡住候选」：**不再因这三项视觉 P1 挡住**。

---

## 6）最小下一步

若 Codex 还要收口观感，优先：完成情况图拆组或只标有柱的轴；默认时间点去掉「时间点未列示」；核 APPLY 对照已随机 62 是否应为 35。不必再为 82.3 点击或默认筛选四维做同一套修补。

本轮是只读定向复核，没有新的实现工作。若需要把剩余 P2 收成 Trellis 任务，可以说一声；否则本会话不再开任务。
