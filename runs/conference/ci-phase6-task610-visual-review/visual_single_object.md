# Conference Participant Output: ci-phase6-task610-visual-review - visual_single_object

## Boundary Check

- 工作范围限定在 runner 绑定的工作目录 `.`；只读操作 + 本地临时 HTTP 服务（`python3 -m http.server 8931`，检查结束后已停止），未修改候选门户、fixture、源代码或任何生产路径；未写 runner 管理的报告路径。
- 检查对象：`output/acceptance/task-6.10/b-pnh-current-v7/reports/B/v-fixture-b-pnh-001/html/`（v7，不可变评审目标）；对照真源 `fixtures/positive/b-pnh/inputs/report-data.json`、`contracts/kangzhe/design_specs/{core,project_profile,track_interactive}.md`。
- 未阅读其他参与者输出（`runs/conference/.../medical_manager_*`、既有 `visual_single_object.md` 均未打开）；仅按 Source Of Truth 阅读了 `worker_03.md`（v3 执行史）用于确认修复点，全部结论基于本次对 v7 的独立实浏览器复核。
- 未做最终视觉/科学放行；以下为 Codex 的独立验收证据与异议。
- 工具：真实 headless Chromium（xd://browser），视口 1024×800 / 1280×900 / 1440×900；fixture 用 Python 只读解析。

## Independent Work Product

### 复核方法（独立全流程）

1. 24 个物理页面 × 3 个视口宽度的几何扫描（`scrollWidth − clientWidth`、最右元素越界）。
2. 全部页面可见文本（TreeWalker 排除 script/style/template）扫描后端键与内部状态正则（`factor-b`、`*-cohort`、`not_yet_disclosed`、`disclosure_state`、`row_id`、`24week` 等约 30 个模式）。
3. 筛选面板全量标签抽取；筛选→URL→刷新恢复→数据依据抽屉→`focus` 恢复→关闭全链路复测。
4. 数值三向一致性抽查：图（82.3/1.8）↔ 表（51/60、0/35 + 口径提示）↔ 抽屉（值 82.3、分子 51、分母 60）↔ fixture（`eff-apply-treatment`）；热图 54.8% ↔ fixture 34/62；矩阵 x 值 80.5 = 82.3−1.8 复算；全 fixture 疗效/安全行 `value` vs `100·n/d` 复算。
5. 视觉截图人工判读：首页首屏、安全性热图、矩阵气泡图、受试者流转、人口学（1024）。
6. 字号直方图与 <16px 元素定位、`data-density`/`data-detail-ref` 例外标记核查、`prefers-reduced-motion` 规则计数、指针目标 ≥24px 抽查、1024 折叠菜单与分组展开交互。

### 阳性证据（v7 已达标的合同项）

- **零横向溢出**：24/24 页面在 1024、1280、1440 下 `scrollWidth == clientWidth`，最右元素 = `site-header` 边界内。满足 project_profile §10 默认视野底线。
- **中文原生性**：全部 24 页可见文本 0 处后端键/内部状态泄漏；筛选器全中文（"因子B""第24周""筛选至第24周""已报告值/已报告零值/不适用/未公开"）。v3 的 `factor-b`/`apply-cohort` 泄漏已修复。
- **数值一致性**：抽查的图—表—抽屉—fixture 四向一致；疗效 82.3%/1.8% 与原始计数 51/60、0/35 的表观矛盾已由 v7 新增"口径提示"列显式解释（"比较值为模型估计比例；分子与分母为原始应答计数，不用于直接复算"）——这是我本次复核中最重要的先前风险点，现已闭环。
- **缺失状态语义区分**：已报告值/已报告零值（热图 0.0 单元格印数值）/不适用（APPOINT-PNH 单臂对照列，热图用斜纹+文字双编码，非纯颜色）/未公开（不可点击，fail-closed）/未列示/来源未列示/原文未提供 互相区分。
- **热图与气泡图合同项**：热图单元格直接印数值（54.8/60/不适用）+ 图例 + 纹理；气泡图轴标题为中文临床变量（"试验内疗效差（百分点）""治疗组治疗期间不良事件发生率（%）"），"气泡大小：治疗组样本量"就近说明，APPOINT-PNH 无法成坐标的原因同页列示。
- **交互链路**：`?trial=nct04558918` 刷新后筛选恢复（可见行 2/3）；点值开抽屉→URL 追加 `focus=`→刷新抽屉自恢复→关闭后 `focus` 移除且表格焦点行保留；1024 折叠"菜单"展开 4 组 21 链，分组可展开（疗效与安全性→疗效/纵向结果/安全性）。
- **可及性**：`prefers-reduced-motion` 规则存在（3 条）；抽查 40 个筛选 chip/按钮全部 ≥24×24px。
- **首屏**：仅报告名+适应症+观察截止，无报告类型解释/工作流文案，图在表前。

### 发现（按严重度）

**P1-V1：基线图表把不可比单位画在同一数值轴上（图表语义错误）**
- 路由：`baseline-demographics.html`、`baseline-severity.html`、`baseline-overview.html`（同一"多项可比指标｜基线变量与披露状态"组件）；视口：1024 复现，与宽度无关。
- 证据：图表 y 轴标签为"人"，但同一组柱混合了 62**人**（样本量）、51.7**岁**（年龄）、69.4**%**（性别）、8.9 **g/dL**（基线血红蛋白）——fixture `baseline_views.facts` 单位逐行核实。柱高跨单位比较在医学上无意义，且"人"轴标签对岁/%/g/dL 行直接错误。
- 最低修复：按单位拆小多图（§9 明确允许"拆图/小多图"），或改为纯披露状态分面图（不编码量值）；轴单位标签随分面更正。

**P1-V2：14px 字号突破站点轨 §0.7 地板且未满足例外条件**
- 路由：全部页面（在 disposition-overview 实测定位）；视口：全部。
- 证据：页眉元信息"适应症：…""观察截止：…"与页脚行 = 14px（非表体、非轴标签，无例外通道）；43 个 SVG 轴标签 = 14px，但全页 `data-density` 元素 = 0、`data-detail-ref` = 0——§0.7/§16.A 要求 `data-density=ultra` 且满足 §13.3 全部条件才允许轴标签 ≥14px。core.md I-03 规定 site_html 下限 16px 为 MUST。
- 最低修复：元信息/页脚升至 16px；轴标签升至 16px，或为图表容器补 `data-density="ultra"` + 真实 `data-detail-ref` 并逐项满足 §13.3（行数、行高、对比度、人工可读）。前者更稳妥。

**P2-V1：受试者流转/处置族图表数值标签互相重叠，直接不可读**
- 路由：`participant-flow.html`（截图实证："6262626260605959"、"404038837" 粘连）、`disposition-overview.html`、`loss-exit.html`、`screen-failure.html`、`adherence.html`（同组件，各 22 处文本边界盒重叠）；视口：1024 实测，组件级缺陷预计全宽度复现。
- 最低修复：相邻同值标签去重/错位/仅显示系列代表值，或改表格为主、图为辅；复测标准 = 原分辨率下每个数值单独可读。

**P2-V2："未公开/未列示"缺就近图例说明，阻断三问未答全**
- 证据：disposition 页整行"未公开｜未列示｜未列示｜未公开｜未列示｜未公开"，无 title、无页面级注释（notes 扫描为空）。合同 §2 要求阻断信息回答"缺了什么、系统已尝试什么、用户可提供什么"；当前只答了第一问，且"未公开"与"未列示"两个标签的区别对非技术读者不自明。
- 最低修复：表格就近加一行中文图例（未公开=登记处/文献均未披露；未列示=来源记录中无此字段；未公开行不可点击因无数据依据），并在页脚或"研究依据与局限"页说明已检索的来源范围。

## Evidence And Assumptions

- 证据（实测）：上述所有几何、文本、字号、交互数据均为本次 xd://browser 实时求值；截图 4 张位于系统临时目录（会话结束后可能不可复用，已在本报告内嵌描述）。
- 证据（静态）：fixture 复算 — 疗效 3/3 行 `value ≠ 100·n/d`（82.3 vs 85.0；1.8 vs 0.0；92.2 vs 93.9），安全 12/12 行一致；渲染 HTML 中 snake_case 键存在于 data 属性/JSON 内嵌（4424 处 `not_yet_disclosed` 等），但可见文本 0 泄漏——该区分已由 TreeWalker 扫描确认。
- 推断 [INFERENCE]：P2-V1 在 1280/1440 同样存在（组件级布局，未逐宽度截图）；P1-V1 三页共用同一图表组件，修一处应同修三处。
- 假设：fixture 为数值真源（模型估计值 vs 原始计数的并存是有意设计，v7 口径提示列已佐证）；未做 WebKit 复核（合同 §8 要求 Chromium+WebKit 双内核，本轮仅 Chromium）。

## Risks, Gaps, And Verification Needs

- **未覆盖**：WebKit 逐页检查（合同硬性要求，需 Codex 或独立验证者补做）；200% 缩放功能保持、键盘焦点可见性仅抽查未全量；未逐页核对全部 87 条记录的图-表-抽屉一致性（抽查 4 条链路）。
- **风险**：P1-V1 若医学经理按柱高比较"69.4% > 62人"类结论，构成实质性误导，优先级高于纯排版问题；P1-V2 是合同 MUST 项，不修复则验收标准"字号地板"无法勾销。
- **对 Codex 的有界问题**（不阻断结论）：(1) P1-V2 的修复取向——全站升 16px，还是为轴标签引入 `data-density=ultra` 例外通道？前者改动小、语义干净，建议前者。(2) "未公开 vs 未列示"图例文案由谁审定（科学措辞边界属 Codex 终审）。
- **挑战既有假设**：此前轮次把"无横向溢出"作为主要几何判据；本轮 P2-V1 证明组件内部标签碰撞可在零页面溢出下发生，验收清单应加入"图表内文本边界盒两两不重叠"的机器检查，而不仅是页面级 scrollWidth。

## Recommended Next Step

1. Codex 裁定 P1-V1（拆小多图/改披露状态图）与 P1-V2（升 16px vs 引入 ultra 例外）的修复路线，进入 v8 定向修复。
2. 修复后复测清单：三基线页图表单位分面截图、全站 <16px 可见文本计数归零或全部落入合法例外、五页流转族图表标签重叠计数归零、补 WebKit 一轮。
3. P2-V2 图例文案随 v8 一并修复；修复成本低、医学经理困惑收益高。
4. 两项 P1 关闭前，我的独立意见是**不予视觉放行**；其余合同项（溢出、中文原生、数值一致、缺失语义区分、交互恢复、热图/气泡图底线）本轮均通过，修复面收敛、无架构性返工。
