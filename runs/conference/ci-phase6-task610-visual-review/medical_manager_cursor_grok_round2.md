只读复核 v6 最终候选：先确认产物路径，再用真浏览器按三个宽度定向核对首轮问题。# Task 6.10 视觉定向复核（B 类 PNH · v6）

审阅身份：独立视觉审阅者（非最终验收方，不替代 Codex）。  
候选：`output/acceptance/task-6.10/b-pnh-current-v6/reports/B/v-fixture-b-pnh-001/html/`  
视口：真实 Chromium **1024×900、1280×900、1440×900**。未改仓库、未读其他审阅、不宣称 Codex 放行。  
口径：82.3% 为来源披露的模型估计值；51/60 为同时披露的计数，不据此复算、不改为 62。

---

## 1）浏览器证据

| 项 | 记录 |
| --- | --- |
| 服务 | 本地 `http://127.0.0.1:57901/` |
| 定向路径 | 首页 → 筛选/更多筛选 → 表内 82.3 开抽屉 → 疗效轴 → 安全性热图 → 矩阵 → 基线/完成情况轴 → 1280/1440 对照 |
| 1024 | 顶栏「菜单」；疗效柱图 SVG 约 863 宽、Y 轴 **0–100**；表 `tr` 为 **grid 卡片**（`td: block`）；无整页横滚 |
| 1280 | 主导航展开；表回到 `table-row`，列宽约 84–165px，行高约 123px；`APPLY-`/`PNH` 换行 |
| 1440 | 图表约 1254 宽；Y 轴 0–100；口径列约 292px，行高约 70px；无 `comparable`/`sample_size` |
| 抽屉 | 值 **82.3**、分子 **51**、分母 **60**；原文定位为「章节：Efficacy results；表 Table 2；列：…」；**无** `nct04558918/efficacy/apply-treatment` |
| 热图 | 格内 54.8 / 60 / 60；APPOINT 对照格 **「不适用」+ 浅底斜线**；色条 **0%–60%** |
| 矩阵 | APPLY：疗效位置 **80.5**、安全性 **54.8**、比较状态 **可比较**、披露 **已报告值**；APPOINT：组别/比较/披露均为 **不适用**，页上有单臂说明 |
| 中文枚举 | 表内统计形式为 **样本量 / 均值 / 比例**；矩阵为 **可比较** |

---

## 2）端到端（定向项）结果

所列定向项多数已落地：抽屉与表数字一致、内部路径已藏、1024 卡片可读、热图不适用有中性编码、矩阵口径与单臂一致、百分比轴到 100、低频维进「更多筛选」。

仍不能说「医学经理完全不用停下来猜」：**默认筛选仍挤**；**基线表组别仍是「组别未列示」**、轴标签不齐；**图上 82.3 仍点不进抽屉**（本次用 SVG 文本点击，高约 20px）。

---

## 3）首轮缺陷对照

### 已修复

| 原号 | 项 | 证据 |
| --- | --- | --- |
| D2 | 抽屉分子/路径 | 分子 51、分母 60、值 82.3；无存储路径；「规范化说明」改为「数据说明」；「固定此条」改为「加入对照」 |
| D3 | 英文内部状态 | 矩阵「可比较」；基线「样本量/均值/比例」；三宽度均未再扫到 `comparable`/`sample_size`/`mean`/`proportion` |
| D4（1024） | 表可读 | 1024 为卡片：横排「产品 / 试验 / 分子 / 分母」，`APPLY-PNH` 不再竖切 |
| D5 | 热图不适用 | 印「不适用」+ `#F4F1EC` 底 + `url(#…-p0)` 斜线；色条有 0%/最大刻度 |
| D6 | 矩阵口径 | APPLY **已报告值 + 可比较**；APPOINT **不适用** + 「单臂研究没有试验内对照组…」；组别改为「试验内治疗组与对照组」 |
| D10 | 轴顶穿 | 疗效 Y 轴 0–100，82.3 未顶出画布 |
| D7（部分） | 低频维 | 「字段族 / 分母角色 / 计量对象 / 队列」在 **更多筛选**，默认层不再直接铺这些组 |

### 仍存在

**P1-R1 — 默认筛选仍不精简**  
- 路由：各页「筛选条件」；宽度 1024（1280/1440 结构相同）  
- 复现：展开筛选，默认仍见「终点/事件/**字段**」下一排终点、TEAE、基线样本量、年龄、已筛选、筛选失败……；组别里仍有 **组别未列示 / 不适用**。  
- 观察：「更多筛选」存在且装了低频维，但默认层仍像维度浏览器。  
- 分类：inference  
- 最小修补：默认只留产品/试验/终点/时间点/治疗组对照；「组别未列示」进更多或去掉。

**P1-R2 — 基线/完成情况：轴有字段名，表组别仍空**  
- 路由：`baseline-overview.html`、`disposition-overview.html`  
- 宽度：1024  
- 复现：轴上可见「基线样本量 / 治疗组」「年龄 / 治疗组」，**不再印「组别未列示」**；部分柱下无标签（如 8.9、40、8.2）。表仍写组别 **组别未列示**（APPLY 治疗 62 与对照 35 两行都如此）。完成情况轴有「已筛选 / 已随机」等，仍夹大量「治疗组」，表同样「组别未列示」，「已随机」行重复。  
- 分类：evidence  
- 最小修补：表组别补治疗组/对照；轴一律「字段+组别」且每柱有标签；去重。

**P1-R3 — 图上 82.3 仍点不开抽屉（首轮 D1）**  
- 路由：首页/疗效  
- 宽度：1024（标签约 29×20）  
- 复现：点 SVG 文本 82.3，抽屉 `display:none`、内容空。点表卡片 82.3 才打开并填 51/60。  
- 分类：evidence  
- 最小修补：柱体/标签命中 ≥24×24，与表同一套 `openByRowId`。

**P2 遗留（不升 P0/P1）**  
- 1280 仍是 12 列表，`APPLY-`/`PNH`、口径提示多行折；1440 明显好转。定向只要求 1024 卡片。  
- 热图色条黄橙渐变 vs 格子深红。  
- 矩阵 APPLY 口径提示仍为「未列示」（披露已是已报告值）。  
- 抽屉仍占「量表/阈值：来源未列示」「原文未提供」。  
- 1024 主导航仍进汉堡。  
- 未测 `prefers-reduced-motion`。

### 无法验证 / 未作为本轮阻断

- 产品/试验档案返回：本轮未再点档案页（不在本次定向清单）。  
- 真实鼠标对 ECharts 柱体（只测了 SVG `text` 点击）。  
- 82.3 与 51/60 的统计关系：按任务要求不复算。

---

## 4）视觉与中文

- 1024 卡片：标签横排，51 / 60 / 82.3 一眼能对上。  
- 中文枚举已进表。破坏点剩下默认筛选里的「字段」「组别未列示」，以及抽屉空字段。  
- 热图「不适用」不再靠「看不见」表达。  
- 品牌浅顶栏+橙识别仍在。

---

## 5）P0/P1 结论

**本轮未再复现原 P0（抽屉分子对不上、英文 enum 上屏）。**  
**仍有 P1：默认筛选过满、基线表组别空、图上数值点不开抽屉。**  
不替代 Codex 最终验收。  
就「定向清单是否做完」：清单核心（51/60、中文、1024 卡片、热图不适用、矩阵已报告/不适用、轴到 100、更多筛选）**基本已做**；筛选精简与基线组别未闭环。

---

## 6）最小下一步

1. 默认筛选只留临床五维；「组别未列示」移出默认。  
2. 基线/完成情况表组别写成治疗组/对照，轴每柱「字段+组别」。  
3. 柱顶 82.3 与表同一点击开抽屉（命中 ≥24×24）。  

再在 1024 走一遍：「点柱顶 82.3 → 抽屉 51/60」和「基线表不再出现组别未列示」。
