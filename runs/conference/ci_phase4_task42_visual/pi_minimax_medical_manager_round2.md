All deltas verified. Let me compose the round-2 review.

---

# Task 4.2 视觉与浏览器复审 · 资深医学经理视角（第二轮）

## 一、证据

**视觉（六张当前截图，01:44 重建）**
- `screenshots/overview-1280.png`、`overview-1280-menu.png`
- `screenshots/overview-1440.png`
- `screenshots/overview-1920.png`
- `screenshots/overview-1024.png`、`overview-1024-menu.png`

**实时浏览器**：Camoufox 1728×947，静态服务器 `http://127.0.0.1:8766/`；1024 用 `html,body{max-width:1024px}` 强制宽度复现。

**工具限制**：Camofox 不开放 viewport 设置；1024 折叠态通过 CSS 强制宽度复现，与浏览器原生媒体查询等效。

## 二、本轮变更逐条核验

| 项 | 旧 | 新 | 状态 |
|---|---|---|---|
| 首页 kicker | `<p class="portal-page-kicker">总览</p>` | 节点删除 | ✅ 删除（`document.querySelector('.portal-page-kicker')` 返回 `null`） |
| 首页 H1 | `首页` | `首页` | ✅ 保留，无冗余 |
| 首页 lead | `本页给出特应性皮炎试验比较总览，便于进入疗效、安全性与人群结构。` | `集中呈现特应性皮炎试验的疗效、安全性与人群结构。` | ✅ 改为简短临床性 lead，去掉"便于进入……"和"总览"重述 |
| 节标题 | `本页阅读路径` | `重点模块` | ✅ 全文替换 |
| 卡片 03 | `疗效与安全性位置` | `疗效与安全性矩阵` | ✅ 修正为指向真实矩阵页 `efficacy-safety-matrix.html` |
| 搜索占位 | `搜索页面…` | `搜索页面或关键词…` | ✅ 扩大语义提示 |
| 搜索数据 | 7 关键词 | 仍 21 条；首页条目 keywords 现含 `疗效与安全性矩阵` | ✅ 一致 |
| 顶栏 active | 仅组级触发器高亮 | `纵向结果` 页：组触发器 `aria-current=page`；子链接 `纵向结果` 同时具有 `class="site-nav-group__link--active"` 和 `aria-current="page"` | ✅ 实测，HTML 内联生成（`b-portal/longitudinal-results.html` 等页直接生成） |
| 页脚 | `供产品中心-医学部内部研判使用。` | `仅供产品中心医学部内部研判使用。` | ✅ 中文-原生、符合临床经理语境 |
| 卡片交互 | 无链接、`tabIndex=-1`、`cursor:auto` | 同上 | ✅ 不暗示点击 |

## 三、视觉证据要点

**1280 / 1440 / 1920（截图）**：Logo+站点名+4 组导航+搜索框同排；橙色品牌细线在顶栏底部；主区"首页"+临床 lead+1×3 卡片；卡片下方留白至页脚"仅供产品中心医学部内部研判使用。"。三档宽度下顶栏无重叠、无串行、无裁切。

**1280 菜单展开（截图）**：dropdown 面板收窄到仅包含"疗效 / 纵向结果 / 安全性"3 个子项，最右边界不再延伸到正文 lead 之下（与上一轮相比，面板宽度从 220px 缩到 ~140px）。在 1728 视口实测，面板仍与下方 lead 文字在 y≈188–205 矩形相交；按本轮裁定范围（"popup 覆盖正文是普通下拉行为"），不再计入 P1。

**1024 折叠（截图）**：仅 Logo+站点名 + `菜单` 按钮；导航/搜索全部隐藏到折叠区。点开 `菜单`：纵向 1 列 — `总览`（橙底高亮）+ 4 个组触发器（`疗效与安全性` / `基线与人群` / `试验完成情况` / `试验与证据`），再下方为搜索框，再下为主内容。导航与搜索同时展开。

## 四、医学经理端到端路径复测

| 步 | 动作 | 结果 |
|---|---|---|
| 1 | 进入 overview | kicker 节点不存在；H1 `首页`；lead `集中呈现……`；active 链接 = `总览` |
| 2 | 点 `疗效与安全性` 触发器 | aria-expanded=true，面板显示 3 子项 |
| 3 | 点 `纵向结果` | 跳到 `longitudinal-results.html`，kicker `疗效与安全性`、H1 `纵向结果`、active link `纵向结果` 同时具有 `--active` 类与 `aria-current=page`；触发器 `aria-expanded=false` 自动收起 |
| 4 | 搜索 `安全性` | 5 条命中，第一项即 `安全性` |
| 5 | 点结果 `安全性` | 跳到 `safety.html`，active link = `安全性`（`aria-current=page`），其他子链接 `aria-current="false"` |
| 6 | 在 `safety.html` 用 Logo 回首页 | 成功跳回 overview |
| 7 | 1024 强制宽度：开/关菜单 | `菜单` 按钮 toggle `aria-expanded`；折叠菜单纵向铺开，组触发器对齐左侧 |

**直观之处**：标题层级只剩 H1+lead，简洁；drop down 的子项 active 状态清晰可见（橙色左边线 + 文字色加深），不再需要靠 H1 反推当前页；搜索占位提示更明确"页面或关键词"。

**残留困惑**：首页 3 张"重点模块"卡仍是编号+标题、不含摘要和锚点；按本轮范围不计入 P1（属 Task 4.5 抽屉/锚点、4.4 完整表的内容任务）。

## 五、判定

### P0（阻塞）：0

### 范围内 P1（接受前必修）：0

- 所有 PRD § 0.3 / § 14.12.10 验收点的硬性要求已闭环：标题层级、分组导航、当前态（组级 + 子链接）、搜索、3+ 互通物理页、Logo 资产、深浅底配色、reduced-motion、native 中文、Console 无 error、popup 行为判定内。
- 不再要求：图、表、抽屉、筛选、URL 状态、Hero 数据预览（这些是 Task 4.3–4.5）。

### 范围外 / 后续任务：P2（记录但不阻断）

- 首页"重点模块"3 张卡仍是占位（无摘要、无锚点、无跳转）。属于 Task 4.4（图表/完整表）和 Task 4.5（证据抽屉/锚点）落地后由真实数据填充的占位，本轮范围不视作 P1。
- 1280 / 1440 / 1920 主页下半屏留白较大；同 Task 4.4 / 4.5 自然填满。
- popup 与正文矩形相交在 ≥1280 视口下仍存在（1728 实测 overlap=true），按本轮裁定属普通下拉覆盖行为，不再计入 P1。

### 裁决

**PASS** — P0=0，范围内 P1=0。Task 4.2 共用报告壳层可接受进入 Task 4.3。

## 六、残余限制

- Camofox 视口固定 1728×947，1024 折叠态通过 CSS 强制宽度复现；不是真实 1024 设备像素测量。
- 仅抽样验证 3 页（overview、longitudinal-results、safety）+ 强制 1024 折叠；其余 18 个物理页未逐一打开浏览器，但其 HTML 模板与 overview/longitudinal/safety 一致（已抽样确认节标题、页脚、kicker 命名一致）。
- 本评审严格只覆盖 Task 4.2 壳层（branding、标题层级、分组导航、搜索、页面互通、空壳节奏、中文-原生、视觉清晰度）。
