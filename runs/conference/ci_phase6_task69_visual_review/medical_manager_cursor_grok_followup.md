浏览器复核结果已齐，我先把四个宽度上的 D1–D5 测量读全，再给出只读状态报告。# Task 6.9 修订复核（仅 D1–D5）

只读视觉审阅，未改文件，未读其他审阅者报告，**不代替 Codex 终验**。  
浏览器：本机 Chromium，`http://127.0.0.1:8769/`，视口 1440×900 / 1280×900 / 1024×900 / 768×900。

---

## D1 1280 一级导航 — **关闭（原 P1）**

1280 已露出一级导航，不必点「菜单」。

| 宽度 | `#site-primary-nav` | 「菜单」 |
| --- | --- | --- |
| 1440 | `flex` | `none` |
| 1280 | `flex` | `none` |
| 1024 | `none` | `flex` |
| 768 | `none` | `flex` |

1280 点「疗效与安全性」后，`#nav-group-2` 为 `display:flex`，链接：疗效 / 纵向结果 / 安全性。  
1024/768 走汉堡，不构成原 D1。

---

## D2 产品/试验/终点搜索 — **关闭（原 P1）**

首页搜索已能命中产品、试验、终点。

- 「澄明」：澄明-3试验档案、EASI-75 / TEAE / SAE 等（7 条）。
- 「泰瑞奇」：泰瑞奇单抗产品档案、相关终点、澄明-3试验档案（8 条）。
- 「EASI」：四家产品的 EASI-75 应答率。

**残留（P3）：** 同一终点治疗组/对照各一条，列表成对重复。不影响「搜得到」。

---

## D3 数据依据入口与内部字段 — **关闭（原 P1）**

入口可见；抽屉不再泄漏 fixture id；表与抽屉披露用词一致。

- 图区提示：「点击数值查看数据依据。」（14px）。
- 点表内 `68.4` 后 `#kz-evidence-drawer` 为 `block`。
- `hasFixture: false`（无 `ctgov-fixture` / `eff-fixture`）。
- 原文定位：`来源类型：登记记录；章节：登记结果`。
- 表「披露状态」与抽屉均为「已报告值」。

本次快照里抽屉还出现「来源未列示」字段；这不是 fixture 泄漏，也不把原 P1 重新打开。

---

## D4 无图页不再虚假承诺图形 — **关闭（原 P1）**

矩阵/基线/处置缺真实坐标时 fail-closed。四页 `svgCount: 0`，无「先看图形」、无「气泡面积」。**未要求造数据。**

| 页 | 页头 | 区块 |
| --- | --- | --- |
| 疗效与安全性矩阵 | 暂无可绘制的真实疗效—安全性覆盖值，完整比较状态见表。 | 优先查看完整数据表 |
| 基线与人群总览 | 暂无公开基线记录…见表。 | 同上 |
| 试验完成情况总览 | 暂无公开试验完成情况记录…见表。 | 同上 |
| 研究依据与局限 | 来源成熟度导语 | 同上 |

---

## D5 安全性首屏四产品行 — **关闭（原 P1）**

四个宽度下，四家产品行与数值都在 900px 视口内。TEAE：0 / 70.5 / 74.8 / 69.3。筛选芯片 16px，图例 14px。`allFourInFold: true`。

**残留（P2）：** 比较色条仍可能贴底被切。`svgBottom`：1440 = 906（视口 900）；1280 = 900；1024 = 896；768 = 962。四行数字仍可见，768 色条切得更明显。

---

## 剩余 P0 / P1

就 **D1–D5 原阻断项**：当前 **未见 P0，也未见仍成立的 P1**。  
1024/768 汉堡、搜索重复行、依据抽屉压表、安全性色条被切，均不回到原 P1 口径。

本结论只覆盖这五项修订复核，**不是 Task 6.9 完成或 Codex 终验**。

---

## 建议的最小下一步

D1–D5 不必再作为修订主线。若继续打磨（非阻断）：

1. 安全性首图把比较色条收进视口（尤其 768）。
2. 数据依据打开时主表让宽或表内横滚，避免压列。
3. 搜索对同一终点的治疗组/对照合并为一条。

不需要为此再造矩阵/基线/处置图形数据。

---

这轮是只读复核，没有改代码。若需要把后续打磨做成 Trellis 任务，说一声即可；否则本会话不建任务。
