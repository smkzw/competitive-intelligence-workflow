这是对已有 Task 6.4 的最终只读复核，不新建 Trellis 任务。先对照上轮 P1 读合同与测试，再跑官方用例和两个定向反例。合同层已把效应形式写进行标识，切安全性家族时也不再继承 TEAE 窗。接下来跑官方测试和两个定向反例。# Task 6.4 医学经理最终复核（上轮两条 P1）

**结论：有条件通过（PASS）**

上轮两条会把立项会卡住的 P1，合同层已经关掉：跨效应口径不再撞行标识、不再打掉整图；只切安全性家族时不再继承 TEAE 的事件和时间窗，唯一 SAE 能落到 3%。样本量仍可能用安全性分析集例数。当前没有可渲染页面，**视觉验收未发生**。

本轮未新建 Trellis 任务，未改文件。

---

## 复核范围

- 对照：`runs/tests/task64/cursor_grok_medical_manager_followup.md`
- 只读：`src/ci_workflow/reports/b/pages.py`、`tests/reports/b/test_bubble_area.py`、`tests/reports/b/test_matrix_states.py`
- 官方测试：`test_bubble_area.py` + `test_matrix_states.py`，**28 passed**（上次 26）
- 定向反例：风险差对比值比分桶；只改 `safety_family` 切到唯一 SAE

---

## 上轮 P1：已关闭

### 1. 跨 `effect_measure` 不再撞标识、不造差、不崩图

比较行身份现在含 `effect_measure`（未标明则为 `not-specified`）。治疗组风险差、对照比值比时：

- 两行、两个不同 `comparison_row_id`
- 各缺一臂，状态「未报告」，横轴均为 `None`
- 0 个气泡，`build_bubble_matrix` 不再因「比较行标识不得重复」失败

立项读图含义：不会把风险差和比值比相减，也不会因为误标效应量把整张图打没。两臂不同口径时本来就不该配对。

官方：`test_effect_measure_buckets_keep_unique_row_ids_and_never_subtract`。

### 2. 只切安全性家族，不再继承 TEAE 窗，能命中唯一 SAE

`_selection_for_build` 在家族变化时不再回填事件、定义、时间窗、分析人群、分母口径。默认 TEAE 为「治疗期间 / teae-any / 治疗期间出现的不良事件」时，只传 `safety_family=sae`：

- 事件、时间窗、定义均为空（不继承）
- 唯一 SAE（研究期间 3%）状态「可比」，纵轴 3%
- 图、完整表、提示、证据同一行标识

把 SAE 窗写成「治疗期间」、与默认 TEAE 窗相同，只切家族同样落到 3%，不再停在「请调整筛选」。

官方：`test_switching_safety_family_drops_previous_family_dependent_filters`。

---

## 剩余问题

**P0：** 无。视觉仍未做，不能用 28 项绿灯代替目视。

**P1**

- 无样本量映射时，气泡 N 仍用安全性分母（反例 87）。疗效常在全分析集，安全性在安全性分析集。差几个例可接受，点上必须写清「安全性分析集例数」，不能让人当成 FAS/ITT。这不是配对错误，但会上会被问把握度。

**P2**

- 对照臂安全性仍可不进纵轴（符合「纵轴=治疗组原始发生率」），完整表仍宜标明「对照本次未用于纵轴」。
- 唯一总体 TEAE 时默认窗取该条事实；多窗仍空着等选。预先指定窗若与唯一总体 TEAE 不是同一条，需锁定快照显式给出。

---

## 视觉未验证

**视觉验收未发生。** 无 HTML、无倒序纵轴是否真的“向上=更低”、无面积是否按 \(\pi r^2\) 渲染、无提示是否压住气泡旁的优劣联想。URL 中文窗仍是百分号编码。未用真实 B 类 CSR 多臂/多窗/SS≠FAS 走一遍。

合同仍禁止综合分和象限标签；一旦上图出现「高效低毒」之类，本次合同结论作废。

---

## 医学经理判断

上轮会在会上翻车的两件事——**效应口径一乱整图消失**、**切 SAE 还带着 TEAE 的窗切不过去**——类型层已经堵住。现在可以当成试验内二维观察草稿：横轴同一试验同一终点桶、同一效应口径的方向校正差；纵轴该治疗组原始发生率；切安全性家族时从该家族自己的事实里选，不借用上一维度的事件和时间窗。

**还不能进竞品会。** 缺一次真页面的倒序轴和面积目视；气泡 N 仍可能是安全性分析集。合同层这两条 P1 可记关闭；放行进会要等视觉过目，并在点上标明 N 的分析集。
