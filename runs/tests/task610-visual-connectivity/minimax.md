连通结果：✅ 出网与无头浏览器均已实测通过（google.com/npm 注册返回 200；Playwright headless Chromium 已成功打开 example.com 并产出 16577 字节截屏）。
实际提供方：本会话本地运行时，未调用任何上游路由。
实际模型：cms-router/MiniMax-M3（会话头声明），未实测其他模型。
可用的真实浏览器或视觉能力：✅ 可用 Python `playwright` 1.60.0 + 本地 Chromium 1228（ms-playwright 缓存）+ `/Applications/Google Chrome.app`，可启动 headless/有头、截屏、PDF、录制 trace；当前已在 `example.com` 拿到真实像素截屏并被图像解码确认。
