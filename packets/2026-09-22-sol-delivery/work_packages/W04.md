# W04 — 实际事实编辑、派生与跨报告原子同步

状态：pending。主执行：gpt-5.6-sol:medium（运行时核实）。

## 输入与边界

依赖：W01/W02/W03公共合同。
需求与验收：FR14/16；V2-06/36/37/39；DATA03/04/06；EDIT01–10。
先读：本包PRD、DESIGN对应段、REVIEW对应发现、本文件；再读以下实际定义/消费者/现有测试，不全量重读历史。
允许实现入口（均相对工程根）：application/correction_service.py、refresh_service.py、latest_delivery.py；graph/impact.py；domain/facts.py；storage/render_transaction.py、event_store.py、sqlite.py；按需本地编辑接口及相关schemas。
不得修改无关用户文件、历史接受记录、旧中文根；新增文件限完成本包所必需。

## 连贯实施步骤

1. 复用CorrectionService事件/版本/冲突，引入用户显式保存typed命令expected_revision/request_id；不新增人工审批，不将旧单报告owner/QC提案当全新用户编辑已实现。

2. 支持n/N、时间/单位/语义字段及C阈值的实际事实新版本。源quote不改；记录用户输入和依据，不让同数字无关事实联动。

2a. 0923V1 presence-aware 补丁：省略字段=不变，显式 `null`=按字段规则清除，非空=设置。清除数值的用户可见状态必须是“用户清除，待重新核实”，原来源值仍可查；当前数值及依赖派生失效，不能伪造为来源未公开，也不能生成 `reported_value + null` 或字面 `None`。合法共享事实须以真实 A+B 消费者保存验证；C 只在确实引用时加入。

3. 最小扩展impact至事实→派生→视图/叙事，检测环和缺边；明确公式重算粗率，已报告调整率不覆盖；旧分面/语义提案与叙事失效。

4. staging重建受影响A/B/C所有图、表、文字、索引、来源指针，检查实际文件而不是回执格式；完成原子切current，失败留旧完整版；撤销追加新版本。

5. 两标签页冲突、幂等重试、崩溃恢复；新修订user_modified未独立复核，旧accepted仅属旧版。按需loopback编辑，Host/Origin/CSRF/会话/路径边界从首次实现就收紧。

6. 与W07定义三方刷新冲突，不静默覆盖用户事实。

## 一次批次验证与完成条件

一批集成测试+一条完整浏览器编辑旅程：n20/N80→24/80粗率25%→30%；C阈值变化；无关同数字不动；重建中断、并发、重试、撤销、恶意接口/导入。全部关联产物revision/hash核对。

出口：不是批注/patch导入，而是实际事实和所有消费者更新；失败无混版。

## 产物与接续

提交代码/合同/必要迁移/集成测试；证据记入本包 evidence/W04-result.md（未来实施产出，本轮不存在），至少含实际源身份、范围、命令/旅程、结果/限制、产物hash、未解决项。新测试文件先在对应现有层落地，不创建第二验收框架。

完成后更新 ../STATUS.md 的当前包和下一安全动作，不逐步骤造检查点。下一步：W05接入编辑；W06分享当前修订，W07刷新。
缺实质产品决策用原生Ask选择题；普通实现自行决定并留理由，不人为阶段暂停。
