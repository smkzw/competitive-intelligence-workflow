# W00 — 当前基线、资源单源与可重复接手

状态：pending。主执行：gpt-5.6-sol:medium（运行时核实）。

## 输入与边界

依赖：无。
需求与验收：FR08/17；F11；V2-01/02/40，BASE01/02。
先读：本包PRD、DESIGN对应段、REVIEW对应发现、本文件；再读以下实际定义/消费者/现有测试，不全量重读历史。
允许实现入口（均相对工程根）：AGENTS.md、README.md、ARCHITECTURE.md（只更新当前入口指针）；package-manifest.json；renderers/portal/builder.py、assets；tools/bundle_contract.py；tests/contract/test_page_catalogs.py、test_bundle_scientific_closure.py。
不得修改无关用户文件、历史接受记录、旧中文根；新增文件限完成本包所必需。

## 连贯实施步骤

1. 重算本包baseline关键hash，记录HEAD/工作树/正在写入者；本轮结束HEAD为2df24bb但不能假定此后不变。核对专家包10个摘要和144页候选的实际verdict绑定。

2. 固定source-set与依赖锁；将未跟踪但被代码/候选引用的payload、derivation、CAS逐项纳入必要清单，不git add .，不整体复制runs。干净实施worktree由固定提交建立，需要的未提交输入以白名单复制和hash绑定。

3. 一次base/head同环境对照当前两个失败。C多evidence-limitations：按实际产品需求同步catalog/合同，不删除测试或页面仅为绿。portal.css：查运行时选源、打包复制和清单生成，修源→包一致性而非只改摘要。

4. 确认模块assets为实际主要来源；决定根副本仍需的消费者及生成同步策略。将新PRD/Plan指针加入活文档，封存历史不改；不另造项目管理系统。

5. 记录当前真实测试命令/时间及新包携带方式，给W01–03/W07可运行环境。

## 一次批次验证与完成条件

一次已有40项批次base/head对照（日志分别保存）；资产一致性和隔离安装相关子集；此处不重抓研究/全门户会商。干净source-set含必要未跟踪输入且无敏感/缓存。

出口：已知失败有事实归因与修复或明确责任；不能写历史脏树原因而无证据。若安装修复较大明确转W08但保留阻断状态，不谎称M0全绿。

## 产物与接续

提交代码/合同/必要迁移/集成测试；证据记入本包 evidence/W00-result.md（未来实施产出，本轮不存在），至少含实际源身份、范围、命令/旅程、结果/限制、产物hash、未解决项。新测试文件先在对应现有层落地，不创建第二验收框架。

完成后更新 ../STATUS.md 的当前包和下一安全动作，不逐步骤造检查点。下一步：W01，随后W02/W03；可先做无依赖的W02合同。
缺实质产品决策用原生Ask选择题；普通实现自行决定并留理由，不人为阶段暂停。

