# W08 — HTML-only安装、三宿主、恢复与安全

状态：pending。主执行：gpt-5.6-sol:medium（运行时核实）。

## 输入与边界

依赖：W04/W05ABC/W06/W07。
需求与验收：FR17；G02/10/11/12；V2-11/40；END03/04。
先读：本包PRD、DESIGN对应段、REVIEW对应发现、本文件；再读以下实际定义/消费者/现有测试，不全量重读历史。
允许实现入口（均相对工程根）：application/fresh_install.py、host_smoke_runner.py、host_smoke_scenario.py、acceptance_runner.py；tools/bundle_contract.py；package-manifest.json；公共及宿主Skill适配；相关合同/集成。
不得修改无关用户文件、历史接受记录、旧中文根；新增文件限完成本包所必需。

## 连贯实施步骤

1. 固定release source-set，构建HTML-only核心包+Codex/Hermes/OMP轻适配，一个公开入口；PDF/PPT渲染代码排除，必要PDF原文处理保留。

2. 安装于干净目录并隔离开发source/PYTHONPATH；验证真实metadata/资源/依赖hash，当前portal.css安装错误必须关闭，不靠删verify。

3. 三宿主真实触发相同合同，记录实际模型/宿主身份、独立上下文、失败/恢复/快照/门户语义；模型selector不是执行证据，无上下文能力应预检失败。

4. manifest-only恢复包空目录还原后实际resume/刷新/重建；模拟中断current原子性、漂移重装拒绝，旧历史只读兼容。

5. 包和分享/编辑端安全合并批次：凭据/绝对路径/商业原文、导入注入、路径/链接/Host/Origin/会话/CSRF；不写全新云安全平台。

6. 安装说明和preflight失败指引按非技术同事可理解标准撰写，但不让同事承担验收。

## 一次批次验证与完成条件

一次固定候选包装/实际安装/3宿主/恢复批次，失败只返修相关链并重验；同源码测试与安装运行资产摘要一致。保留非HTML兼容检查准确范围，不跑其真实渲染门。

出口：三真实宿主与隔离恢复有实际产物/回执，不是CLI --help或协议schema通过。缺宿主记UNVERIFIED并阻断RC。

## 产物与接续

提交代码/合同/必要迁移/集成测试；证据记入本包 evidence/W08-result.md（未来实施产出，本轮不存在），至少含实际源身份、范围、命令/旅程、结果/限制、产物hash、未解决项。新测试文件先在对应现有层落地，不创建第二验收框架。

完成后更新 ../STATUS.md 的当前包和下一安全动作，不逐步骤造检查点。下一步：W09完整矩阵。
缺实质产品决策用原生Ask选择题；普通实现自行决定并留理由，不人为阶段暂停。

