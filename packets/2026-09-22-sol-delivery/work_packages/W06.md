# W06 — 个人配置持久化与自包含静态分享

状态：in progress。R24-20 已有 `project share` 开发切片；R24-22 页内配置现可导出/导入并跨同报告页面复用，A/B/C 浏览器切片与 B 当前 0/1、C 未受修订仍为 0 的 CLI 原样分享合同已验证。执行模型/effort无可核验身份回执，不能声明为指定模型。真实同适应症 A/B/C、跨浏览器/移动目录/断网完整新候选、全部视口和发布科学门仍 pending；历史分享通过不得自动继承。

开发侧使用：`ci-workflow project share --root <项目目录> --output <新的zip路径> --reports A,B,C`。可选 `--view-config <json>`，内容仅含 `{"schema_version":"1.0","selections":[{"report":"A","revision":1,"entry_page":"safety.html","query":{"product":["已显示的产品名"]}}]}`；`--reports` 必须与配置中的报告集合一致。配置 revision 必须等于**当前交付包中该报告自己的 revision**：例如 A/B 升至 1、C 未受影响仍为 0 时，C 页面导出的 0 可直接用于当前包，不能伪改为 1。分享清单另保留项目 current generation；无配置时每份报告从 `overview.html` 打开。分享包内的“打开报告.html”只是文件选择入口，不合并 A/B/C 门户。此接口不是历史 accepted/latest 的绕行，也不是发布验收。

## 输入与边界

依赖：W04/W05A/B/C；W02ViewState。
需求与验收：FR13/15；V2-29/38/39；SHARE01–05/UI03/06。
先读：本包PRD、DESIGN对应段、REVIEW对应发现、本文件；再读以下实际定义/消费者/现有测试，不全量重读历史。
允许实现入口（均相对工程根）：reports/common/view_state.py及门户公共assets；application/delivered_artifacts.py、latest_delivery.py、cli.py、share_export.py；现有manifest/归档工具，可按需扩展，不新建分享服务。
不得修改无关用户文件、历史接受记录、旧中文根；新增文件限完成本包所必需。

## 连贯实施步骤

1. 配置保存/导入包含合法schema/revision/视图选择，不能只localStorage；旧revision配置显式迁移或拒绝，任意函数/HTML/script不可执行。

2. 从current实际修订导出A、B、C单包及组合，资源依赖闭合，无融合首页；默认呈现导出时选定配置/修订。

3. 静态file://无需Agent/Python/服务/外网，图表数据不靠本地fetch受限JSON；既有本地图库复用，CDN不当必需。

4. 保留事实修订/来源合法摘录/元信息和版本；受限论文/商业数据按许可不默认附全文，在线链接离线清楚标注。

5. 扫描绝对内部路径、token/cookie、会话、开发目录；新修订不携旧接受冒充新接受。分享包和安装包分别清单。

## 一次批次验证与完成条件

一次分享往返批次：改事实+配置→导出→全新目录→断网新浏览器→清缓存→打开；三单包和组合；恶意配置/路径及版本错配；核实际图/表/来源/状态。

出口：同事打开即当前事实和配置，不需手工patch，不依赖本机环境；编辑需安装端重开且说明。

## 产物与接续

提交代码/合同/必要迁移/集成测试；证据记入本包 evidence/W06-result.md（未来实施产出，本轮不存在），至少含实际源身份、范围、命令/旅程、结果/限制、产物hash、未解决项。新测试文件先在对应现有层落地，不创建第二验收框架。

完成后更新 ../STATUS.md 的当前包和下一安全动作，不逐步骤造检查点。下一步：W08；W07研究刷新并行收口。
缺实质产品决策用原生Ask选择题；普通实现自行决定并留理由，不人为阶段暂停。
