# W01 — 候选信任、逐事实原文与快照闭包

状态：pending。主执行：gpt-5.6-sol:medium（运行时核实）。

## 输入与边界

依赖：W00。
需求与验收：FR04/08；F01–04；V2-03/04/05/07–11；DATA01/02/05。
先读：本包PRD、DESIGN对应段、REVIEW对应发现、本文件；再读以下实际定义/消费者/现有测试，不全量重读历史。
允许实现入口（均相对工程根）：application/fresh_research_ingestion.py、research_package_submission.py、review_issuer.py、scientific_review_transition.py；domain/facts.py、research_package.py；storage/source_derivation.py/snapshot_store.py；对应schemas/migrations；PNH audit builder仅修迁移/兼容接口。
不得修改无关用户文件、历史接受记录、旧中文根；新增文件限完成本包所必需。

## 连贯实施步骤

1. 先一次性加入生产入口反例：无独立签发/复制旧reviewer时间改digest、粗locator、不同source字节、漏fact fragment、内容只改科学context。

2. 允许未复核候选进入研究/渲染工作区；去掉生产者赋予事实独立accepted的权力，保留确定性输入校验。复用真实issuer绑定请求/来源/规则/产物；仅改名字为pending而发布仍绕过不算修复。

3. 从真实来源字节按locator重提取，严格比对对象/quote；翻译/规范值另记派生。旧studies[]+生成文本明确不满足精确定位，不凭格式非空通过。

4. 扩充事实内容身份至影响科学意义全部字段；同ID异内容显式冲突；source version、获取attempt和幂等request分开。

5. 生成传递闭包manifest与返回lineage，包含所有采用fact引用的fragment/source/derivation；在空目录只用manifest恢复。保留历史不可变快照，迁移不补签。

## 一次批次验证与完成条件

一批source→research submit→DB→snapshot→实际接受链集成；现有test_review_r07_ingestion_invariants及source_capture_precision_ingestion/source_to_claim_chain/research_source_binding相关组。红测试集中，修完一次合并GREEN，不逐fragment跑。

出口：原文可重提取、闭包可恢复、新版本/attempt正确、自签无效；旧独立回执不授权新内容。

## 产物与接续

提交代码/合同/必要迁移/集成测试；证据记入本包 evidence/W01-result.md（未来实施产出，本轮不存在），至少含实际源身份、范围、命令/旅程、结果/限制、产物hash、未解决项。新测试文件先在对应现有层落地，不创建第二验收框架。

完成后更新 ../STATUS.md 的当前包和下一安全动作，不逐步骤造检查点。下一步：W02/W03，W07可开始真实来源链。
缺实质产品决策用原生Ask选择题；普通实现自行决定并留理由，不人为阶段暂停。

