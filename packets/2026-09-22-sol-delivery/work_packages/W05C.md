# W05C — C完整条款检索与横比先例库

状态：pending。主执行：gpt-5.6-sol:medium（运行时核实）。

## 输入与边界

依赖：W02/W03；编辑接W04，和A/B同优先级。
需求与验收：FR09/12/13；F09；V2-30–34；C01–07。
先读：本包PRD、DESIGN对应段、REVIEW对应发现、本文件；再读以下实际定义/消费者/现有测试，不全量重读历史。
允许实现入口（均相对工程根）：reports/c/contracts.py、endpoint_instances.py、eligibility_source.py、synthesis.py、pages.py；fresh_c_research_package.py；portal/report_c.py和模板/资产；skills/_internal/analysis-c。
不得修改无关用户文件、历史接受记录、旧中文根；新增文件限完成本包所必需。

## 连贯实施步骤

1. 把默认C完成条件从多候选方案转为设计先例；单项有效研究可用，无candidate_paths数量门。保留旧历史方案可读，不强制生成。

2. 提取所有入排/设计条款并保持母子/AND/OR/NOT/例外/阈值/洗脱期/条件人群，完整原文与译文/主题分别存。无条数/长度截断，计划/结果及方案版本分离。

3. 按W03实例保存所有主/次终点和时间、组/期给药；缺失/冲突逐研究可见，不删研究伪全覆盖。

4. 实现关键词/中英主题搜索→选研究列→共同/差异→原文，设计模式/时间轴/终点矩阵按适格数据绘图；全1无信息图换说明和完整表。

5. 编辑一条C阈值后索引/主题/比较/引用同步，原始quote保留；保存配置/返回状态；C独立交付。

6. 处理当前During/单位省略和中文被标未译回归：显示层标注只基于原文与译文状态，不整体放宽正则。

7. 宽屏优先用于多研究列、端点×时间窗矩阵、组期结构和条款横比；纵向/窄屏收紧卡片gap、padding、标题区和固定图高，以紧凑目录/筛选/摘要建立首屏定位，再按需展开完整条款。不得缩小正文字号、压缩触控目标、截断条款或用横向滚动掩盖布局问题。

## 一次批次验证与完成条件

一批条款/实例/检索集成+完整浏览器连续旅程；多primary、多臂多期、1项研究、缺项研究、嵌套否定、数值符号、全文库存映射；修订索引实际更新；四视口记录内容占宽、首屏关键模块、卡片gap/padding、图高和overflow，配套真实截图。

出口：医学用户可检索横比完整先例，不是方案推荐页或条款摘要墙。

## 产物与接续

提交代码/合同/必要迁移/集成测试；证据记入本包 evidence/W05C-result.md（未来实施产出，本轮不存在），至少含实际源身份、范围、命令/旅程、结果/限制、产物hash、未解决项。新测试文件先在对应现有层落地，不创建第二验收框架。

完成后更新 ../STATUS.md 的当前包和下一安全动作，不逐步骤造检查点。下一步：W06，和A/B一同检查独立交付。
缺实质产品决策用原生Ask选择题；普通实现自行决定并留理由，不人为阶段暂停。
