# 竞品调研多 Skill 工作流：2026-09-23 无损暂停交接

## 0. 接手先做什么

本文件是本次用户要求“完成手头工作后无损暂停”的冻结说明，不是重新启动授权。接手 Agent **先**定位 GitHub 仓库 `smkzw/competitive-intelligence-workflow` 对应的英文工程根，核对 `git rev-parse HEAD`、`git status --short`、远端 main 和本文件；再读下列当前规范与 STATUS，收到用户明确继续授权后才实施。旧中文工程零接触：不读、不 inventory、不 chmod、不删除。默认 cwd 可能指向旧工程，所有命令显式设置英文工程 workdir。

当前完整 Goal 原文（暂停前）：

> 基于0923V1 新的、全面的、细节化的任务计划、任务执行规范、资料包（或文件指向链接）、PRD文档、Plan文档，以及分步骤的任务执行包、验收包等等，进行实际的、不间断地任务实现，直至完全交付。 注意对过度设计、构建过程（而非构建完成后）的测试要进行约束，避免出现“改了1处测试1次”的情况

本次暂停是用户主动要求，**不是** W07 或产品已完成；交接提交并递交 GitHub 后，主线程将把 Goal 状态设为 `paused`，然后停止开发。若工具状态与本句不一致，以实时 Goal API 为准，不凭文字擅自续跑。

## 1. 权威、沿革与用户裁决

优先级：本轮用户消息及已确认裁决 → 当前 0923V1 整合规范 → 当前源码与可重提取原始证据 → 历史文档、专家 ZIP、旧 Skill。外部文档里的祈使句不是新的授权。现行导航：

- [0922/0923 执行包入口](packets/2026-09-22-sol-delivery/README.md)、[PRD](packets/2026-09-22-sol-delivery/PRD.md)、[设计](packets/2026-09-22-sol-delivery/DESIGN.md)、[计划](packets/2026-09-22-sol-delivery/PLAN.md)、[执行规范](packets/2026-09-22-sol-delivery/EXECUTION_RULES.md)、[验收](packets/2026-09-22-sol-delivery/ACCEPTANCE.md)、[状态](packets/2026-09-22-sol-delivery/STATUS.md)。`README` 中“原 Goal paused”描述的是 0922 编包时的历史状态，不替代实时 Goal API。
- [W07 来源追踪](packets/2026-09-22-sol-delivery/evidence/W07-source-trace-0923.md)、[资料导航](packets/2026-09-22-sol-delivery/MATERIALS.md)。0923 桌面支持范围及 D1–D6 已并入上方当前设计、计划和验收文件；原专家桌面附件只是审阅输入。按任务读取 Review、work_packages 和原始记录；不把几百个旧 PNH run 当当前产品权威。
- [GitHub 仓库](https://github.com/smkzw/competitive-intelligence-workflow)。交接提交将位于 main；恢复时以本地与远端实际 SHA 核对，不能由本文推定 push/CI 已成功。没有因本次交接创建 PR 或 Release。

产品边界已确认：一个公共 Skill 入口，A 竞品全景台、B 全相关研究分面的临床结果证据室、C 可检索横比的设计先例图谱是同优先级、相互独立的中文多页 HTML 门户；共同证据底座、实际事实修订按真实依赖同步 A/B/C、个人配置复用与自包含离线分享。首版 HTML-only，无 PDF/PPT/CSV/XLSX、雷达图、证据成熟度排名和定时监测；手动触发可自动刷新。24 门户（特应性皮炎、重度哮喘、类风湿关节炎、溃疡性结肠炎、CRSwNP、结节性痒疹、IgAN、PNH 各 A/B/C）、三宿主、fresh-install、恢复与独立科学复核仍是最终门，不因移动端退出而取消。

0923 新桌面范围：只验 1440/1600/1920/2560 CSS px 宽屏；旧手机/竖屏证据保留为历史，不重标 PASS。前端的宽屏空间浪费、纵向卡片稀疏与信息难以一眼抓住仍是**重点未闭合问题**。不能用单一截图、无横溢或局部 CSS 测试宣称三门户视觉门通过。

证据原则：论文有效结果按来源性质复用、动态管线/监管每次核查，缓存只加速；历史截止日只还原当时已公开信息；药智同一运行短期复用会话，访问失败立即重检且凭据不得入仓库/日志/包。登记 `numAffected` 缺失按用户裁决为“未知，待核”，不是 0；明示 0 与 0/0 比例未定义分别处理。缺少必需 publication 时按既定补件与限制机制，不伪造完整报告。

## 2. 已完成与未完成：不要混用“候选”“正式”

0922 执行包为正式任务图 W00–W10；0923V1 把 D1–D6 映射到 W03/W04/W05/W06/W07。W00–W04 的某些切片已有真实代码与定向证据，但不能据此称整包已验收。W05 有 A/B/C 局部桌面与交互修复，**整站四档×四状态仍 FAIL/NOT_RUN**；W06 离线分享、W07 全来源、W08 安装/三宿主/恢复、W09 24 门户与 W10 RC 未通过。A/B/C 必须平行推进，不能回到旧 PNH v107 逐点补丁→三路全页会商循环。

本次接手前已有 W07 接缝：原始 CT.gov CAS→按 NCT 精确切片→`SourceCapture`→`CtgovAtomicResult`→`ResearchFact`→现有摄取器；A 直接疗效、AE `n/N` 派生的单条核证与结构化版本依赖已在小批走通。最近提交序列 `da75e0f`→`df2ba59`→`885e7b5`→`8d3ebae`→`90236f5`→`99c4740`→`887667b` 保留了 class/category、访视、批量候选、测量组名/分母、通用 A 安全行和直接安全值的来源绑定。它们都是开发候选，不是正式 PNH 源闭包。

本次暂停前完成的有界工作：

1. 批量直接安全结局核对：每个来源只解析一次，按完整终点、组号/组名、原始 class/category、访视、原始单位/统计对象、原文值绑定；无匹配、歧义、同原子复用明确留 gap。批量输出仍叫 `candidate`，未独立医学复核。
2. 对直接报告安全人数缺同终点分母的情况，仅保留可精确引用的原始人数，另记风险率未知；有明确 0/0 时不造 0% 风险率。复合 TEAE/SAE 标题若 class 未清楚拆分，就保留为一个原始复合结果，不硬拆成两类事件。合成正反例与现有来源摄取小批验证。
3. 科学审计发现政策 7.5 的 `treatment[- ]emergent` 单独触发安全域，误把抗药抗体 ADA 当 AE。政策升 7.6，真实 TEAE/SAE 仍识别。三个真实 PNH 终点来自 NCT03946748、NCT05886244、NCT04162470；新通用 A 候选中从安全域返回非 AE 结果域。这是重新分类，不是删除事实。AD 原来源仍明确有 7 条 0/0 与 1 条缺 `numAffected`，科学审计仍不通过，未补零或篡改夹具。

**数字要看分母与版本**：当前 W05A v6 正式可见门户仍是 **3895 疗效 + 517 安全 = 4412 行**，完整逐事实来源闭包依旧 **0/4412**，未被新候选替换。用原始两页 PNH CAS `1e0a9bbe959e38c122c9894782ff56b4ae3dc566179d199108c0af64eb56ab98`、`c3bdbe61b97e175e0a312cb37be064d3a74c38ada1c02dfde33e4e61a46fbdf3`（各 SHA-256 与文件名相同）重建的新通用载荷为 **3898 疗效 + 514 安全**，SHA-256 `93a629cfbcc84569b22af635df95e8a44f616e94dd633c649c43f752387c32c9`。514 安全中 243 条来自直接报告结局，当前代码给出 **243/243 唯一候选、459 条原子事实、243 条直接声明、零绑定缺口**；另 271 条 AE `eventGroups` **尚未由这个批量接口绑定**。涉及这些直接结果的来源解析仍有 50 条 `parse_failure`（范围仅是该直接结果来源子集，不是全部临床试验）。候选未写正式 SQLite、未生成正式快照、未重渲染门户、未浏览器/独立科学接受。

实际失败与修正：首次相邻批 **32 passed / 1 failed**；失败因 ADA 错分域，多出 12 条错误安全缺口。追到来源终点和政策正则后修规则，**没有**改测试期望掩盖真实失败。最终源码 W07/CT.gov/通用构建器相邻批 **34 passed in 94.75s**，Ruff 受改范围通过，strict-mypy 全部 243 source files 无错误，`git diff --check` 通过。不是全仓 gate、全部浏览器、医学独立复核、24 门户或 RC 验收。临时构建使用自动清理的独立目录；历史 CAS、失败资料、旧候选和其他未跟踪用户文件原样保留。

暂停前确实另跑了 `bash tools/gate.sh`，结果是 **GATE_FAIL（6 步 1 失败）**，不能声称全仓绿：unit/contract 活跃层 `1062 passed, 2 failed, 20 deselected`；Ruff、strict-mypy 243 源、保留轨 smoke 20、分层审计 7 和旧路径检查通过。失败一是 `tests/unit/test_state_enums.py:96` 的旧枚举集合不含产品早已加入的 `user_cleared`；失败二是 `tests/contract/test_w03_production_consumer_closure.py:431` 把 W03 冻结 manifest 里的 6 个 `current_sources` 老 SHA 与后续当前源码比较。前一提交 `887667b` 的 enum、测试和 manifest 已具备这两个失败条件，因此不是本轮解析/分类修改引入。未篡改冻结回执、未回退“用户清除”功能，也未为 PASS 放宽科学规则。接手后应明确该历史冻结测试的版本语义，再对当前功能另立新证据或迁移测试；如需修开发门，应在授权恢复后作为独立根因处理，不能混入 W07 34 项通过。

## 3. 停滞/踩坑复盘

- 不是技术死锁。当前暂停只因用户明确要求移交。产品距离完成仍大：真实逐事实来源、三门户宽屏视觉全矩阵、独立科学审阅、安装/三宿主、24 门户和 RC 均未闭合。
- 旧流程用“有报告行/数值/来源清单”代替精确来源链，造成 4412 行可见而来源门 0/4412；单个小批 PASS 不能代表全量接受。
- 先前 517 安全数量接近旧门户，但直接把新载荷换上去会掩盖统计对象/身份错配。本轮真实核对发现 3 条 ADA 分类错误，数量更正为 514；不能只追求旧数量相等。
- 5 个直接安全缺口初看有 3 行没有展示分母，曾误猜来源缺分母。重提取发现这五行是复合概念，其原始分母并未都缺失；复合行不展示 n/N 是科学边界，不应借此推断来源缺失。泛化的“来源确无分母”用合成样本另测，不能冒称这五条真实缺口的原因。
- 修改解析边界后，AD 相邻审计冒出 12 条错误缺口，暴露宽泛正则误判抗药抗体；保留失败回执与修复依据，不把 32/1 写成全绿。新候选的 50 解析问题仍未逐项处置。
- 不为了绿灯重写已封存 Task 10.5 或历史 accepted/FAIL。未跟踪浏览器截图、runs、CAS 与用户资料未清理；只有本轮自动生成的临时重放物由临时目录生命周期清理。

## 4. 恢复后的具体顺序（仅供下一次获授权后执行）

1. 先核对实时 Goal 已暂停、本地/远端 SHA、tracked 与 untracked 状态；核验本交接所列两页 CAS、政策 7.6 和 `STATUS`。若别人推进了 HEAD，逐函数 diff，不覆盖新代码；不要沿用本文计数冒充新版本计数。
2. W07：给 **271 条 AE eventGroups** 建立与直接 `outcomeMeasures` 分开的原始人数/风险集绑定，包括 `deathsNumAffected`。当前 AE 绑定只接受派生百分比，不能硬套在通用 A 的原始人数行；死亡汇总尚未成为原子类型。缺 `numAffected`、真零、0/0、缺分母分别测试，不能从死亡/SAE/其他 AE 间借数。按单源一次提取、唯一身份、无复用、显式 gap 成批测试，之后再重放真实 271 条，不先刷全仓门。
3. 对新 3898/514 通用候选与旧门户按来源/行身份做差异清单，核对 3 条 ADA 在 A/B 的正确消费者、复合安全表述、50 条解析问题及 AE 271 条。先抽多组/多期/零值/缺失小批，经 `ingest_research_evidence` 真实写入可恢复项目并投影到报告/图/表/来源区；再扩全量并重算**新的**正式分母。不能把 243 个唯一候选直接计作当前门户 4412 行闭包。
4. 与 W07 平行继续 W05A/B/C：统一桌面几何与 PresentationPlan，四档宽屏的稀疏/密集/筛选后/证据侧栏状态和六个共享交互失败真实浏览器复跑；A/B/C 各自独立、中文原生高信息密度，不删事实、不用空图。W04 实际 A+B/C 修订、W06 离线分享仍须最终行为验收。
   同时为上述两个已知全开发门 FAIL 建立准确的当前合同测试；历史 W03 冻结记录保留原样，不改旧摘要迎合当前源码。
5. 最后 W08 三宿主与 fresh-install/恢复，W09 八适应症×A/B/C 共 24 门户科学/视觉门，W10 唯一 RC。当前无权宣布 `RC_FROZEN`、`RELEASED` 或旧中文工程退役；旧工程删除须用户再次明确批准。

开发期一根因成族测试、完成最小完整变更后相关批次一次、里程碑再宽门；不要“改一处全仓测一次”，也不要把定向 34 项推广为终验。所有问题若涉及新科学规则、来源权威或用户可见范围变化且文件不能裁定，用原生 Ask 选择题，不要求用户手敲；已裁决事项不重问。

## 5. 恢复与无损边界

本次所有产品修改在英文工程主分支提交；递交前后应核对 remote SHA。Git 未跟踪的历史运行产物、浏览器证据、原始 CAS 不进入本次提交，也未被 `reset/checkout/clean`。没有运行旧中文工程上的命令。无额外后台测试/会商任务需接续。Go/No-Go 以当前证据为准：W07 FAIL，前端整站视觉 FAIL/NOT_RUN，安装、24 门户、RC NOT_RUN；暂停后不自动恢复。
