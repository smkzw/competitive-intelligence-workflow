# R13 浏览器验收路由更新

## 用户修正

从本检查点起，本项目所有浏览器相关测试统一基于 ego(lite)，独立视觉测试者的任务提示也必须明确使用 ego(lite)。

## 已完成

- 已停止 Playwright 浏览器二进制安装，不再将其作为 R13 验收前置。
- 已撤销本轮执行角色新增的 Playwright R13 产品验收文件。
- 已把 ego(lite) 写入 R13 设计、实施记录和任务元数据。
- 既有 Playwright 测试文件保留为历史代码资产，不删除用户或前序工程成果，但其结果不计入当前候选的浏览器验收。

## 技术故障记录

首次安装 Chromium 1228 时，171 MiB 下载达到 100%，随后安装器读取临时压缩包报 `ENOENT`；这说明下载通道可达，失败发生在本机临时文件/解包阶段。固定临时目录的第二次尝试在用户指定 ego(lite) 后主动终止，未继续消耗时间和存储。

## 下一安全动作

完成非浏览器确定性回归和源代码集成审查，生成新的 R13 候选；随后使用同一 ego(lite) 任务空间完成 A/B/C 四视口真实交互、截图与几何验收。
