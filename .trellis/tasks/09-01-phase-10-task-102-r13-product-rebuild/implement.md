# R13 实施记录

## 当前

- 已撤回 R12 产品验收并保留其全部证据。
- 已中断并冻结 Task 10.3。
- 已完成 A/B/C 数据投影、语义匹配、渲染器与真实浏览器页面的根因审计。
- 已完成 R13 数据、交互、图表、矩阵、响应式与可访问性设计规格。
- 已完成治理执行包的四个工作项；A/B/C 的第一轮实现已进入 Codex 集成审查，执行角色不能自行判定验收通过。
- 已确认项目虚拟环境的 Playwright 浏览器缺件；一次默认安装在下载完成后因临时压缩包消失而失败，第二次固定临时目录尝试因用户将浏览器验收统一调整为 ego(lite) 而主动停止。该问题属于测试环境技术故障，不是页面结论。
- 已把当前任务的浏览器验收入口切换为 ego(lite)，撤销本轮新增的 Playwright R13 验收文件；既有历史用例不作为当前产品验收证据。
- 已形成不可变视觉候选 `task-10.2-20260902-043000-r13h`。A 报告产品气泡、B 报告治疗组/对照组横向比较与 C 报告全字段矩阵沿用 R13g 已修复结果。
- 已定位 C 报告入排标准大量缺失的根因：20 份 ClinicalTrials.gov 原始登记文件完整，但上游 `report-c-data.json` 每项研究仅保留一条代表性入选/排除标准；这不是“未公开”，也不是渲染器丢数。
- 已新增 `eligibility_source.py`，从锁定的官方登记源完整恢复入选/排除标准；任何缺源、重复、身份不一致或空原文均阻断报告生成。当前 C 候选展开 148 条入选标准、276 条排除标准，逐条保留唯一表格身份并可回到原始登记依据。
- 已将 C 入排标准图改为“产品｜NCT号”横向条形图，显示每项研究公开条目数；点击条形可打开对应试验与设计要素的数据依据。
- 已清理 C 单研究详情表中直接暴露的 `target_population`、`inclusion_criterion` 等内部字段名，改为“来源位置”和中文设计要素；英文仅作为来源原文保留在依据层。
- 确定性回归：`node --check` 通过，C 数据恢复、精确下钻、A/B/C 静态合同与 B 语义分组相关 67 项测试通过。
- ego(lite) 实测：排除标准页 276 行且 276 个唯一条目身份，图表真实点击打开正确来源抽屉；NCT02260986 详情页完整展开 2 条入选和 12 条排除标准，内部字段名不再可见。
- ego(lite) 全站回归：114 条 A/B/C 路由 × 1024/1280/1440/1920 四档宽度，共 456 次页面运行；Logo、主内容、页面级横向溢出和禁用内部文案均 0 缺陷。
- R13i 将 A 抽屉中的项目最高阶段、当前证据试验、证据试验分期、疗效时间点和安全性观察窗拆为独立口径；B 安全性页补齐中文事件名和图间色阶不可比较说明；C 将完整研究 × 设计要素矩阵前置到首屏。
- 三位指定测试者在既有会话中只使用 ego(lite) 完成 R13i 复验：ZCode 与 Gemini 未发现阻断；MiniMax 发现 B 的 AESI 数据依据标题仍保留英文。
- R13j 将该抽屉标题和字段统一为“特别关注不良事件”。Codex 使用 ego(lite) 真实点击 25.8% 数据点，核对 16/62、网址定位、四档宽度和页面级横向溢出；MiniMax 在原会话复验确认英文残留为 0、Escape 回焦正常。
- 最终确定性回归为 70 项通过；执行包独立审计通过，独立视觉会商包校验通过。执行包与视觉会商按全局规则分属两个独立包，因此不使用要求同一任务编号内嵌会商的组合开关作为验收口径。

## 下一步

本任务已闭环。后续从总实施计划中选择下一项未完成的站点式 HTML 工作；PDF、HTML-PPT 与 PPTX 仍按用户要求暂缓。
