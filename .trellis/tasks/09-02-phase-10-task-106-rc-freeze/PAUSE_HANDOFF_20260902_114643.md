# Task 10.6 无损暂停与续接 Handoff

暂停时间：2026-09-02 11:46:43 CST  
工作区：`/Users/smkzw/Documents/AI Products/competitive-intelligence-workflow`  
当前 HEAD：`bb27ec9`  
任务状态：Task 10.6 `in_progress`，人为无损暂停；未完成、未验收、未冻结 RC。

## 1. 暂停动作与安全状态

- 用户明确要求“无损暂停，写详细 handoff”。收到后立即向仍运行的 unified exec session `94056` 发送 Ctrl-C。
- `conference_session_runner.py` 以 `KeyboardInterrupt` 退出，退出码 `130`。没有使用 kill -9，没有删除、移动、回滚、reset、checkout、clean 或覆盖任何工作区内容。
- worker_01 在中断前留下的源码/测试修改全部保留；其 runner-managed 报告仍为 `PENDING`，因此这些修改一律视为**部分实现、未验收**。
- worker_02、worker_03 的完整报告和 stdout 日志保留；没有运行 `cleanup-execution --apply`，以便续接时审计完整证据链。
- 暂停后检查未发现仍在运行的本任务 runner/ZCode 进程；`pgrep` 仅回显检查命令自身 PID，不构成存活 worker 证据。
- 没有创建 commit/tag，没有写外部 acceptance root，没有安装 bundle，没有调用真实宿主，没有访问、读取或向工具传入真实旧根 `/Users/smkzw/Documents/AI Products/竞品调研工作流`。

## 2. 上一稳定检查点

Task 10.5 已完成并封存：

- 检查点：`.trellis/tasks/09-02-phase-10-task-105-cutover-tools/checkpoint_20260902_completed.md`
- 最终证据：26 项聚焦测试、166 项扩展回归、Ruff、mypy、`LEGACY_REF_OK`、独立会商 P0/P1/P2=0、review-gate 与同 ID audit 均通过。
- Task 10.5 没有触碰真实旧根；Task 10.7/10.8 仍是未来受控动作。

这是续接 Task 10.6 时可依赖的最后一个完整接受点。Task 10.6 的任何文件都不能反向降低 10.5 的授权、恢复、残留或 owner-stage 失败关闭合同。

## 3. Task 10.6 目标与阶段

任务合同位于：

- `.trellis/tasks/09-02-phase-10-task-106-rc-freeze/prd.md`
- `.trellis/tasks/09-02-phase-10-task-106-rc-freeze/design.md`
- `.trellis/tasks/09-02-phase-10-task-106-rc-freeze/implement.md`
- `.trellis/tasks/09-02-phase-10-task-106-rc-freeze/task.json`

四阶段设计：

1. **10.6A**：clean-commit bundle provenance、最终 fresh-install 内容合同、recovery-package-v1 生产者/严格回执/隔离演练。
2. **10.6B**：显式 release source set、Phase 0-10 回归、隔离 clean worktree、唯一 RC commit/tag。
3. **10.6C**：从最终 commit 构建/验证/fresh-install bundle，使用全新 release root 重跑 A/B/C 与 full-matrix。
4. **10.6D**：恢复演练、owner-stage receipts、科学/视觉/包/恢复独立验收与最终 freeze。

当前只进入了 **10.6A**。10.6B/C/D 均未开始；所有 implement checklist 保持未勾选，避免把 worker 输出误当成主 Agent验收。

## 4. 治理执行包

任务 ID：`ci-phase10-task106-rc-freeze`  
类型/风险：`long_horizon_code` / `high`  
主路由：`zcode / GLM-5.3-Flash / max`  
执行上下文：`context/ci-phase10-task106-rc-freeze_execution_context.md`  
执行计划：`plans/codex_execution_ci-phase10-task106-rc-freeze.md`  
路由清单：`context/ci-phase10-task106-rc-freeze_execution_route_manifest.json`

三个 worker prompt 均已通过 guard preflight。当前执行包**不能 audit 通过**：worker_01 未完成，execution review/metrics 未填写，Task 10.6A 尚未经过 Codex 集成验证。不要运行 review-gate/audit 并声称通过。

## 5. Worker 状态

### worker_01：bundle provenance / fresh-install 内容合同

状态：**被用户暂停中断；未完成、不可接受。**

- unified exec session：`94056`
- 中断退出码：`130`
- `runs/execution/ci-phase10-task106-rc-freeze/worker_01.md` 仍只有 `Status: PENDING`。
- runner 在中断前未生成 `logs/execution/.../worker_01_stdout.txt`，因此没有可审计的内部 ZCode session id、最终命令表、完整测试结果或终态报告。
- 该 worker 确实在 11:31-11:44 修改了授权文件；这些修改已保留，但没有主 Agent验收：
  - `tools/build_bundle.py` — SHA-256 `cfe35315b1a7c53c2090c91b5ba62d563825eadbd508b2b8ddb3e24fa0b6ff6b`
  - `tools/verify_bundle.py` — `31d6a1ddf31dc8c6fd282d3d83d6e1e71985d23a8411e32e4842aa7b45b1fc30`
  - `tools/bundle_contract.py` — `538d7754b3b67801d9da123486ccf0f12868d5f5317ec6f4e592f1f6a0ac775c`
  - `tests/hosts/test_fresh_install.py` — `151bc954fcc0401de5675775dc8d027236ac185afeeb7e2888af66526b8f3722`
- 静态观察到的部分实现：`--from-clean-commit`、`--expect-source-commit`、Git root/HEAD/dirty fail-closed、bundle manifest `source_commit`、`BUNDLE_OK source_commit=...`、最终 required content 检查。
- **未验证**：逻辑是否正确、是否破坏旧 bundle 兼容性、测试是否通过、required content 是否违反 10.5 “治理工具不进入运行时 bundle”的既有裁决。
- 不要假设旧 worker 会话可恢复；runner 未留下可用的内部 session id。续接应先由 Codex审计当前字节，而不是立即重派。

### worker_02：recovery-package-v1

状态：**worker 报告完成；仅完成局部机械复核，尚未通过 Task 10.6A 总体验收。**

- 报告：`runs/execution/ci-phase10-task106-rc-freeze/worker_02.md`
- 日志：`logs/execution/ci-phase10-task106-rc-freeze/worker_02_stdout.txt`
- ZCode session：`sess_6405d497-5df8-475c-8492-54f8a32b6d1c`
- runner 记录第一次 returncode 1 后在同一 ZCode 会话完成受控恢复，最终 returncode 0；没有跨模型 fallback。续接审计时需保留这个事实，不能只写“首次通过”。
- 新增文件：
  - `tools/build_recovery_package.py` — SHA-256 `a5207141889139cf3e641f7883949901e0cc860f9ad6e93ad0a98cb8725e2721`
  - `migration/recovery_package.schema.json` — `8e6dccf58c0b12cd723b68dd9fe062b72c595542d7f21c758748bda44aa72b32`
  - `migration/recovery_package_receipt.schema.json` — `16a45390933e3cb82eb843d26bf19bbb28be39a244dd047fd94ccdb1a2c3074f`
  - `tests/migration/test_recovery_rehearsal.py` — `9c7c5412a04b4fa4802946e3d8abd36e2e933a8c1c5613a420ea626f166aa246`
- worker 自报：迁移集合 42 passed；相邻闭包 10 passed；Ruff/format-check/mypy strict 通过。
- 暂停前 Codex 独立重跑：`tests/migration/test_recovery_rehearsal.py` **17 passed**，Ruff 通过，严格 mypy 两文件通过。
- 尚未运行全仓或完整相邻回归；schema 放在 `migration/` 而非 `schemas/` 是刻意避开 package schema 双向闭包，但仍需主 Agent确认它是否应进入最终恢复包/RC 来源清单。

### worker_03：full-matrix / receipts / freeze 只读审计

状态：**完成只读审计，无源码修改。**

- 报告：`runs/execution/ci-phase10-task106-rc-freeze/worker_03.md`
- 日志：`logs/execution/ci-phase10-task106-rc-freeze/worker_03_stdout.txt`
- session：`sess_7c53ec47-2b6c-430c-8a05-5783042560ea`
- duration：536.162s；fallback：无。
- 核心缺口：RC identity 未定义；release-case-v1 无生产者；旧 run/session/artifact/verdict 不能机械拒绝；catalog pre-RC 指针与 final release receipt 分层尚未由 freeze 编排显式绑定；`legacy-absence` 可被过早 accepted；安装包不含完整验收执行面；`formats=4` 无机械来源。
- 报告给出 T1-T5 测试组和 10.6C-D 交接，但它是建议，不是既定合同。

## 6. 当前工作树事实

- HEAD：`bb27ec9`；暂停时 `git status --porcelain | wc -l` 为 847。
- 847 包含长期项目改动、用户改动、前序 Task 产物、运行证据和本次新增文件，**不能**解释为 Task 10.6 的改动数，也不能用 `git clean/reset/checkout` 处理。
- Task 10.6 新增治理/任务文件、execution packet、worker 报告/日志全部保留。
- `package-manifest.json` 与 `schemas/package-manifest.schema.json` 在 worker_02 开始前已经是 modified；不要把它们自动归因给 worker_02。
- 当前未创建 release commit/tag，外部 acceptance root 未写入，真实旧根未触碰。

## 7. 暂停时尚未裁决的关键合同

### Q1：RC identity

`release_candidate_sha256` 不能继续接受任意 64 位占位值。建议主 Agent采用规范复合摘要：

`sha256(canonical_json({schema_version, source_commit, bundle_sha256, package_manifest_sha256, catalog_sha256, release_scope}))`

原因：当前仓库 commit 很可能是 SHA-1（40 位），不能直接冒充字段名明确要求的 SHA-256；复合摘要也能把 commit、最终包和 catalog 绑定为一个 RC 身份。该建议尚未编码或验收。

### Q2：pre-RC host receipt 与 final owner-stage release receipt 分层

Task 10.5 已明确两层信封：catalog 的 `schemas/host-receipt.schema.json` 是 pre-RC 运行回执；`release-case-receipt.schema.json` 是 acceptance root 下最终 owner-stage 信封。优先建议保持双层，不机械修改 18 个 catalog 指针和 case digest；应在 freeze record/producer 中显式绑定两层证据及路径。worker_03 的“直接改 catalog 指针”仅是备选，不能直接实施。

### Q3：`legacy-absence` 必须保持唯一 future owner

Task 10.6 冻结结果必须是 `pending_future=1`，唯一对象为 `legacy-absence`。现有 `verify_release_receipts.py`/测试允许它 accepted，属于冻结门前必须修复的严重缺口。Task 10.6 不得提前关闭 Task 10.8 的责任。

### Q4：切换工具是否进入可安装 bundle

存在真实合同冲突：

- Task 10.5 已记录两个 Python 工具为“仓库内治理工具，不属于可安装运行时 bundle”。
- 批准的 Task 10.6 计划又要求 fresh bundle 包含 cutover tools；worker_01 当前 `bundle_contract.py` 也已把 `tools/legacy_cutover.py` 纳入 required content。

续接时必须先裁决：是把切换工具放入单独 release-governance sidecar/source freeze record，还是扩大运行时 bundle。未裁决前不要接受 worker_01 的 required-content 改动。

### Q5：安装入口与验收测试的关系

最终产品运行必须来自安装 bundle；但 catalog verifier 当前硬编码 `uv run pytest` 并依赖 RC source tree。建议区分：产品 create/run/render 从安装入口执行；治理 verifier 从 clean RC worktree 执行，同时在 receipt 中绑定同一 RC commit 和 bundle digest。不要为了“全都从安装包运行”把整个测试树塞进用户安装 bundle，除非重新批准 bundle 边界。

### Q6：`formats=4` 的权威来源

应来自同一 final run 的 A/B/C × HTML/PDF/HTML-PPT/PPTX artifact manifests/receipts，而不是 catalog 当前 html-only 计数或静态常量。冻结信号必须由实际 artifact closure 计算，不能硬编码后直接输出。

## 8. 已验证与未验证清单

### 已验证

- Task 10.5 完整闭环（见上一稳定检查点）。
- Task 10.6 task/context/plan 已建立，3 个 worker prompt 均通过 preflight。
- worker_02 recovery focused：17 passed；Ruff；严格 mypy。
- worker_03 完成只读审计。
- 暂停动作已终止 worker_01 runner，保留文件，无 destructive cleanup。

### 未验证/不得宣称

- worker_01 的任何测试结果或实现正确性。
- Task 10.6A 的整体完成、execution review-gate 或 audit。
- Phase 0-10 全量回归。
- clean release source set、RC commit/tag、最终 bundle、fresh install。
- A/B/C fresh-source rerun、四格式、cross-format、三真实宿主、full-matrix 当次运行。
- recovery package 在真实最终 bundle 上的演练。
- release-case producer、freeze record、owner-stage closure、`RC_FROZEN`。
- 科学、视觉、PDF/PPT/浏览器、包/恢复独立验收。
- 任何真实旧根 inventory、切换、删除或 absence closure。

## 9. 下一次续接的最安全顺序

1. 先读本 handoff、Task 10.5 checkpoint、Task 10.6 PRD/design/implement 和三个 worker 报告。
2. 用 `pgrep` 确认本任务 runner 不存在；不要运行 cleanup。
3. 重算第 5 节列出的 8 个文件 SHA-256，若任何不一致，先停止并定位外部改动。
4. 逐文件审计 worker_01 当前字节，重点检查：Git root/dirty 语义、在输出前失败关闭、manifest 兼容、allowlist/required content 是否违反 Q4/Q5、fresh-install 测试是否只写 tmp。
5. 先裁决 Q1-Q6，并把裁决写入 Task 10.6 design/checkpoint；不要直接根据 worker 建议改 catalog digest。
6. 在裁决后补齐负向测试，再运行 worker_01 聚焦测试与 worker_02 相邻回归；只有当前字节全绿才勾 A01-A03。
7. 填 execution review/metrics，必要时为 worker_01 开新的受治理审计/修复会话；旧 session 不可假定可恢复。
8. 通过 execution audit 后才进入 10.6B source closure。创建 commit/tag 前再次确认用户改动保留和 release source set 显式闭合。

建议的第一条只读命令：

```bash
cd '/Users/smkzw/Documents/AI Products/competitive-intelligence-workflow'
sed -n '1,320p' '.trellis/tasks/09-02-phase-10-task-106-rc-freeze/PAUSE_HANDOFF_20260902_114643.md'
shasum -a 256 tools/build_bundle.py tools/verify_bundle.py tools/bundle_contract.py tests/hosts/test_fresh_install.py tools/build_recovery_package.py migration/recovery_package.schema.json migration/recovery_package_receipt.schema.json tests/migration/test_recovery_rehearsal.py
```

## 10. 严禁事项

- 不得删除/清理 execution packet、worker 报告或日志。
- 不得 reset/checkout/clean 当前 847 项 dirty tree。
- 不得把 worker_01 的 PENDING 文件当成 accepted。
- 不得创建 RC commit/tag，直到 10.6A 验收和 release source set 闭合。
- 不得写外部 acceptance root、调用真实宿主或使用旧 R13/pre-RC receipts 冒充当次证据。
- 不得读取或操作真实旧根；Task 10.7/10.8 权限没有提前授予。
- 不得输出 `RC_FROZEN`，直到 10.6C/D 全部门实际通过。

## 11. 下一安全动作

**仅进行 worker_01 当前四个文件的只读审计，并裁决 Q1-Q6；在审计完成前不继续执行、测试、commit、安装或外部写入。**
