# Phase 7 技术设计

- 复用现有事实、缺失状态、证据 locator、快照、图表—表格同步和证据抽屉合同，不建立第二套平行状态。
- `c_design_observation` 是 C 类事实原子；门槛只依据版本化规则判断关键字段完整性。
- 完整下钻链固定为：适应症 → 产品 → 试验 → 队列/组别 → 设计要素 → 原始字段 → 量表/评分 → 时间 → 运算符/阈值/值/单位 → 来源版本与 locator。
- 页面和综合层只消费类型化视图模型，不直接读取原始证据文件。
