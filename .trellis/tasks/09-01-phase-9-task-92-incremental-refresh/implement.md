# Task 9.2 实施步骤

- [x] RED：新增 `tests/integration/test_incremental_refresh.py` 两个批准节点及中断/重大变化反例。
- [x] 实现 `src/ci_workflow/graph/impact.py` 的稳定影响闭包、复用集合与摘要。
- [x] 实现 `src/ci_workflow/graph/definitions/refresh.py` 的刷新节点/分支合同。
- [x] 实现 `src/ci_workflow/application/refresh_service.py` 的截止日扩展、门槛收紧、局部重建回执与幂等恢复。
- [x] GREEN：运行 Task 9.2 指定节点与整套增量刷新测试。
- [x] 回归：运行历史截止日与门槛覆盖严格性测试。
- [x] 运行目标 Ruff 与 mypy。
- [x] 独立审查影响范围、不可变历史、草稿阻断与恢复边界；治理审计通过后收口。

## 独立复审修订

- 门槛结果绑定本轮子规则版本和规则指纹；父规则结果不能先行登记。
- 门槛结果、报告快照与本轮证据快照必须同源；科学质控引用必须存在于已锁定证据快照及报告声明中。
- 变化台账完整覆盖影响与复用对象，支持新增、变化、撤回、修订、取代和未变化六种状态。
- 每个页面必须显式投影到交付格式，依赖边不得跨层；首版格式仍仅为站点式 HTML。
- 科学质控结论绑定实际审查输入包，否决或超过有效期均阻断版本接受。
- 影响图完整登记由编排层负责；本服务验证图内闭包与页面到格式投影，不从存储层猜测遗漏关系。

## 回滚点

若父版本发生任何字节变化、影响集合不确定、候选晋级越过截止日或关键门槛未通过仍生成报告，立即停在本任务，不接入站点按钮或宿主。
