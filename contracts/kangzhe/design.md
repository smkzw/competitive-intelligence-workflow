# 竞品调研工作流 · 康哲设计规范入口（非全文）

> **项目完整合同位于**：同目录下 `design_specs/`  
> Agent 必须按 `design_specs/ROUTER.md` 指定的项目路线加载。  
> **本文件不再承载全文 MUST/NEVER**，仅作兼容入口。

## 立即开始

1. 打开 [`design_specs/ROUTER.md`](design_specs/ROUTER.md)
2. 读取 [`design_specs/core.md`](design_specs/core.md) 与 [`design_specs/project_profile.md`](design_specs/project_profile.md)
3. 按项目路线读取对应 [`design_specs/track_*.md`](design_specs/)
4. 使用 [`design_specs/assets/logo_bot.svg`](design_specs/assets/logo_bot.svg)

## 历史

本项目于 2026-08-11 从当时稳定的康哲通用 `design.md → design_specs/` 内化；来源摘要见 `manifest.json`。此后本目录独立演进，不读取或同步通用版设计文件。
