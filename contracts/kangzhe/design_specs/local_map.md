# 项目路径说明（可移植）

本文件只记录项目根相对路径。运行时不读取通用版 `design.md`，也不读取个人机器上的样例、缓存、历史成片或失败样本。

| 资源 | 项目相对路径 |
|---|---|
| 设计入口 | `contracts/kangzhe/design.md` |
| 完整设计合同 | `contracts/kangzhe/design_specs/` |
| 康哲 Logo | `contracts/kangzhe/design_specs/assets/logo_bot.svg` |
| 离线前端资产 | `assets/` |
| 设计来源与当前摘要 | `contracts/kangzhe/manifest.json` |

后续设计变更只修改本项目合同并更新 `manifest.json`；不读取通用版、不要求双文件同步。
